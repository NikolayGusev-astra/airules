# 🚀 Быстрый старт с AIRules

Полный гид по настройке AIRules за 5 минут.

---

## 🎯 Выберите ваш сценарий

### Сценарий1: Быстрый прототип (5 мин)
### Сценарий2: Enterprise проект (15 мин)
### Сценарий3: Memory Graph интеграция (10 мин)
### Сценарий4: Sequential Thinking (8 мин)
### Сценарий5: Accounting система (20 мин)

---

## 🚀 Сценарий1: Быстрый прототип (5 мин)

**Для:** Новичков, быстрого прототипа, небольших проектов

### Шаг 1: Скопируйте базовый шаблон (1 мин)

```bash
# Скопируйте базовый .cursorrules
cp templates/.cursorrules .cursorrules

# Скопируйте базовый INSTRUCTIONS.md
cp templates/docs/INSTRUCTIONS.md docs/
```

### Шаг 2: Определите технологический стек (2 мин)

```bash
# Создайте файл TECH_STACK.md
cat > docs/TECH_STACK.md << 'EOF'
# Технологический стек проекта

## Frontend
- Framework: React 18
- Language: TypeScript 5.0
- Styling: Tailwind CSS

## Backend
- Runtime: Node.js 20
- Framework: Express 4.18
- Database: PostgreSQL 15

## Инструменты
- Testing: Jest
- Linter: ESLint
- Formatter: Prettier
EOF
```

### Шаг 3: Начните разработку (2 мин)

**В Cursor AI:**
1. Откройте свой проект
2. Cursor автоматически прочитает `.cursorrules`
3. Начните давать задачи: "Создай компонент Button"

**В Claude/Cline:**
1. Создайте файл `docs/INSTRUCTIONS.md` с контекстом
2. Добавьте `@docs/INSTRUCTIONS.md` в начале вашего запроса
3. Начните разработку

### ✅ Что вы получили:

- ✅ Базовые правила для AI
- ✅ Чёткое определение технологического стека
- ✅ Автоматическая проверка стека
- ✅ Готовность к быстрой разработке

---

## 🏢 Сценарий2: Enterprise проект (15 мин)

**Для:** Критически важных систем, Enterprise-приложений, командной разработки

### Шаг 1: Настройте многоагентную автоматизацию (5 мин)

```bash
# Скопируйте многоагентный протокол
cp templates/docs/MULTI_AGENT_SETUP.md docs/

# Скопируйте чеклист валидации
cp templates/docs/VALIDATION_CHECKLIST.md docs/

# Создайте AUTO_PROTOCOL.md с конфигурацией проекта
cat > docs/AUTO_PROTOCOL.md << 'EOF'
PROJECT_NAME: "My Enterprise App"
PROJECT_DIR: "/path/to/project"
DOMAIN: "Accounting"
DOMAIN_RULES: "docs/ACCOUNTING_CONSTITUTION.md"
EOF
```

### Шаг 2: Определите доменные правила (5 мин)

```bash
# Создайте конституцию домена
cat > docs/ACCOUNTING_CONSTITUTION.md << 'EOF'
# Конституция Accounting домена

## Основные принципы

1. **NUMERIC(15,2)** для всех финансовых операций
2. **NO FLOAT/DOUBLE** в базе данных
3. **Transfer vs Expense** — четкое разделение
   - Transfer: перемещение между счётами (не расход)
   - Expense: реальные расходы
4. **Триггеры** не меняют баланс в pending → completed

## Запрещённые паттерны

❌ Использование Float для денег
❌ Запись погашения кредита как Expense
❌ Использование NEW./OLD. в RLS Policy

## Разрешённые паттерны

✅ DECIMAL.js для точных вычислений
✅ NUMERIC(15,2) в PostgreSQL
✅ BigInt/decimal.js в TypeScript
EOF
```

### Шаг 3: Создайте автоматические Git hooks (5 мин)

```bash
# Создайте директорию hooks
mkdir -p .git/hooks

# Создайте pre-commit hook
cat > .git/hooks/pre-commit << 'EOF'
#!/bin/sh
echo "🔍 Pre-commit: Проверка..."
node scripts/ontology/analyze-dependencies.js || exit 1
git add docs/DEPENDENCIES.json
EOF

chmod +x .git/hooks/pre-commit

# Создайте pre-push hook
cat > .git/hooks/pre-push << 'EOF'
#!/bin/sh
echo "🔍 Pre-push: Валидация..."
node scripts/ontology/validate-graph.js || exit 1
EOF

chmod +x .git/hooks/pre-push
```

### ✅ Что вы получили:

- ✅ **4-шаговый workflow**: Architect → Executor → Validator
- ✅ **Emergency Stop**: Автоматический стоп при проблемах
- ✅ **Domain validation**: Чёткие правила для домена
- ✅ **Git hooks**: Автоматическая синхронизация
- ✅ **Memory Graph**: Онтологический граф знаний
- ✅ **Enterprise-grade**: Надёжность и качество

---

## 🧠 Сценарий3: Memory Graph интеграция (10 мин)

**Для:** Понимания связей между типами, предсказания последствий изменений

### Шаг 1: Скопируйте скрипты онтологического анализа (2 мин)

```bash
# Создайте директорию скриптов
mkdir -p scripts/ontology

# Скопируйте скрипты из Homeaccounting проекта
cp ../homeaccounting/homeaccounting/scripts/ontology/analyze-dependencies.js scripts/ontology/
cp ../homeaccounting/homeaccounting/scripts/ontology/validate-graph.js scripts/ontology/
cp ../homeaccounting/homeaccounting/scripts/ontology/sync-types.js scripts/ontology/
```

### Шаг 2: Настройте Git hooks (3 мин)

```bash
# Создайте pre-commit hook
cat > .git/hooks/pre-commit << 'EOF'
#!/bin/sh
echo "🔍 Pre-commit: Анализ зависимостей..."
node scripts/ontology/analyze-dependencies.js
git add docs/DEPENDENCIES.json
EOF

chmod +x .git/hooks/pre-commit

# Создайте pre-push hook
cat > .git/hooks/pre-push << 'EOF'
#!/bin/sh
echo "🔍 Pre-push: Валидация..."
node scripts/ontology/validate-graph.js
EOF

chmod +x .git/hooks/pre-push
```

### Шаг 3: Протестируйте интеграцию (5 мин)

```bash
# Тестирование pre-commit
git add --all && git commit -m "test: Проверка hooks"

# Проверьте, что создан DEPENDENCIES.json
ls -la docs/DEPENDENCIES.json

# Тестирование валидации
node scripts/ontology/validate-graph.js

# Проверьте содержимое графа
cat docs/DEPENDENCIES.json | head -n 50
```

### ✅ Что вы получили:

- ✅ **Автоматический анализ** — зависимости при каждом commit
- ✅ **Валидация** — проверка перед push
- ✅ **Memory Graph** — синхронизация с MCP
- ✅ **Предсказание последствий** — понимание связей типов
- ✅ **Поиск неиспользуемого кода** — чистота проекта

---

## 🤔 Сценарий4: Sequential Thinking (8 мин)

**Для:** Сложных задач, неясного scope, исследовательских задач с несколькими гипотезами

### Шаг 1: Когда использовать Sequential Thinking (2 мин)

**ФАЗА 1 (Architect) — Сложное планирование:**
- Анализ требований с несколькими альтернативами
- Разбиение на управляемые этапы
- Генерация и верификация гипотез решений

**ФАЗА 2 (Executor) — Решение проблем:**
- Исследование альтернативных подходов
- Ревизия предыдущих решений
- Анализ причин ошибок

**ФАЗА 3 (Validator) — Верификация гипотез:**
- Проверка гипотез на основе фактов
- Валидация соответствия плану

### Шаг 2: Установка Sequential Thinking (2 мин)

```bash
# Установите Sequential Thinking MCP
npx -y @modelcontextprotocol/server-sequential-thinking
```

### Шаг 3: Базовый пример использования (6 мин)

```javascript
// Пример из ФАЗЫ 1: Планирование архитектуры
await use_mcp_tool("sequentialthinking", {
  thought: "Анализирую миграцию: User имеет 15 полей, нужно перенести 12 в Profile, оставить 3, удалить deprecated поля.",
  nextThoughtNeeded: true,
  thoughtNumber: 1,
  totalThoughts: 8
});
```

---

## 💰 Сценарий5: Accounting система (20 мин)

**Для:** Финансовых систем, бухгалтерских приложений, систем учёта

### Шаг 1: Настройте Accounting домен (5 мин)

```bash
# Создайте директорию проекта
mkdir -p my-accounting-app
cd my-accounting-app

# Инициализируйте Git
git init

# Скопируйте Accounting правила из Homeaccounting
cp -r ../homeaccounting/homeaccounting/.clinerules .clinerules

# Создайте базовую структуру
mkdir -p docs scripts/ontology src/types src/services

# Создайте PROJECT_STATE.md
cat > docs/PROJECT_STATE.md << 'EOF'
# Состояние проекта: Accounting App

## Текущая фаза: INITIAL_SETUP
## Последнее обновление: 2026-01-08

## Журнал последних действий
- [x] Настройка Accounting правил
- [x] Копирование онтологических скриптов
- [x] Настройка Git hooks
EOF
```

### Шаг 2: Настройте онтологические скрипты (5 мин)

```bash
# Скопируйте скрипты из Homeaccounting
cp ../homeaccounting/homeaccounting/scripts/ontology/analyze-dependencies.js scripts/ontology/
cp ../homeaccounting/homeaccounting/scripts/ontology/validate-graph.js scripts/ontology/
cp ../homeaccounting/homeaccounting/scripts/ontology/sync-types.js scripts/ontology/

# Создайте ACCOUNTING_CONSTITUTION.md
cat > docs/ACCOUNTING_CONSTITUTION.md << 'EOF'
# Конституция Accounting домена

## Основные принципы

1. **NUMERIC(15,2)** для всех финансовых операций
2. **NO FLOAT/DOUBLE** в базе данных
3. **Transfer vs Expense** — четкое разделение
   - Transfer: перемещение между счётами (не расход)
   - Expense: реальные расходы
4. **Триггеры** не меняют баланс в pending → completed

## Финансовые типы данных (CRITICAL)

### SQL (PostgreSQL)
```sql
-- ✅ ПРАВИЛЬНО
amount NUMERIC(15,2)  -- Максимум 15 цифр, 2 после запятой
balance NUMERIC(15,2)

-- ❌ ЗАПРЕЩЕНО
amount FLOAT          -- Неточная математика
amount DOUBLE         -- Неточная математика
```

### TypeScript/JavaScript
```typescript
// ✅ ПРАВИЛЬНО
import { Decimal } from 'decimal.js';
amount: Decimal        // Точные вычисления
balance: BigInt        // Для больших сумм

// ❌ ЗАПРЕЩЕНО
amount: number         // Неточная математика без decimal.js
```

## Бухгалтерская логика (CRITICAL)

### Кредитная карта
```typescript
// ✅ ПРАВИЛЬНО - Кредит = долг
type AccountType = 'wallet' | 'credit';
const creditCard = {
  type: 'credit',       // Credit = долг (Liability)
  isLiability: true     // Пассивный счёт
};

// ❌ НЕПРАВИЛЬНО - Кредит как кошелёк
const creditCard = {
  type: 'wallet',       // Кредит не кошелёк!
  isLiability: false    // Кредит - это актив?
};
```

### Погашение кредита
```typescript
// ✅ ПРАВИЛЬНО - Transfer
const payment = {
  type: 'transfer',     // Transfer (не показывается в расходах)
  from: 'wallet',
  to: 'credit_card',
  amount: 1000.00
};

// ❌ НЕПРАВИЛЬНО - Expense
const payment = {
  type: 'expense',      // Платёж по кредиту не расход!
  amount: 1000.00
};
```

### Баланс при статусе транзакции
```typescript
// ✅ ПРАВИЛЬНО - Баланс НЕ меняется
const transaction = {
  status: 'pending',    // Баланс не влияет
  amount: 100.00
};
// После подтверждения
transaction.status = 'completed';  // Баланс НЕ меняется повторно
```

## Запрещённые паттерны

❌ Использование Float для денег
❌ Запись погашения кредита как Expense
❌ Использование NEW./OLD. в RLS Policy
❌ Изменение баланса при pending → completed

## Разрешённые паттерны

✅ DECIMAL.js для точных вычислений
✅ NUMERIC(15,2) в PostgreSQL
✅ BigInt/decimal.js в TypeScript
✅ RLS без NEW./OLD. с UNIQUE constraints
✅ SECURITY DEFINER функции
EOF
```

### Шаг 3: Настройте Git hooks (5 мин)

```bash
# Создайте директорию hooks
mkdir -p .git/hooks

# Создайте pre-commit hook
cat > .git/hooks/pre-commit << 'EOF'
#!/bin/sh
echo "🔍 Pre-commit: Анализ зависимостей..."
node scripts/ontology/analyze-dependencies.js
git add docs/DEPENDENCIES.json
EOF

chmod +x .git/hooks/pre-commit

# Создайте pre-push hook
cat > .git/hooks/pre-push << 'EOF'
#!/bin/sh
echo "🔍 Pre-push: Валидация Accounting правил..."
node scripts/ontology/validate-graph.js

# Дополнительная проверка NUMERIC типов
echo "🔍 Проверка NUMERIC типов..."
grep -r "FLOAT\|DOUBLE" src/ && echo "❌ Найдены запрещённые типы!" && exit 1

echo "✅ Accounting правила соблюдены"
EOF

chmod +x .git/hooks/pre-push
```

### Шаг 4: Создайте базовую структуру типов (5 мин)

```bash
# Создайте базовые типы
cat > src/types/Transaction.ts << 'EOF'
import { Decimal } from 'decimal.js';

export enum TransactionType {
  EXPENSE = 'expense',
  INCOME = 'income',
  TRANSFER = 'transfer'
}

export enum TransactionStatus {
  PENDING = 'pending',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled'
}

export interface Transaction {
  id: string;
  type: TransactionType;
  amount: Decimal;           // NUMERIC(15,2)
  accountId: string;
  userId: string;
  status: TransactionStatus;
  createdAt: Date;
  updatedAt: Date;
  // TODO: Implement per ACCOUNTING_CONSTITUTION.md section 3.2
  // TODO: Use decimal.js for precision as per SPEC-2024-01
}
EOF

# Создайте типы счетов
cat > src/types/Account.ts << 'EOF'
import { Decimal } from 'decimal.js';

export enum AccountType {
  WALLET = 'wallet',        // Активный счёт
  CREDIT = 'credit'         // Пассивный счёт (долг)
}

export interface Account {
  id: string;
  name: string;
  type: AccountType;
  isLiability: boolean;     // true для кредитов
  balance: Decimal;         // NUMERIC(15,2)
  userId: string;
  // TODO: Implement per ACCOUNTING_CONSTITUTION.md section 5.1
  // TODO: Credit card must be marked as liability
}
EOF
```

### Шаг 5: Протестируйте настройку (5 мин)

```bash
# Тестирование онтологического анализа
node scripts/ontology/analyze-dependencies.js

# Проверка DEPENDENCIES.json
cat docs/DEPENDENCIES.json

# Тестирование валидации
node scripts/ontology/validate-graph.js

# Тестирование Git hooks
git add --all && git commit -m "feat: Настройка Accounting системы"

# Проверка pre-push
git push origin main  # Должен пройти валидацию
```

### ✅ Что вы получили:

- ✅ **Accounting-first подход** — правила домена в основе
- ✅ **NUMERIC(15,2) типы** — финансовая точность
- ✅ **Правильная логика** — Credit = долг, Transfer vs Expense
- ✅ **Автоматическая валидация** — Git hooks проверяют правила
- ✅ **Онтологический граф** — понимание связей в финансовой системе
- ✅ **Context7 интеграция** — актуальная документация для финансовых библиотек
- ✅ **Sequential Thinking** — структурированное мышление для сложных финансовых задач
- ✅ **Memory Graph** — персистентное знание о финансовых моделях

### 🔧 Начать разработку:

```bash
# Создайте первую задачу
echo "Создай сервис для создания транзакций с NUMERIC точностью" > task.txt

# AI автоматически:
# 1. ФАЗА 1: Architect - планирует с учётом Accounting правил
# 2. ФАЗА 2: Executor - пишет код с decimal.js и NUMERIC
# 3. ФАЗА 2.5: Ontology Syncer - обновляет граф знаний
# 4. ФАЗА 3: Validator - проверяет соответствие Accounting конституции
```

---

## 📊 Сравнение сценариев

| Сценарий | Время | Сложность | Результат |
|----------|---------|-----------|-----------|
| **Быстрый прототип** | 5 мин | ⭐ | Базовые правила |
| **Enterprise проект** | 15 мин | ⭐⭐ | Полная автоматизация |
| **Memory Graph** | 10 мин | ⭐⭐ | Понимание связей |
| **Sequential Thinking** | 8 мин | ⭐⭐ | Структурированное мышление |
| **Accounting система** | 20 мин | ⭐⭐⭐ | Финансовая точность |

---

## 🎯 Рекомендации

### Для новичков:
Начните с **Сценария 1**, затем добавляйте элементы из других сценариев по мере роста проекта.

### Для Enterprise-проектов:
Используйте **Сценарий 2** полностью — это обеспечивает качество и надёжность.

### Для сложной архитектуры:
Обязательно используйте **Сценарий 3** — Memory Graph критически важен для понимания связей.

---

## 🔧 Дополнительная настройка

### Включить Context7

```bash
# В VS Code + Cline
{
  "cline.mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"]
    }
  }
}
```

### Включить Sequential Thinking

Sequential Thinking автоматически интегрируется через `mcp/servers/sequentialthinking-usage-in-airules.md`

---

## 📚 Следующие шаги

### Изучение документации:

1. **Основы**: [basics/role-based-development.md](basics/role-based-development.md)
 - [mcp/servers/sequentialthinking-usage-in-airules.md](mcp/servers/sequentialthinking-usage-in-airules.md)
2. **Паттерны**: [basics/pattern-library.md](basics/pattern-library.md)
 - [AI_BEST_PRACTICES.md](AI_BEST_PRACTICES.md)
3. **Best Practices**: [AI_BEST_PRACTICES.md](AI_BEST_PRACTICES.md)
4. **Memory Graph**: [examples/memory-graph-integration.md](examples/memory-graph-integration.md)

### Готовые примеры:

- **Sequential Thinking Integration**: [mcp/servers/sequentialthinking-usage-in-airules.md](mcp/servers/sequentialthinking-usage-in-airules.md)

### Практика:

1. Начните с маленького проекта
2. Следуйте выбранному сценарию
3. Проверяйте результаты
4. Добавляйте автоматизацию постепенно

---

## 🎓 Заключение

**Вы можете настроить AIRules за 5 минут** и получить:

- ✅ Структурированную разработку с AI
- ✅ Автоматическую валидацию
- ✅ Понимание связей в проекте (Memory Graph)
- ✅ Качественный код без галлюцинаций (Context7)
- ✅ Масштабируемость от прототипа до Enterprise
- ✅ Структурированное мышление (Sequential Thinking)
- ✅ Синергия всех MCP серверов

**Начните с выбранного сценария выше и развивайтесь постепенно!** 🚀

---

## 💡 Частые вопросы

### Q: Какой сценарий выбрать для меня?

**A:**
- Прототип/MVP → Сценарий 1
- Production API → Сценарий 2
- Сложный проект → Сценарий 3 + Сценарий 2

### Q: Можно ли комбинировать сценарии?

**A:** Да! Начните с Сценария 1, затем добавляйте элементы из других сценариев по мере роста проекта.

### Q: Что такое Memory Graph и зачем он нужен?

**A:** Memory Graph — это персистентный слой знаний для LLM. Он хранит связи между типами, компонентами и файлами. Это позволяет LLM предсказывать последствия изменений и понимать, что и где используется.

**Подробнее:** [🧠 Memory Graph Integration Guide](examples/memory-graph-integration.md)

### Q: Что такое Sequential Thinking и зачем он нужен?

**A:** Sequential Thinking — это MCP сервер для структурированного пошагового мышления. Позволяет разбивать сложные задачи на управляемые этапы, делать ревизии решений, ветвиться в альтернативные пути и генерировать/верифицировать гипотезы решений.

**Подробнее:** [🧠 Sequential Thinking Usage Guide](mcp/servers/sequentialthinking-usage-in-airules.md)

---

## 🚀 Задачи

1. [x] Создать документацию по Sequential Thinking в AIRules
2. [x] Интегрировать Sequential Thinking в auto-protocol.md
3. [x] Обновить README.md с разделом Sequential Thinking
4. [x] Обновить QUICK_START.md с Accounting сценарием
5. [x] Проверить правила homeaccounting на соответствие MCP практикам

---

**Готово к интеграции!** ✅