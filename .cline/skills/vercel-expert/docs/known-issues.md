# ⚠️ Известные проблемы для Vercel Expert Skill

Этот файл содержит известные проблемы и их решения для работы с Vercel.

---

## 🔥 Критические проблемы

### Проблема: Ошибка деплоя "Build failed"

**Симптомы:**
- Веб-интерфейс показывает "Build failed"
- Логи содержат ошибки TypeScript или Next.js

**Причины:**
1. Типизация ошибок в TypeScript
2. Отсутствующие переменные окружения
3. Неверная конфигурация vercel.json
4. Проблемы с зависимостями

**Решения:**
```bash
# 1. Проверить логи деплоя
vercel logs [deployment-url]

# 2. Локально протестировать билд
npm run build

# 3. Проверить переменные окружения
vercel env ls

# 4. Проверить vercel.json
cat vercel.json
```

**Полезные ссылки:**
- [Vercel Build Logs](https://vercel.com/docs/deployments/logs)
- [Context7: Vercel Deployment](https://www.context7.ai)

---

### Проблема: Edge Functions не работают

**Симптомы:**
- API возвращает 500 ошибку
- Edge function не деплоится

**Причины:**
1. Неправильный экспорт функции
2. Отсутствие handler
3. Превышение размера функции

**Решения:**
```typescript
// ✅ Правильно
export default function handler(req, res) {
  res.json({ message: 'Hello' });
}

// ❌ Неправильно (нет default)
export function handler(req, res) {
  res.json({ message: 'Hello' });
}
```

**Полезные ссылки:**
- [Vercel Edge Functions](https://vercel.com/docs/functions/edge-functions)
- [Context7: Edge Functions Best Practices](https://www.context7.ai)

---

## ⚠️ Общие проблемы

### Проблема: Переменные окружения не доступны

**Симптомы:**
- Код выдает `undefined` для переменных окружения
- Локально работает, но на Vercel — нет

**Причины:**
1. Переменная не добавлена в Vercel Dashboard
2. Неверное имя переменной
3. Кэш Vercel

**Решения:**
```bash
# 1. Добавить переменную
vercel env add MY_VAR

# 2. Проверить доступность
vercel env ls

# 3. Обновить проект (чтобы применить переменные)
vercel --prod
```

**Полезные ссылки:**
- [Vercel Environment Variables](https://vercel.com/docs/projects/environment-variables)

---

### Проблема: Кэширование не работает

**Симптомы:**
- Изменения не видны сразу
- Старый кэш используется

**Причины:**
1. Отсутствует Cache-Control header
2. Неверная конфигурация кэша в vercel.json

**Решения:**
```json
// vercel.json
{
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=3600, s-maxage=86400"
        }
      ]
    }
  ]
}
```

**Полезные ссылки:**
- [Vercel Caching](https://vercel.com/docs/platform/caching)
- [Context7: Caching Strategies](https://www.context7.ai)

---

## 📚 Дополнительные ресурсы

- [Vercel Documentation](https://vercel.com/docs)
- [Vercel CLI](https://vercel.com/docs/cli)
- [Context7: Vercel Best Practices](https://www.context7.ai)