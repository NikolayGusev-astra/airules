# GitHub Expert - Известные проблемы

**Версия:** 1.0.0  
**Дата создания:** 2026-01-17  
**Статус:** ✅ Поддерживается

---

## ⚠️ ЧАСТЫЕ ПРОБЛЕМЫ

### Проблема 1: Workflow не запускается

**Симптомы:**
- Workflow в Actions tab не появляется
- Trigger события не срабатывают
- Push не активирует workflow

**Возможные причины:**
1. ❌ Неправильное название файла (должно быть в `.github/workflows/`)
2. ❌ Неправильный синтаксис YAML (отступы, кавычки)
3. ❌ Webhook event неправильно настроен
4. ❌ Файл не в правильной ветке

**Решения:**
```yaml
# ✅ ПРАВИЛЬНО: .github/workflows/ci.yml
name: CI Workflow
on:
  push:
    branches: [main]

# ❌ НЕПРАВИЛЬНО: workflows/ci.yml (нет точки)
```

**Как проверить:**
```bash
# Проверить синтаксис YAML
yamllint .github/workflows/*.yml

# Проверить webhook события
gh workflow list
```

---

### Проблема 2: Secrets не доступны

**Симптомы:**
```
Error: Invalid secret: SECRET_NAME
Error: Secrets are not available
```

**Возможные причины:**
1. ❌ Секрет не добавлен в GitHub Settings → Secrets and variables → Actions
2. ❌ Неправильное имя секрета (чувствительно к регистру)
3. ❌ Использование `var` вместо `secrets`

**Решения:**
```yaml
# ❌ НЕПРАВИЛЬНО
env:
  MY_SECRET: ${{ var.SECRET_NAME }}

# ✅ ПРАВИЛЬНО
env:
  MY_SECRET: ${{ secrets.SECRET_NAME }}
```

**Как добавить секрет:**
1. GitHub Repository → Settings → Secrets and variables → Actions
2. New repository secret
3. Имя: `SECRET_NAME`
4. Значение: `<ваш секрет>`

---

### Проблема 3: Pull Request Target не работает

**Симптомы:**
```
Error: Cannot access workflow in pull request from fork
```

**Возможные причины:**
1. ❌ Используете `pull_request` вместо `pull_request_target`
2. ❌ Пытаетесь выполнить код из fork (небезопасно)

**Решения:**
```yaml
# ❌ НЕПРАВИЛЬНО (небезопасно)
on:
  pull_request:
    branches: [main]
jobs:
  build:
    steps:
      - uses: actions/checkout@v5  # Проверяет PR код (fork)

# ✅ ПРАВИЛЬНО (безопасно)
on:
  pull_request_target:
    branches: [main]
jobs:
  build:
    steps:
      - uses: actions/checkout@v5  # Проверяет base branch код
```

**Когда использовать:**
- `pull_request`: Для обычной проверки PR (обычно безопасно)
- `pull_request_target`: Когда нужно выполнить из base branch (безопасно)

---

### Проблема 4: Cache не работает

**Симптомы:**
```
Cache miss every time
Dependencies installed fresh every run
```

**Возможные причины:**
1. ❌ Неправильный cache key
2. ❌ Cache path неправильно указан
3. ❌ hashFiles() неправильно настроен

**Решения:**
```yaml
# ❌ НЕПРАВИЛЬНО
- uses: actions/cache@v4
  with:
    path: node_modules
    key: ${{ runner.os }}-node  # Недетерминированный

# ✅ ПРАВИЛЬНО
- uses: actions/cache@v4
  with:
    path: node_modules
    key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
```

**Ключевые моменты:**
- ✅ Используйте `hashFiles()` для детерминированного ключа
- ✅ Включите файлы, которые меняются (package-lock.json)
- ✅ Используйте `cache-read-only` для пропуска кеширования

---

### Проблема 5: Matrix не работает

**Симптомы:**
```
Error: matrix strategy is not supported
Jobs not running in parallel
```

**Возможные причины:**
1. ❌ Неправильная структура матрицы
2. ❌ Exclude неправильно настроен
3. ❌ Matrix не на уровне jobs

**Решения:**
```yaml
# ❌ НЕПРАВИЛЬНО
strategy:
  matrix:
    os: [ubuntu-latest]
    node: [18, 20]

# ✅ ПРАВИЛЬНО
jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest]
        node: [18, 20]
```

---

### Проблема 6: Secrets раскрываются в логах

**Симптомы:**
- Секреты видны в логах Actions
- Секреты передаются через `echo`

**Возможные причины:**
1. ❌ Используете `echo` для секретов
2. ❌ GitHub не маскирует автоматически
3. ❌ Используете секрет в `run` напрямую

**Решения:**
```yaml
# ❌ НЕПРАВИЛЬНО
- name: Print secret
  run: echo "My secret is ${{ secrets.MY_SECRET }}"

# ✅ ПРАВИЛЬНО
- name: Use secret safely
  env:
    MY_SECRET: ${{ secrets.MY_SECRET }}
  run: echo "Using secret (value masked)"
```

**Как GitHub маскирует секреты:**
- Автоматически маскирует значения, начинающиеся с `GH_SECRET_`
- Автоматически маскирует секреты из `secrets` context
- ❌ Не маскирует в `if` statements

---

### Проблема 7: Composite Action не виден

**Симптомы:**
```
Error: Action not found
Composite action steps not running
```

**Возможные причины:**
1. ❌ Неправильный путь к action
2. ❌ Action не в репозитории
3. ❌ Отсутствует `action.yml`

**Решения:**
```
✅ ПРАВИЛЬНАЯ СТРУКТУРА:
.github/actions/my-action/
└── action.yml

❌ НЕПРАВИЛЬНАЯ СТРУКТУРА:
.github/actions/my-action/action.yml
```

**Как использовать composite action:**
```yaml
jobs:
  test:
    steps:
      - uses: ./.github/actions/my-action  # ✅ Правильно
      # НЕ: uses: ./.github/actions/my-action/action.yml
```

---

### Проблема 8: Permissions error

**Симптомы:**
```
Error: Permission denied
Resource not accessible
```

**Возможные причины:**
1. ❌ Permissions не настроены в workflow
2. ❌ Недостаточно прав у PAT token
3. ❌ GITHUB_TOKEN не передан

**Решения:**
```yaml
# ❌ НЕПРАВИЛЬНО
name: Deploy
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

# ✅ ПРАВИЛЬНО
name: Deploy
permissions:
  contents: read
  id-token: write
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
```

**Общие permissions:**
- `contents: read` - чтение файлов
- `contents: write` - запись файлов
- `pull-requests: write` - создание/редактирование PR
- `id-token: write` - для OIDC токенов

---

## 🔍 Диагностика проблем

### Проверка workflow синтаксиса
```bash
# Проверить YAML
yamllint .github/workflows/*.yml

# Проверить workflow через GitHub CLI
gh workflow view <workflow-name>
```

### Проверка webhook событий
```bash
# Получить список webhook событий
gh api repos/OWNER/REPO/hooks

# Симулировать webhook
gh workflow run <workflow-name>
```

### Проверка секретов
```bash
# Проверить наличие секретов
gh secret list

# Проверить права доступа
gh auth status
```

---

## 📊 Статистика проблем

| Проблема | Частота | Сложность решения | Priority |
|-----------|---------|-------------------|----------|
| Workflow не запускается | Высокая | Низкая | Medium |
| Secrets не доступны | Средняя | Низкая | High |
| Pull Request Target | Средняя | Средняя | High |
| Cache не работает | Высокая | Средняя | Medium |
| Matrix не работает | Низкая | Высокая | Medium |
| Secrets раскрываются | Средняя | Низкая | Critical |
| Composite Action | Низкая | Низкая | Low |
| Permissions error | Средняя | Низкая | High |

---

**Всё ещё? Проверьте [solutions.md](solutions.md) для решений этих проблем!**