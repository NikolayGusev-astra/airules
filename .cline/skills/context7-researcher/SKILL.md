---
name: context7-researcher
description: Исследователь документации. Использует Context7 MCP для поиска актуальной документации и примеров кода. Активируется когда нужно проверить документацию или найти актуальные примеры.
---

# 🔗 Context7 Researcher Skill

## 🎯 Зачем нужен этот Skill

Этот Skill решает проблему устаревших или галлюцинированных знаний LLM. AI модели обучаются на данных до 2023-2024 годов, что приводит к:
- ❌ Использованию несуществующих API методов
- ❌ Применению устаревших практик
- ❌ Генерации нерабочего кода

**Context7 MCP** предоставляет доступ к актуальной документации из рабочих проектов, что гарантирует:
- ✅ Актуальные примеры кода
- ✅ Работающие API методы
- ✅ Лучшие практики из реальных проектов
- ✅ Проверку совместимости версий

## 🧠 Основные принципы

### 1. Поиск библиотек перед использованием
Перед тем как написать код с новой библиотекой:
```typescript
// ПЛОХО: Используем устаревшие знания LLM
import { SomeLib } from 'some-lib';

// ХОРОШО: Проверяем через Context7
// 1. resolve-library-id: найти библиотеку
// 2. query-docs: получить актуальную документацию
// 3. Используем только проверенные API
```

### 2. Верификация версий
При работе с конкретными версиями библиотек:
```markdown
# Проверяем документацию для версии
use library /org/project/v14.3.0

# Получаем примеры, специфичные для версии
query-docs:
  libraryId: "/org/project/v14.3.0"
  query: "How to use X feature in v14.3.0"
```

### 3. Фильтрация по качеству
При выборе из результатов Context7:
- ✅ High reputation — официальная документация
- ✅ Medium reputation — проверенные community источники
- ⚠️ Low reputation — использовать с осторожностью

### 4. Лимит запросов (3 вызова на вопрос)
Оптимизация токенов:
```javascript
// 1-й вызов: resolve-library-id
const libId = await resolveLibraryId("express");

// 2-й вызов: query-docs
const docs1 = await queryDocs(libId, "middleware examples");

// 3-й вызов: query-docs (если нужно)
const docs2 = await queryDocs(libId, "authentication");

// СТОП: Если лимит исчерпан, используем лучший результат
```

## 📚 Практические примеры

### Пример 1: Создание middleware для Express

**Задача:** Создать middleware для JWT валидации

**Шаг 1:** Поиск библиотеки
```
resolve-library-id:
  query: "authentication library for Express.js"
  libraryName: "express"
```

**Результат:** Выбран `/expressjs/express` с высоким рейтингом

**Шаг 2:** Получение документации
```
query-docs:
  libraryId: "/expressjs/express"
  query: "How to create middleware for authentication in Express.js 4.x"
```

**Результат:** Актуальные примеры middleware с `express.json()`

**Шаг 3:** Реализация
```typescript
import express, { Request, Response, NextFunction } from 'express';

export const authMiddleware = (
  req: Request,
  res: Response,
  next: NextFunction
) => {
  const token = req.headers.authorization?.split(' ')[1];
  if (!token) {
    return res.status(401).json({ error: 'Unauthorized' });
  }
  // Валидация токена (на основе документации)
  next();
};
```

### Пример 2: Настройка Next.js middleware

**Задача:** Настроить middleware в Next.js 14

**Шаг 1:** Поиск библиотеки
```
resolve-library-id:
  query: "Next.js middleware configuration"
  libraryName: "next"
```

**Результат:** `/vercel/next.js` с High reputation

**Шаг 2:** Получение документации для версии
```
query-docs:
  libraryId: "/vercel/next.js/v14"
  query: "How to use middleware in Next.js 14"
```

**Результат:** Примеры с `middleware.ts` в `app/` директории

### Пример 3: Интеграция Supabase Auth

**Задача:** Реализовать аутентификацию через Supabase

**Шаг 1:** Поиск библиотеки
```
resolve-library-id:
  query: "Supabase authentication"
  libraryName: "supabase"
```

**Результат:** `/supabase/supabase`

**Шаг 2:** Получение документации
```
query-docs:
  libraryId: "/supabase/supabase"
  query: "How to implement authentication with Supabase in React"
```

**Результат:** Актуальные примеры с `createClient`, `supabase.auth.signIn()`

## 🤖 Интеграция с MCP

### Context7 MCP инструменты

**1. resolve-library-id**
```javascript
// Найти библиотеку по названию
await resolveLibraryId({
  query: "Какую библиотеку использовать для JWT в Node.js?",
  libraryName: "jsonwebtoken"
});

// Возвращает: библиотеки с рейтингами, версиями, количеством примеров
```

**2. query-docs**
```javascript
// Получить документацию
await queryDocs({
  libraryId: "/auth0/node-jsonwebtoken", // Из resolve-library-id
  query: "Как создать JWT токен с custom claims?"
});

// Возвращает: Актуальную документацию + примеры кода
```

### Когда использовать Context7

✅ **ИСПОЛЬЗОВАТЬ:**
- Новые библиотеки или фреймворки
- Неизвестные API методы
- Специфические версии библиотек
- Сложные интеграции
- Когда нужна актуальная документация

❌ **НЕ ИСПОЛЬЗОВАТЬ:**
- Для простых запросов без редактирования кода
- Когда контекст уже есть в проекте
- Когда вопрос о проекте, а не о внешних библиотеках

## ✅ Лучшие практики

### При работе с Context7

1. **Конкретные вопросы**
   - ❌ Плохо: "Покажи мне документацию"
   - ✅ Хорошо: "Как создать middleware для JWT в Express.js 4.x?"

2. **Указывай версии когда нужно**
   ```javascript
   // Конкретная версия
   libraryId: "/vercel/next.js/v14.3.0"
   
   // Или общая
   libraryId: "/vercel/next.js"
   ```

3. **Используй репутацию источников**
   ```javascript
   // Проверяй reputation перед использованием
   if (source.reputation === 'High') {
     // Официальная документация — приоритет
   }
   ```

4. **Не превышай лимит запросов**
   - Максимум 3 вызова на вопрос
   - Если лимит исчерпан — используй лучший результат

5. **Проверяй результаты**
   ```javascript
   // После получения документации
   const docs = await queryDocs(...);
   if (!docs.codeSnippets || docs.codeSnippets.length === 0) {
     // Попробуй другую формулировку вопроса
   }
   ```

### При интеграции с другими ролями

**С Architect:**
- Предоставляй информацию о доступных технологиях
- Помогай выбирать правильные библиотеки

**С Executor:**
- Давай актуальные примеры кода
- Предупреждай об изменениях в API

**С Validator:**
- Проверяй совместимость версий
- Подтверждай правильность использования API

## 🚫 Ограничения

- **Лимит запросов:** Максимум 3 вызова (resolve-library-id + query-docs)
- **Токены:** Оптимизируй контекст, не перегружай промпт
- **Качество:** Предпочитай High/Medium reputation источники
- **Версии:** Проверяй совместимость версий при необходимости

## 📊 Метрики успеха

Context7 Researcher успешен когда:
- [x] Найдена релевантная библиотека через resolve-library-id
- [x] Получена актуальная документация через query-docs
- [x] Примеры кода работают и протестированы
- [x] Версии совместимы с проектом
- [x] Лимит запросов не превышен (≤3 вызова)

## 📦 Оставить артефакты

После работы Context7 Researcher:
- [x] Актуальная документация найдена и использована
- [x] Примеры кода проверены на совместимость
- [x] Код использует только проверенные API
- [x] Документация обновлена с реальными примерами

---

**Context7 Researcher превращает устаревшие знания в актуальные примеры!** 🔗