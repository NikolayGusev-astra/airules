# 💡 Решения для Librarian Skill

Типичные проблемы и их решения будут добавляться по мере обнаружения.

## 📝 Как добавить решение

1. Опишите проблему (симптом, причина)
2. Укажите шаги для решения
3. Добавьте примеры если нужно
4. Ссылайтесь на соответствующую документацию

---

## 🚀 Быстрые решения для типовых задач

### Решение 1: Как определить жанр документа?

**Проблема:** Не понимаю к какому жанру относится мой документ.

**Решение:**
1. Проверь цель документа
2. Посмотри на структуру:
   - Пошаговые? → Tutorial
   - Конкретная задача? → How-to
   - Справочник/API? → Reference
   - Концепция/почему? → Explanation
3. Добавь явное указание в начале

**Команда:**
```bash
./scripts/validate-librarian.sh --check-genre <file.md>
```

### Решение 2: Как упростить сложные предложения?

**Проблема:** Предложения слишком длинные и тяжело читать.

**Решение:**
1. Используй `./scripts/fix-style.sh --target <file.md>`
2. Автоматически разобьёт на короткие
3. Заменит пассивный залог на активный

**Команда:**
```bash
./scripts/fix-style.sh --target docs/README.md --auto-fix
```

### Решение 3: Как найти дублированный контент?

**Проблема:** Не знаю, где текст дублируется.

**Решение:**
1. Запусти `./scripts/audit-links.sh --check-duplicates`
2. Автоматически найдёт все дубликаты
3. Получишь отчёт с путями к файлам

**Команда:**
```bash
./scripts/audit-links.sh --check-duplicates --report docs/LIBRARIAN_DUPLICATE_REPORT.md
```

### Решение 4: Как реорганизовать структуру документации?

**Проблема:** Хаотичное расположение файлов и папок.

**Решение:**
1. Определи жанровую структуру (tutorials/, how-to/, reference/, explanation/)
2. Запусти `./scripts/order-docs.sh --reorganize`
3. Скрипт автоматически переместит файлы

**Команда:**
```bash
./scripts/order-docs.sh --target docs/ --reorganize --create-backup
```

### Решение 5: Как проверить все ссылки в документации?

**Проблема:** Боюсь что ссылки битые после рефакторинга.

**Решение:**
1. Запусти `./scripts/audit-links.sh --all-docs`
2. Автоматически проверит все внутренние ссылки
3. Создаст отчёт о найденных проблемах

**Команда:**
```bash
./scripts/audit-links.sh --all-docs --report docs/LIBRARIAN_LINK_AUDIT.md
```

### Решение 6: Как добавить валидацию в git workflow?

**Проблема:** Хочу автоматически проверять документацию перед коммитом.

**Решение:**
1. Создай `.git/hooks/pre-commit`
2. Добавь вызов скриптов валидации
3. Автоматически блокируй коммиты с ошибками

**Пример хука:**
```bash
#!/bin/bash
# .git/hooks/pre-commit

echo "🔍 Librarian: Проверка документации..."

# Валидация ссылок
.cline/skills/librarian/scripts/audit-links.sh

# Валидация жанра
.cline/skills/librarian/scripts/validate-librarian.sh --quick

# Если есть ошибки - остановить коммит
if [ $? -ne 0 ]; then
  echo "❌ Найдены проблемы в документации. Исправьте их."
  exit 1
fi

echo "✅ Документация валидна."
```

### Решение 7: Как создать отчёт о качестве документации?

**Проблема:** Хочу проанализировать всю документацию проекта.

**Решение:**
1. Запусти полную валидацию
2. Получи детальный отчёт
3. Исправь найденные проблемы

**Команда:**
```bash
./scripts/validate-librarian.sh --full-report --output docs/LIBRARIAN_FULL_AUDIT.md
```

### Решение 8: Как упорядочить список?

**Проблема:** Список элементов не в алфавитном порядке.

**Решение:**
1. Используй `./scripts/order-docs.sh --sort-list`
2. Автоматически отсортирует
3. Обновит файл с упорядоченным списком

**Команда:**
```bash
./scripts/order-docs.sh --target docs/README.md --sort-list
```

### Решение 9: Как исправить жаргон без контекста?

**Проблема:** Использовал термин, но не уверен что он значит.

**Решение:**
1. Найди в Reference документах
2. Добавь краткое объяснение при первом использовании
3. Ссылайся на более детальное объяснение

**Пример:**
```markdown
## Authentication

**Auth** — процесс проверки подлинности пользователя.

Подробнее: см. [docs/explanation/authentication-concepts.md](docs/explanation/authentication-concepts.md)

Для использования:
```typescript
const auth = new AuthService();
await auth.authenticate(user);
```
```

### Решение 10: Как создать новый документ по шаблону?

**Проблема:** Не знаю с чего начать новый документ.

**Решение:**
1. Скопируй шаблон из `templates/docs/`
2. Заполни обязательные секции (Цель, Scope, TL;DR)
3. Определи жанр и аудиторию
4. Следуй структуре Diátaxis

**Команда:**
```bash
cp templates/docs/template.md docs/my-new-doc.md
# Теперь редактируй docs/my-new-doc.md
```

### Решение 11: Как интегрировать в CI/CD?

**Проблема:** Хочу автоматически линтовать документацию.

**Решение:**
1. Добавь GitHub Action для markdown linting
2. Используй markdown-lint или remark
3. Автоматически проверяй в PR

**Пример GitHub Action:**
```yaml
name: Librarian Check
on: [pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Validate Documentation
        run: |
          .cline/skills/librarian/scripts/validate-librarian.sh
```

### Решение 12: Как обновить старую документацию?

**Проблема:** Документ устарел, но не знаю что именно.

**Решение:**
1. Проверь последнюю дату изменения
2. Сравни с текущим состоянием
3. Обнови или добавь @deprecated

**Пример:**
```markdown
## Authentication

**⚠️ Этот документ устарел**

Для аутентификации используйте:
- [JWT tokens](docs/how-to/jwt-authentication.md) (рекомендуемый подход)
- [OAuth 2.0](docs/how-to/oauth-implementation.md) (для внешних сервисов)

Старый подход: `Basic Auth` — [deprecated](/archive/basic-auth.md)
```

---

**Совет:** Регулярно запускайте скрипты валидации для поддержания качества документации.