# 📚 Продвинутые темы Librarian Skill

Этот файл содержит сложные и продвинутые сценарии использования Librarian Skill.

## 📬 Общая структура

Для каждой темы:
```
### Тема: Название
**Сложность:** Средняя / Высокая / Экспертная
**Предварительные требования:** Что нужно знать
**Описание:**
Детальное объяснение концепции.

**Пример:**
```typescript
// Продвинутый пример кода
```

**Лучшие практики:**
- Практика 1
- Практика 2
```

---

## 🔬 CI/CD Интеграция для документации

**Сложность:** Высокая
**Предварительные требования:** Понимание GitHub Actions, bash scripting

**Описание:**
Интеграция автоматической проверки документации в CI/CD пайплайн обеспечивает, что документация остаётся актуальной и качественной на протяжении всего жизненного цикла проекта.

**Ключевые концепты:**
1. **Pre-commit hooks** — проверка перед коммитом
2. **GitHub Actions** — автоматическая валидация в PR
3. **Linting** — проверка стиля markdown
4. **Automated tests** — консистентность документации

**Пример pre-commit hook:**
```bash
#!/bin/bash
# .git/hooks/pre-commit

echo "🔍 Librarian: Проверка документации..."

# Установи путь к скриптам
SKILL_DIR="$(git rev-parse --show-toplevel)/.cline/skills/librarian/scripts"

# Проверка ссылок
$SKILL_DIR/audit-links.sh --quick
LINKS_EXIT=$?

# Валидация жанра
$SKILL_DIR/validate-librarian.sh --check-genre
GENRE_EXIT=$?

# Если есть ошибки - остановить коммит
if [ $LINKS_EXIT -ne 0 ] || [ $GENRE_EXIT -ne 0 ]; then
  echo "❌ Найдены проблемы в документации."
  echo "   Запустите ./scripts/validate-librarian.sh для деталей."
  exit 1
fi

echo "✅ Документация валидна."
```

**Пример GitHub Action:**
```yaml
name: Librarian Documentation Check

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

jobs:
  validate:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Validate documentation
        run: |
          echo "🔍 Запуск полной валидации..."
          .cline/skills/librarian/scripts/validate-librarian.sh --full-report
          
      - name: Upload audit report
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: librarian-audit-report
          path: docs/LIBRARIAN_AUDIT_REPORT.md
          
      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            const report = fs.readFileSync('docs/LIBRARIAN_AUDIT_REPORT.md', 'utf8');
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## 📚 Отчёт Librarian\n\n${report}`
            });
```

**Пример workflow с автоматическим исправлением:**
```yaml
name: Librarian Auto-Fix

on:
  schedule:
    - cron: '0 0 * * *'  # Ежедневно в полночь

jobs:
  auto-fix:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Fix documentation style
        run: |
          echo "🔧 Автоматическое исправление..."
          .cline/skills/librarian/scripts/fix-style.sh --all-docs --auto
          
      - name: Order documentation
        run: |
          echo "📁 Упорядочивание..."
          .cline/skills/librarian/scripts/order-docs.sh --target docs/
          
      - name: Create PR with fixes
        uses: peter-evans/create-pull-request@v4
        with:
          title: "📚 Librarian: Автоматические улучшения документации"
          body: |
            ## Изменения
            - Исправлен стиль (пассивный залог → активный)
            - Упорядочены списки
            - Исправлены ссылки
            
            ## Проверьте перед мержем
            - Прочитайте изменения
            - Убедитесь что всё корректно
          branch: librarian/auto-fix-${{ github.sha }}
          branch-suffix: timestamp
```

**Лучшие практики:**
- ✅ Валидация в pre-commit (быстрая обратная связь)
- ✅ Автоматические фиксации в scheduled workflow
- ✅ Детальные отчёты в PR (прозрачность процесса)
- ✅ Артефакты с результатами аудита

---

## 🔗 Анализ связей в документации

**Сложность:** Средняя
**Предварительные требования:** Понимание графов, regex, bash scripting

**Описание:**
Анализ ссылок между документами для построения графа зависимостей. Это помогает понять структуру документации и выявить изолированные документы.

**Концепции:**
1. **Внутренние ссылки** — связи между документами проекта
2. **Внешние ссылки** — ссылки на внешние ресурсы
3. **Орфаные документы** — документы без входящих ссылок
4. **Популярные документы** — наиболее часто используемые

**Пример анализа связей:**
```bash
#!/bin/bash
# scripts/analyze-links.sh

echo "🔗 Анализ связей..."

# Найти все .md файлы
find . -name "*.md" -type f | while read file; do
  echo "📄 $file"
  
  # Найти внутренние ссылки [текст](путь.md)
  grep -oP '\[([^\]]+\]\(([^)]+)\)' "$file" | \
    sed 's/.*\[\([^]]*\)\](\([^)]*\)/\1 -> \2/' | \
    sort -u
  
  # Найти внешние ссылки (http://, https://)
  grep -oP '\[([^\]]+\]\(https?:[^)]+\)' "$file"
  
done | sort -u

# Отчёт: топ документов по количеству ссылок
```

**Пример вывода:**
```
🔗 Отчёт о связях:

Внутренние ссылки:
  docs/README.md → 15 ссылок
  ontology-schema.md → 12 ссылок
  QUICK_START.md → 8 ссылок

Внешние ссылки:
  docs/README.md → 3 ссылки
  mcp/README.md → 5 ссылок

Орфаные документы:
  - templates/.cursorignore (0 ссылок)

Популярные документы:
  1. docs/README.md (15 ссылок)
  2. ontology-schema.md (12 ссылок)
  3. .cline/skills/README.md (10 ссылок)

📦 Артефакты:
  - docs/LINK_ANALYSIS_REPORT.md создан
```

**Лучшие практики:**
- ✅ Регулярный анализ связей (раз в неделю)
- ✅ Выявление орфаных документов
- ✅ Понимание структуры документации через ссылки
- ✅ Оптимизация навигации на основе анализа

---

## 🧠 Автоматическая классификация жанров

**Сложность:** Высокая
**Предварительные требования:** Понимание NLP, машинное обучение, Python

**Описание:**
Автоматическое определение жанра документа (Tutorial/How-to/Reference/Explanation) на основе контента. Это полезно для больших проектов с множеством документов.

**Методы классификации:**
1. **По ключевые слова** — "шаг", "учебник", "tutorial" → Tutorial
2. **По структуре** — наличие "## Шаг 1" → Tutorial
3. **По цели** — "Как сделать" → How-to
4. **По контекcту** — списки, таблицы → Reference

**Пример классификации:**
```python
#!/usr/bin/env python3
# classify_document_genre.py

import re
from pathlib import Path

class DocumentClassifier:
    def __init__(self):
        self.tutorial_keywords = ['шаг', 'учебник', 'tutorial', 'начало', 'завершение']
        self.howto_keywords = ['как', 'как сделать', 'решение', 'как использовать']
        self.reference_keywords = ['api', 'справочник', 'команда', 'таблица', 'список']
        self.explanation_keywords = ['почему', 'концепция', 'принцип', 'объяснение']
    
    def classify(self, filepath):
        with open(filepath, 'r', encoding='utf-8') as f:
            content = f.read().lower()
            
        # Подсчёт ключевых слов
        scores = {
            'tutorial': sum(content.count(k) for k in self.tutorial_keywords),
            'howto': sum(content.count(k) for k in self.howto_keywords),
            'reference': sum(content.count(k) for k in self.reference_keywords),
            'explanation': sum(content.count(k) for k in self.explanation_keywords)
        }
        
        # Определение жанра
        max_genre = max(scores, key=scores.get)
        return max_genre

# Использование
classifier = DocumentClassifier()
for md_file in Path('.').glob('**/*.md'):
    genre = classifier.classify(md_file)
    print(f"{md_file}: {genre}")
```

**Пример интеграции с валидацией:**
```bash
# Сначала классифицируем
python scripts/classify_genre.py

# Потом проверяем что жанр правильный
for file in $(find . -name "*.md"); do
  genre=$(python scripts/classify_genre.py "$file")
  current_genre=$(grep -oP "Жанр: (.*)" "$file")
  
  if [ "$genre" != "$current_genre" ]; then
    echo "⚠️  $file имеет жанр $current_genre, но контент указывает $genre"
  fi
done
```

**Лучшие практики:**
- ✅ Автоматическая классификация для больших проектов
- ✅ Машинное обучение для повышения точности
- ✅ Интеграция с validate-librarian.sh
- ✅ Исправление ошибок классификации в скриптах

---

## 📊 Метрики качества документации

**Сложность:** Средняя
**Предварительные требования:** Понимание метрик, статистика

**Описание:**
Сбор метрик о качестве документации для отслеживания улучшений и выявления проблемных областей.

**Ключевые метрики:**
1. **Полнота документов** — наличие обязательных секций
2. **Качество стиля** — активный залог, короткие предложения
3. **Покрытие ссылок** — процент битых ссылок
4. **Актуальность** — дата последнего изменения
5. **Упорядоченность** — соответствие жанровой структуре

**Пример скрипта метрик:**
```bash
#!/bin/bash
# scripts/metrics.sh

echo "📊 Сбор метрик качества..."

# Общее количество документов
total_docs=$(find . -name "*.md" -type f | wc -l)

# Документы с обязательными секциями
with_structure=$(grep -l "## Цель\|## TL;DR" $(find . -name "*.md") | wc -l)

# Документы с активным залогом
active_voice=$(grep -l "Реализуй\|Интегрируй\|Добавь\|Создай" $(find . -name "*.md") | wc -l)

# Средняя длина предложения
avg_sentence_length=$(find . -name "*.md" -exec cat {} \; | \
  awk '{print length}' | \
  awk '{sum+=$1; count++} END {print sum/count}')

echo "📈 Метрики:"
echo "   Всего документов: $total_docs"
echo "   С правильной структурой: $with_structure"
echo "   С активным залогом: $active_voice"
echo "   Средняя длина предложения: $avg_sentence_length"
echo "   Покрытие ссылок: $(./scripts/audit-links.sh --percent)"
```

**Пример отчёта метрик:**
```
📊 Отчёт о качестве документации (2026-01-11)

Общие метрики:
  Всего документов: 45
  Документы с правильной структурой: 38 (84%)
  Документы с активным залогом: 29 (64%)
  Средняя длина предложения: 14 слов
  Покрытие ссылок: 92%

Проблемные области:
  - docs/examples/ (7 документов без обязательных секций)
  - mcp/examples/ (3 документа с пассивным залогом)
  - .cline/skills/*/docs/advanced.md (устаревшие)

Рекомендации:
  1. Добавить обязательные секции в docs/examples/
  2. Использовать fix-style.sh для mcp/examples/
  3. Обновить advanced.md для всех скиллов
```

**Лучшие практики:**
- ✅ Регулярный сбор метрик (раз в неделю)
- ✅ Отслеживание улучшений во времени
- ✅ Выявление проблемных областей
- ✅ Целевые показатели качества

---

## 🔄 Работа с Memory Graph MCP

**Сложность:** Экспертная
**Предварительные требования:** Понимание MCP, Memory Graph API

**Описание:**
Интеграция с Memory Graph для хранения и анализа структуры документации. Это позволяет создавать граф связей между документами и искать контент по смысловым запросам.

**Концепции:**
1. **Сущности** — документы, концепции, инструкции
2. **Отношения** — ссылки, упоминания, использование
3. **Наблюдения** — метрики, изменения, проблемы

**Пример создания сущностей:**
```javascript
// Создать сущность документа
await use_mcp_tool("create_entities", {
  entities: [
    {
      name: "Docs_QuickStart",
      entityType: "documentation_file",
      observations: [
        "Path: docs/QUICK_START.md",
        "Genre: Tutorial (обучающий)",
        "Audience: Новички в AIRules",
        "Sections: 6 основных шагов",
        "Last updated: 2026-01-08",
        "Links to: 8 других документов"
      ]
    },
    {
      name: "Docs_OntologySchema",
      entityType: "documentation_file",
      observations: [
        "Path: ontology-schema.md",
        "Genre: Reference (справочник)",
        "Audience: Архитекторы и валидаторы",
        "Defines: Agent, Phase, Artifact classes",
        "Last updated: 2026-01-10",
        "Links from: 15 документов"
      ]
    }
  ]
});
```

**Пример создания связей:**
```javascript
// Создать связи между документами
await use_mcp_tool("create_relations", {
  relations: [
    {
      from: "Docs_QuickStart",
      to: "Docs_SkillsFeature",
      relationType: "references",
      observations: [
        "Ссылка в секции 'Related Documentation'",
        "Контекст: Быстрый старт для новичков"
      ]
    },
    {
      from: "Docs_SkillsFeature",
      to: "Docs_SkillsMigrationGuide",
      relationType: "follows_up_with",
      observations: [
        "Тип: How-to → Tutorial",
        "Контекст: После понимания концепций перейти к практике"
      ]
    }
  ]
});
```

**Пример поиска в Memory Graph:**
```javascript
// Найти документы по жанру
await use_mcp_tool("search_nodes", {
  query: "Tutorial обучающие пошаговые"
});

// Найти документы для новичков
await use_mcp_tool("search_nodes", {
  query: "Novice beginner newcomer аудитория"
});

// Найти документы по концепции
await use_mcp_tool("search_nodes", {
  query: "Docs-as-Code documentation lifecycle"
});
```

**Интеграция с валидацией:**
```bash
#!/bin/bash
# validate-with-memory-graph.sh

# Валидация через скрипт
./scripts/validate-librarian.sh --quick

# Если всё ок - сохранить в Memory Graph
if [ $? -eq 0 ]; then
  echo "💾 Сохранение результатов в Memory Graph..."
  
  # Создать отчёт валидации как сущность
  node -e "
    const MCP = require('mcp-client');
    
    await MCP.create_entities({
      entities: [{
        name: 'Librarian_Validation_$(date +%Y%m%d)',
        entityType: 'validation_report',
        observations: [
          'Date: $(date)',
          'Total documents: $(find . -name '*.md' | wc -l)',
          'Valid: YES',
          'Genre compliance: OK'
        ]
      }]
    });
  "
fi
```

**Лучшие практики:**
- ✅ Хранить структуру документации в Memory Graph
- ✅ Использовать для смыслового поиска контента
- ✅ Анализировать связи между документами
- ✅ Отслеживать изменения во времени

---

## 🎯 Настраиваемая генерация отчётов

**Сложность:** Высокая
**Предварительные требования:** Понимание шаблонизаторов, Jinja2

**Описание:**
Автоматическая генерация отчётов о состоянии документации в разных форматах (HTML, PDF, JSON) для разных аудиторий (разработчики, менеджеры, стейкхолдеры).

**Концепции:**
1. **Шаблоны отчётов** — разные форматы для разных аудиторий
2. **Переменные шаблона** — метрики, проблемы, рекомендации
3. **Автоматизация** — scheduled или webhook генерация

**Пример Jinja2 шаблона:**
```jinja2
<!DOCTYPE html>
<html>
<head>
    <title>📚 Отчёт о документации AIRules</title>
    <style>
        body { font-family: 'Segoe UI', sans-serif; line-height: 1.6; }
        .metric { display: inline-block; margin: 10px 20px; padding: 20px; background: #f0f4f4; border-radius: 8px; min-width: 200px; }
        .metric-value { font-size: 24px; font-weight: bold; color: #10b981; }
        .section { margin: 30px 0; }
        .section h2 { color: #333; border-bottom: 2px solid #ddd; padding-bottom: 10px; }
        .file-list { list-style: none; padding-left: 0; }
        .file-list li { padding: 8px 0; border-bottom: 1px solid #eee; }
        .file-list li:last-child { border-bottom: none; }
        .recommendation { background: #e8f5e9; padding: 15px; border-radius: 6px; margin: 10px 0; }
    </style>
</head>
<body>
    <h1>📚 Отчёт о документации AIRules</h1>
    <p>Сгенерирован: {{ report_date }}</p>
    
    <div class="section">
        <h2>📈 Метрики качества</h2>
        
        <div class="metric">
            <div class="metric-value">{{ total_docs }}</div>
            <div>Всего документов</div>
        </div>
        
        <div class="metric">
            <div class="metric-value">{{ complete_docs }}%</div>
            <div>С правильной структурой</div>
        </div>
        
        <div class="metric">
            <div class="metric-value">{{ active_voice }}%</div>
            <div>С активным залогом</div>
        </div>
        
        <div class="metric">
            <div class="metric-value">{{ coverage }}%</div>
            <div>Покрытие ссылок</div>
        </div>
    </div>
    
    <div class="section">
        <h2>🐛 Проблемные области</h2>
        <ul class="file-list">
            {% for area in problem_areas %}
            <li>{{ area.path }} ({{ area.issues_count }} проблем)</li>
            {% endfor %}
        </ul>
    </div>
    
    <div class="section">
        <h2>💡 Рекомендации</h2>
        {% for rec in recommendations %}
        <div class="recommendation">{{ rec }}</div>
        {% endfor %}
    </div>
    
    <div class="section">
        <h2>📄 Изменения за период</h2>
        <ul class="file-list">
            {% for change in recent_changes %}
            <li>{{ change.date }}: {{ change.file }} ({{ change.type }})</li>
            {% endfor %}
        </ul>
    </div>
</body>
</html>
```

**Пример генератора отчётов:**
```python
#!/usr/bin/env python3
# generate_reports.py

import jinja2
from datetime import datetime
import json

def load_metrics():
    # Собрать метрики из скриптов
    metrics = {
        'total_docs': int(subprocess.run(['find', '.', '-name', '*.md', '|', 'wc', '-l'], capture_output=True).stdout),
        'complete_docs': int(subprocess.run(['grep', '-l', '## Цель', 'find', '.', '-name', '*.md'], capture_output=True).stdout),
        'active_voice': int(subprocess.run(['grep', '-l', 'Реализуй\\|Интегрируй\\|Добавь\\|Создай', 'find', '.', '-name', '*.md'], capture_output=True).stdout),
        'link_coverage': int(subprocess.run(['./scripts/audit-links.sh', '--percent'], capture_output=True).stdout)
    }
    
    # Найти проблемные области
    problem_areas = []
    for doc in Path('.').glob('**/*.md'):
        issues = []
        if not check_structure(doc):
            issues.append("Нет обязательных секций")
        if not check_active_voice(doc):
            issues.append("Пассивный залог")
        
        if issues:
            problem_areas.append({
                'path': str(doc),
                'issues_count': len(issues)
            })
    
    return {
        'metrics': metrics,
        'problem_areas': problem_areas[:5],  # Топ 5
        'recommendations': generate_recommendations(metrics),
        'recent_changes': get_recent_changes(),
        'report_date': datetime.now().strftime('%Y-%m-%d')
    }

# Генерация HTML отчёта
template = jinja2.Template(open('templates/report.html').read())
html_report = template.render(**load_metrics())
open('docs/REPORT.html', 'w').write(html_report)

# Генерация PDF
# (с использованием wkhtmltopdf)
```

**Лучшие практики:**
- ✅ Использовать Jinja2 для гибкости шаблона
- ✅ Генерировать отчёты для разных аудиторий
- ✅ Автоматизация (scheduled или webhook)
- ✅ Визуализация данных (графики, чарты)

---

## 🎓 Обучение и онбординг документации

**Сложность:** Средняя
**Предварительные требования:** Понимание Git, markdown, процессы

**Описание:**
Создание обучающих материалов и онбординга для авторов документации. Это обеспечивает единый стиль и качество документации от разных контрибьюторов.

**Ключевые компоненты:**
1. **Стилевой гайд** — единые правила письма
2. **Шаблоны документов** — для разных жанров
3. **Чеклисты реью** — для review документации
4. **Обучающие примеры** — типовые сценарии

**Пример стильевого гайда:**
```markdown
# 📚 Стилевой гайд документации AIRules

## Общие принципы

### Письмо
- **Активный залог:** Используй "Делай", "Создай", "Добавь"
- **Короткие предложения:** 15-20 слов, одна мысль на абзац
- **Без жаргона:** Объясняй термины при первом использовании

### Структура документа

#### Обязательные секции
Каждый документ должен содержать:

```markdown
## Цель
[Чего хотим достичь]

## Область охвата
[Что входит]

## Out of Scope
[Что НЕ входит]

## TL;DR
[Краткое резюме]
```

#### Дополнительные элементы

**Для Tutorial:**
- ## Шаг 1, ## Шаг 2, ...
- ## Заключение
- Примеры кода

**Для How-to:**
- ## Проблема
- ## Решение
- ## Примеры
- Команды для copy-paste

**Для Reference:**
- Таблицы
- Списки
- Краткие описания
- Алфавитный порядок

#### Жанры документации

| Жанр | Цель | Пример |
|--------|-------|--------|
| Tutorial | Пошаговое обучение | QUICK_START.md |
| How-to | Решение конкретной задачи | SKILLS_MIGRATION_GUIDE.md |
| Reference | Справочник/API | ontology-schema.md |
| Explanation | Объяснение концепций | AI_BEST_PRACTICES.md |

### Чеклист реью

Перед публикацией документа пройдите по чеклисту:

#### Структура
- [ ] Есть ясная цель и аудитория
- [ ] Все секции присутствуют (Цель, Scope, TL;DR)
- [ ] Жанр документа определён явно
- [ ] Стиль соответствует гайду (активный залог, короткие предложения)
- [ ] Нет дублирования контента
- [ ] Все ссылки рабочие

#### Качество
- [ ] Понятен для целевой аудитории
- [ ] Примеры кода рабочие и актуальны
- [ ] Нет жаргона без объяснения
- [ ] Визуализации нужны и помогают

#### Консистентность
- [ ] Терминология соответствует проекту
- [ ] Нет противоречий с другими документами
- [ ] Ссылки на актуальные документы
- [ ] Нет устаревших рекомендаций

### Типовые шаблоны

#### Шаблон Tutorial
```markdown
# [Название]: [Учебный гайд]

## Цель
Пошагово научить [аудиторию] работать с [концепцией].

## Область охвата
- Базовые понятия
- Пошаговые инструкции
- Практические примеры

## Out of Scope
- Продвинутые сценарии
- Альтернативные подходы

## TL;DR
Изучите [X шагов] чтобы [действие].

---

## Шаг 1: [Название]
[Содержание шага]

## Шаг 2: [Название]
[Содержание шага]

...

## Заключение
[Резюме и следующий шаги]
```

**Лучшие практики:**
- ✅ Единый стиль через шаблоны
- ✅ Обязательный реью для документации
- ✅ Онбординг для новых авторов
- ✅ Шаблоны для разных жанров

---

**Совет:** Начните с автоматизации (CI/CD) и постепенно внедряйте продвинутые функции по мере необходимости.