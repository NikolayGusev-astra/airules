# 📜 Скрипты Librarian Skill

Автоматизация проверки, упорядочивания и улучшения документации проекта.

## 📋 Содержание

- [validate-librarian.sh](#validate-librariansh) — Валидация документации
- [order-docs.sh](#order-docssh) — Упорядочивание структуры
- [fix-style.sh](#fix-stylesh) — Исправление стиля
- [audit-links.sh](#audit-linkssh) — Аудит ссылок

---

## validate-librarian.sh

**Описание:** Валидация документации по принципам Diátaxis и Docs-as-Code.

**Использование:**
```bash
# Быстрая проверка (все проверки)
./validate-librarian.sh --quick

# Полный отчёт
./validate-librarian.sh --full-report --output docs/LIBRARIAN_AUDIT.md

# Специфические проверки
./validate-librarian.sh --check-genre
./validate-librarian.sh --check-structure
./validate-librarian.sh --check-style
./validate-librarian.sh --check-links
```

**Аргументы:**
- `--quick` — быстрая проверка (все проверки вкл.)
- `--full-report` — полный отчёт в файл
- `--target DIR` — целевая директория (по умолчанию: текущая)
- `--output FILE` — путь к отчёту
- `--check-genre` — только проверка жанра
- `--check-structure` — только проверка структуры
- `--check-style` — только проверка стиля
- `--check-links` — только проверка ссылок

**Проверки:**
1. **Структура** — наличие обязательных секций (Цель, Scope, TL;DR)
2. **Жанр** — классификация по ключевым словам (Tutorial, How-to, Reference, Explanation)
3. **Стиль** — активный залог, короткие предложения, отсутствие жаргона
4. **Ссылки** — проверка внутренних ссылок
5. **Дублирование** — поиск дублированного контента

---

## order-docs.sh

**Описание:** Упорядочивание структуры документации по жанрам.

**Использование:**
```bash
# Реорганизация в жанровые директории
./order-docs.sh --reorganize --create-backup

# Сортировка списка в файле
./order-docs.sh --target docs/README.md --sort-list

# Dry run (без реальных изменений)
./order-docs.sh --reorganize --dry-run
```

**Аргументы:**
- `--reorganize` — переместить файлы в жанровые директории
- `--create-backup` — создать бекап перед изменениями
- `--sort-list` — отсортировать список в файле
- `--dry-run` — показать изменения без выполнения
- `--target DIR` — целевая директория

**Жанровая структура:**
```
docs/
├── tutorials/        # Tutorial (обучающие)
├── how-to/          # How-to (решение задач)
├── reference/        # Reference (справочники)
└── explanation/      # Explanation (концепции)
```

---

## fix-style.sh

**Описание:** Автоматическое исправление стиля документации.

**Использование:**
```bash
# Исправить все документы
./fix-style.sh --all-docs --auto

# Исправить конкретный файл
./fix-style.sh --target docs/README.md --auto

# Без автоматического разбития предложений
./fix-style.sh --all-docs --no-sentences
```

**Аргументы:**
- `--auto` — автоматическое исправление
- `--all-docs` — обработать все документы
- `--target FILE` — конкретный файл для исправления
- `--no-passive` — не исправлять пассивный залог
- `--no-sentences` — не разбивать длинные предложения
- `--remove-jargon` — удалять жаргон без объяснения

**Исправления:**
1. **Пассивный залог → активный**:
   - "необходимо" → "нужно"
   - "следует сделать" → "сделай"
   - "требуется выполнить" → "выполни"

2. **Длинные предложения** — разбитие (>25 слов)
3. **Жаргон** — замена на понятные слова с пояснением

---

## audit-links.sh

**Описание:** Аудит ссылок в документации.

**Использование:**
```bash
# Быстрая проверка внутренних ссылок
./audit-links.sh --quick

# Полный аудит
./audit-links.sh --check-internal --check-external --check-duplicates

# Сохранить отчёт
./audit-links.sh --report docs/LINK_AUDIT_REPORT.md
```

**Аргументы:**
- `--quick` — быстрая проверка (только внутренние ссылки)
- `--check-internal` — аудит внутренних ссылок
- `--check-external` — аудит внешних ссылок
- `--check-duplicates` — проверка дублирования
- `--report FILE` — путь к отчёту
- `--target DIR` — целевая директория

**Проверки:**
1. **Внутренние ссылки** — проверка существования файлов
2. **Внешние ссылки** — проверка HTTP ответов (curl)
3. **Дублирование** — поиск одинакового контента
4. **Анализ связей** — орфаные документы, популярные документы

---

## 🔧 Git Hooks

### Pre-commit Hook

Создайте `.git/hooks/pre-commit`:

```bash
#!/bin/bash
# .git/hooks/pre-commit

echo "🔍 Librarian: Проверка документации..."

# Валидация ссылок
.cline/skills/librarian/scripts/audit-links.sh --quick
LINKS_EXIT=$?

# Валидация жанра
.cline/skills/librarian/scripts/validate-librarian.sh --check-genre
GENRE_EXIT=$?

# Если есть ошибки - остановить коммит
if [ $LINKS_EXIT -ne 0 ] || [ $GENRE_EXIT -ne 0 ]; then
  echo "❌ Найдены проблемы в документации. Исправьте их."
  exit 1
fi

echo "✅ Документация валидна."
```

### GitHub Actions

```yaml
name: Librarian Documentation Check

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

jobs:
  validate:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Validate documentation
        run: |
          echo "🔍 Запуск полной валидации..."
          .cline/skills/librarian/scripts/validate-librarian.sh --full-report
          
      - name: Upload audit report
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: librarian-audit-report
          path: docs/LIBRARIAN_AUDIT_REPORT.md
```

---

## 📊 Metрики

**Типичные результаты:**
- Проверено: 45 документов
- С правильной структурой: 38 (84%)
- С активным залогом: 29 (64%)
- Битые ссылки: 3
- Дублирование: 2 случая

**Целевые показатели:**
- Полнота структуры: >90%
- Активный залог: >80%
- Покрытие ссылок: >95%
- Дублирование: 0 случаев

---

## 💡 Советы

1. **Регулярная проверка** — запустите `validate-librarian.sh --quick` перед коммитом
2. **Исправление стиля** — используйте `fix-style.sh --all-docs --auto` для быстрой коррекции
3. **Упорядочивание** — используйте `order-docs.sh --reorganize` для структурирования
4. **Аудит ссылок** — запустите `audit-links.sh` после рефакторинга

---

## 🐛 Troubleshooting

**Проблема:** Скрипт не запускается на Windows.

**Решение:**
```bash
# Используйте Git Bash или WSL
# Или добавьте в начало скрипта:
#!/usr/bin/env bash
```

**Проблема:** Ошибка "Permission denied" при выполнении.

**Решение:**
```bash
chmod +x .cline/skills/librarian/scripts/*.sh
```

---

**Для более подробной информации смотрите:**
- [../docs/known-issues.md](known-issues.md) — Известные проблемы
- [../docs/solutions.md](solutions.md) — Решения типовых задач
- [../docs/advanced.md](advanced.md) — Продвинутые темы