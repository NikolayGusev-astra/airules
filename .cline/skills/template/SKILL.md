---
name: template
description: Шаблон для создания нового Skill. Содержит базовую структуру и примеры всех функций.
---

# 📋 Skill Template

## Зачем нужен этот шаблон?

Это базовый шаблон для создания новых Skills. Скопируйте эту директорию и переименуйте её для вашего нового Skill.

## 🏗 Структура Skill

```
my-skill/
├── SKILL.md              # Основной файл (этот файл)
├── docs/                 # Дополнительная документация
│   ├── known-issues.md   # Известные проблемы
│   ├── solutions.md        # Решения проблем
│   └── advanced.md         # Продвинутые темы
├── scripts/               # Утилиты и скрипты
│   ├── init.sh            # Инициализация окружения
│   └── validate.sh         # Валидация конфигурации
└── templates/              # Шаблоны файлов (опционально)
    └── config.yaml         # Конфигурация
```

---

## 🎯 Быстрый старт создания Skill

### Шаг 1: Скопировать шаблон

```bash
cp -r .cline/skills/template .cline/skills/my-new-skill
```

### Шаг 2: Переименовать файлы

Переименовать:
- `template/` → `my-new-skill/`
- `SKILL.md` (внутри директории) остаётся без изменений

### Шаг 3: Редактировать SKILL.md

Изменить frontmatter:
```yaml
---
name: my-skill
description: Краткое описание. Когда активируется этот Skill.
---
```

### Шаг 4: Добавить контент SKILL.md

Следуйте структуре ниже.

---

## 📄 Обязательная структура SKILL.md

Каждый SKILL.md должен содержать:

### 1. Frontmatter (обязательно)

```yaml
---
name: skill-name          # Должен совпадать с именем директории
description: Описание   # Максимум 1024 символа
---
```

### 2. Содержание (обязательно)

```markdown
# Название Skill

## Зачем нужен этот Skill?

## Основные принципы

## Практические примеры

## Интеграция с MCP (если применимо)

## Лучшие практики

## Критерии завершения

## Переход к следующей фазе
```

### 3. Директивы артефактов (обязательно)

В конце SKILL.md ОБЯЗАТЕЛЬНО должна быть директива:

```markdown
📦 Оставить артефакты:
- [x] docs/TASK_SPEC.md создан
- [x] docs/VALIDATION_REPORT.md создан
```

**Почему это важно:**
- Чёткий список созданных файлов
- Автоматическая фиксация прогресса
- Снятие ответственности с разработчика

**Важно:** Директива должна быть в самом конце SKILL.md, после всех других секций!

---

## 🔄 Workflows

### Что такое Workflows?

Workflows — это явные последовательности задач, которые вы вызываете специальной командой (например, `/workflow.md`).

**Отличие от Skills:**
- **Skills:** Активируются автоматически по описанию запроса
- **Workflows:** Вызываются явным образом командой

### Когда использовать Workflows?

✅ Используйте когда:
- Задача требует многошагового процесса
- Нужно повторяющийся алгоритм (релизы, деплои, code review)
- Задача имеет чёткую последовательность действий

❌ НЕ используйте когда:
- Простая задача, которую Skill может выполнить
- Задача требует адаптивности (каждый раз разные)

### Структура workflow файла

Создайте `.cline/workflows/my-workflow.md`:

```markdown
---
name: my-workflow
description: Краткое описание.
steps:
  - step-1
  - step-2
  - step-3
```

### Примеры Workflows

#### Пример 1: Release Workflow

```markdown
---
name: release
description: Выполнить полный релиз проекта.
steps:
  - version-bump
  - changelog
  - tests
  - git-tag
  - publish
```

#### Пример 2: Deployment Workflow

```markdown
---
name: deploy
description: Задеплоить проект на Vercel.
steps:
  - build
  - vercel-deploy
  - verify
```

#### Пример 3: Code Review Workflow

```markdown
---
name: code-review
description: Провести систематический code review.
steps:
  - lint
  - type-check
  - tests
  - review-checklist
```

---

## 🪝 Hooks

### Что такое Hooks?

Hooks — это инжекция логики в ключевые моменты жизненного цикла проекта.

**Типы Hooks:**
- **Pre-edit:** Перед редактированием файла
- **Post-edit:** После редактирования файла
- **Pre-commit:** Перед git commit
- **Post-commit:** После git commit

### Когда использовать Hooks?

✅ Используйте когда:
- Нужно добавить валидацию до изменения
- Нужно форматировать код после сохранения
- Нужно проверить тесты перед коммитом
- Нужно автоматически обновить документацию

❌ НЕ используйте когда:
- Логика может быть в самом Skill
- Простой проверки достаточно
- Нужен пользовательский контроль

### Создание Hook файла

Создайте `.cline/hooks/my-hook.md`:

```markdown
---
name: my-hook
description: Краткое описание.
trigger: pre-edit | post-edit | pre-commit | post-commit
```

### Примеры Hooks

#### Пример 1: Pre-Edit Hook (валидация TypeScript)

```markdown
---
name: typescript-validate
description: Проверить TypeScript код перед редактированием.
trigger: pre-edit

# TypeScript Validation Hook

Этот hook проверяет:
- Отсутствие `any` типов
- Отсутствие импортов из запрещённых пакетов
- Соблюдение стиля кода

## Проверка

1. Искать `any` в коде
2. Проверять импорты
3. Валидировать стиль

## Действия при ошибке

Если найдены проблемы:
- Вернуть список ошибок
- Предложить исправления
- ❌ Запретить редактирование пока не исправлено
```

#### Пример 2: Post-Commit Hook (авто-форматирование)

```markdown
---
name: auto-format
description: Автоматически форматировать код после коммита.
trigger: post-commit

# Auto-Format Hook

Этот hook запускает Prettier после каждого коммита.

## Команда

```bash
npx prettier --write "src/**/*.ts"
```

---

## 📚 Дополнительная документация

### docs/known-issues.md

Известные проблемы Skill и их решения.

```markdown
# 📚 Известные проблемы [Skill Name] Skill

(Проблемы будут добавляться по мере обнаружения)

## Пример

### Проблема: TypeScript ошибки при форматировании

**Симптом:** Prettier изменяет форматирование, ломая код

**Решение:**
1. Отключите Prettier для конкретных файлов
2. Используйте `// prettier-ignore` комментарии
3. Добавьте `.prettierrc` с правилами для этого типа файлов
```

### docs/solutions.md

Типичные сценарии и решения.

```markdown
# 💡 Решения для [Skill Name] Skill

Типичные проблемы и их решения будут добавляться по мере обнаружения.

## 📝 Как добавить решение

1. Опишите проблему
2. Укажите шаги для решения
3. Добавьте примеры если нужно
```

### docs/advanced.md

Продвинутые сценарии использования.

```markdown
# 📚 Продвинутые темы [Skill Name] Skill

Этот файл содержит сложные и продвинутые сценарии использования [Skill Name] Skill.

## 🔬 Общая структура

Для каждой темы:
```markdown
### Тема: Название
**Сложность:** Средняя / Высока / Экспертная
**Предварительные требования:** Что нужно знать

**Описание:**
Детальное объяснение концепции.

**Пример:**
```typescript
// Продвинутый пример кода
```

**Лучшие практики:**
- Практика 1
- Практика 2
```

## Примеры тем

(Добавляйте сюда сложные сценарии использования этого Skill)
```

---

## 🧪 Скрипты (scripts/)

### scripts/init.sh

Скрипт инициализации Skill. Создаёт необходимые директории и проверяет окружение.

```bash
#!/bin/bash
# init.sh - Инициализация окружения [Skill Name]

set -e

echo "🚀 Инициализация [Skill Name] окружения..."

# Проверка зависимостей
if [ -f "package.json" ]; then
  echo "📦 Установка зависимостей..."
  npm install
else
  echo "⚠️  package.json не найден. Пропуск установки зависимостей."
fi

# Создание необходимых директорий
echo "📁 Создание структуры директорий..."
mkdir -p tests/unit
mkdir -p tests/integration
mkdir -p tests/e2e
mkdir -p reports
mkdir -p snapshots
mkdir -p coverage
mkdir -p fixtures
mkdir -p logs

# Проверка конфигурации
echo "⚙️  Проверка конфигурации..."
if [ -f "config.json" ] || [ -f ".skill.json" ]; then
  echo "✅ Конфигурация найдена"
else
  echo "⚠️  Конфигурация не найдена. Создайте config.json или .skill.json"
fi

echo "✅ Инициализация завершена!"
echo ""
echo "Следующие шаги:"
echo "1. Проверьте SKILL.md для инструкций"
echo "2. Запустите ./scripts/validate.sh для проверки конфигурации"
echo "3. Начните работу"
```

### scripts/validate.sh

Скрипт валидации конфигурации Skill.

```bash
#!/bin/bash
# validate.sh - Валидация конфигурации [Skill Name]

set -e

echo "⚙️  Валидация конфигурации [Skill Name]..."

# Проверка структуры директорий
echo "📁 Проверка структуры..."
if [ ! -f "SKILL.md" ]; then
  echo "❌ ОШИБКА: SKILL.md не найден!"
  exit 1
fi

if [ ! -d "docs" ]; then
  echo "⚠️  ПРЕДУПРЕЖДЕНИЕ: Директория docs/ не существует"
fi

if [ ! -d "scripts" ]; then
  echo "⚠️  ПРЕДУПРЕЖДЕНИЕ: Директория scripts/ не существует"
fi

# Проверка необходимых файлов в docs/
if [ -d "docs" ]; then
  echo "📄 Проверка документации..."
  if [ ! -f "docs/known-issues.md" ]; then
    echo "  ⚠️  docs/known-issues.md не найден"
  fi
  if [ ! -f "docs/solutions.md" ]; then
    echo "  ⚠️  docs/solutions.md не найден"
  fi
  if [ ! -f "docs/advanced.md" ]; then
    echo "  ⚠️  docs/advanced.md не найден"
  fi
fi

# Проверка обязательных полей в SKILL.md
echo "📄 Проверка SKILL.md..."
if ! grep -q "^---$" SKILL.md; then
  echo "  ⚠️  SKILL.md не содержит frontmatter (---)"
fi

if ! grep -q "^name:" SKILL.md; then
  echo "  ⚠️  SKILL.md не содержит поле name:"
fi

if ! grep -q "^description:" SKILL.md; then
  echo "  ⚠️  SKILL.md не содержит поле description:"
fi

# Проверка директивы артефактов
if ! grep -q "📦 Оставить артефакты" SKILL.md; then
  echo "  ⚠️  SKILL.md не содержит директиву для артефактов"
fi

echo "✅ Валидация завершена!"
echo ""
echo "Статус:"
echo "✅ SKILL.md существует"
echo "✅ Обязательные поля присутствуют"
echo ""
echo "Если есть предупреждения, исправьте их перед использованием."
```

---

## 🎯 Чеклист создания Skill

Перед завершением убедитесь, что:

### Frontmatter
- [ ] `name` совпадает с именем директории
- [ ] `description` не превышает 1024 символа
- [ ] Frontmatter отделён `---` от содержимого

### Содержание
- [ ] Есть секция "## Зачем нужен этот Skill?"
- [ ] Есть секция "## Основные принципы"
- [ ] Есть секция "## Практические примеры"
- [ ] Есть секция "## Интеграция с MCP" (если применимо)
- [ ] Есть секция "## Лучшие практики"
- [ ] Есть секция "## Критерии завершения"
- [ ] Есть секция "## Переход к следующей фазе"
- [ ] В конце есть директива "📦 Оставить артефакты"

### Документация
- [ ] Создана директория `docs/`
- [ ] Создан файл `docs/known-issues.md`
- [ ] Создан файл `docs/solutions.md`
- [ ] Создан файл `docs/advanced.md`
- [ ] Документация содержит полезный контент

### Скрипты
- [ ] Создана директория `scripts/`
- [ ] Создан файл `scripts/init.sh`
- [ ] Создан файл `scripts/validate.sh`
- [ ] Скрипты исполняемые (chmod +x)

---

## ✅ Готовность

Skill готов к использованию когда:
- ✅ Frontmatter корректен
- ✅ Все секции присутствуют
- ✅ Директива артефактов в конце
- ✅ Документация создана
- ✅ Скрипты созданы
- ✅ Чеклист выполнен

---

## 🔄 Интеграция с другими функциями

### Работа с Workflows

```markdown
## Интеграция с Workflows

### Примеры Workflows для этого Skill

#### Пример: [Workflow Name]

Вызовите:
> /workflow [workflow-name]

**Результат:**
[Skill] выполнит:
1. Шаг 1
2. Шаг 2
3. Шаг 3
```

### Работа с Hooks

```markdown
## Интеграция с Hooks

### Примеры Hooks для этого Skill

#### Пример: [Hook Name]

**Триггер:** [pre-edit | post-edit | pre-commit | post-commit]

**Что делает:**
[Описание действий hook]
```

---

**Шаблон Skills полностью обновлён с поддержкой Workflows и Hooks!**
