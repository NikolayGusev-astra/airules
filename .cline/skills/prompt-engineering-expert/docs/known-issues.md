# 📚 Известные проблемы - Prompt Engineering Expert Skill

## Проблема 1: LLM игнорирует конкретные инструкции

**Симптом:** Несмотря на чёткие инструкции, LLM генерирует не тот формат или стиль ответа.

**Причины:**
- Инструкции находятся в середине длинного промпта
- Инструкции слишком сложные или противоречивые
- Отсутствуют примеры (Few-Shot)

**Решения:**
1. Помести ключевые инструкции в начало и конец промпта
2. Упрости инструкции (разбей на подзадачи)
3. Добавь конкретные примеры Few-Shot
4. Используй негативные примеры (что НЕ делать)

**Пример:**
```markdown
## ВАЖНЫЕ ТРЕБОВАНИЯ (ПРОЧИТИ ВНАЧАЛЕ И В КОНЦЕ)
- Используй ТОЛЬКО TypeScript
- НЕ создавай any типы
- Формат ответа: [конкретный формат]

... [остальной контекст] ...

## ПРОВЕРЬ ПЕРЕД ОТВЕТОМ
- [ ] Использован TypeScript? ✅
- [ ] Нет any типов? ✅
- [ ] Формат соответствует требованиям? ✅
```

---

## Проблема 2: Промпт слишком длинный (переполнение контекста)

**Симптом:** LLM обрывает ответ или игнорирует часть контекста.

**Причины:**
- Слишком много примеров Few-Shot
- Дублирование информации
- Неоправданно длинные описания

**Решения:**
1. Минимизируй количество примеров (2-3 достаточно)
2. Используй @Symbol вместо полного чтения файлов
3. Удали дублирующую информацию
4. Раздели сложный промпт на несколько этапов

**Пример оптимизации:**
```markdown
# ДО оптимизации
В этом проекте мы используем Node.js версии 20.0.0, Express версии 4.18.2, 
TypeScript версии 5.0.0, PostgreSQL версии 15.0.0, Prisma ORM версии 5.0.0.
Нам нужно создать endpoint для регистрации пользователей.

# ПОСЛЕ оптимизации
@PROJECT_CONFIG.md  // содержит весь стек

Создай endpoint для регистрации пользователей.
```

---

## Проблема 3: Непоследовательные ответы от LLM

**Симптом:** При одинаковых запросах LLM генерирует разные результаты.

**Причины:**
- Отсутствует Self-Consistency
- Промпт неоднозначен
- Нет чётких критериев оценки

**Решения:**
1. Используй Self-Consistency: попроси LLM решить задачу 3 разными способами
2. Добавь чёткие критерии оценки результата
3. Используй детерминированные формулировки
4. Установи temperature=0 для стабильных результатов

**Пример:**
```markdown
## Решение с Self-Consistency

Реши задачу 3 разными способами:
1. Способ A: [подход]
2. Способ B: [подход]
3. Способ C: [подход]

Сравни результаты и выбери лучший.
Обоснуй свой выбор.
```

---

## Проблема 4: LLM галлюцинирует несуществующие API

**Симптом:** LLM создаёт код с несуществующими методами или библиотеками.

**Причины:**
- LLM обучен на устаревших данных
- Промпт не проверяет актуальность библиотек
- Отсутствует верификация через Context7

**Решения:**
1. Обязательно используй Context7 для проверки библиотек
2. Указывай конкретные версии библиотек
3. Проверяй существование API в документации
4. Используй реальный пример кода из документации

**Пример с Context7:**
```markdown
Перед созданием кода:
1. Проверь актуальность Express.js через Context7
2. Получи реальные примеры из документации
3. Только после этого создавай код
```

---

## Проблема 5: Промпт плохо работает с другим LLM

**Симптом:** Промпт работает в GPT-4, но не работает в Claude или других моделях.

**Причины:**
- Специфичные инструкции для конкретной модели
- Использование токенов/фич недоступных в других моделях
- Разные паттерны обучения

**Решения:**
1. Тестируй промпт на нескольких моделях
2. Используй универсальные инструкции
3. Избегай специфических фич моделей
4. Создай версии промпта для разных LLM

**Пример универсального промпта:**
```markdown
## Требования (универсальные)
- Используй TypeScript (поддерживается всеми LLM)
- Следуй стандартам (не специфично для модели)
- Включай примеры кода (Few-Shot)

## Альтернативные форматы
Если твоя модель не поддерживает X, используй Y.
```

---

## Проблема 6: LLM игнорирует ограничения "НЕ ДЕЛАЙ"

**Симптом:** Несмотря на список "НЕ ДЕЛАЙ", LLM всё равно это делает.

**Причины:**
- Список ограничений слишком длинный
- Ограничения противоречат инструкции
- Ограничения находятся в конце промпта

**Решения:**
1. Перемести критические ограничения в начало
2. Сократи список до 3-5 самых важных
3. Проверь, нет ли противоречий с инструкциями
4. Используй позитивные формулировки вместо негативных

**Пример:**
```markdown
# ДО (хорошо):
✅ ИСПОЛЬЗУЙ интерфейсы вместо any
✅ ОБРАБАТЫВАЙ все возможные ошибки

# ПОСЛЕ (плохо):
❌ НЕ используй any
❌ НЕ игнорируй ошибки

# Лучше всего:
# Требования к типам
- Все типы должны быть явно определены
- Используй interface для объектов
- Избегай any без веской причины

# Требования к обработке ошибок
- Все async функции должны обрабатывать ошибки
- Используй try-catch для критических операций
- Логируй ошибки для дебага
```

---

## Проблема 7: Промпт не адаптируется к изменениям в проекте

**Симптом:** После изменения кодовой базы, старый промпт перестаёт работать.

**Причины:**
- Промпт содержит хардкодные значения
- Промпт ссылается на несуществующие файлы
- Нет механизма автоматического обновления

**Решения:**
1. Используй @Symbol вместо хардкода
2. Создай валидацию существования файлов
3. Добавь секцию "Апдейт промпта" в git hooks
4. Используй динамические переменные

**Пример:**
```markdown
# ДО (хардкод):
Создай файл в директории src/components/Button.tsx

# ПОСЛЕ (динамически):
@PROJECT_STRUCTURE.md  // актуальная структура

Создай компонент Button в соответствующей директории.
```

---

## Добавление новых проблем

Если вы столкнулись с проблемой при использовании Prompt Engineering Expert:

1. Опишите проблему детально
2. Укажите шаги воспроизведения
3. Предложите решение (если нашли)
4. Добавьте сюда для сообщества

Это поможет другим разработчикам избежать тех же проблем!