# 💡 Решения - Prompt Engineering Expert Skill

Типичные сценарии и решения для создания эффективных промптов.

## 📝 Как добавить решение

1. Опишите задачу
2. Укажите шаги для решения
3. Добавьте примеры если нужно

---

## Решение 1: Создание промпта для код-ревью

### Задача
Создать промпт для автоматического код-ревью TypeScript кода.

### Шаги решения

**Шаг 1: Определить роль и контекст**
```markdown
# Ты — Senior TypeScript Code Reviewer
```

**Шаг 2: Сформулировать задачу**
```markdown
## Твоя задача
Выполни глубокий код-ревью предоставленного TypeScript кода.
```

**Шаг 3: Определить критерии проверки**
```markdown
Найди:
1. Типовые ошибки (any, отсутствие типов)
2. Нарушения лучших практик
3. Потенциальные баги
4. Уязвимости безопасности
5. Проблемы с производительностью
```

**Шаг 4: Создать инструкции для каждой проблемы**
```markdown
Для каждой проблемы укажи:
- Строку кода
- Тип проблемы (error/warning/suggestion)
- Описание проблемы
- Рекомендацию по исправлению
- Пример исправления (код)
```

**Шаг 5: Определить формат ответа**
```markdown
## Формат ответа
```markdown
## Код-ревью: [Имя файла]

### ✅ Сильные стороны
- ...

### ⚠️ Найденные проблемы
#### 1. [Название] - [Строка]
...
```
```

**Шаг 6: Добавить ограничения**
```markdown
## Ограничения
- Не используй any тип без веской причины
- Следуй TypeScript best practices
- Проверь на null/undefined
```

### Результат
```markdown
# Ты — Senior TypeScript Code Reviewer

## Твоя задача
Выполни глубокий код-ревью предоставленного TypeScript кода.

## Критерии проверки
1. Типовые ошибки (any, отсутствие типов)
2. Нарушения лучших практик
3. Потенциальные баги
4. Уязвимости безопасности
5. Проблемы с производительностью

## Инструкции
Для каждой проблемы укажи:
- Строку кода
- Тип проблемы (error/warning/suggestion)
- Описание проблемы
- Рекомендацию по исправлению
- Пример исправления

## Формат ответа
[определённый формат]

## Ограничения
[список ограничений]
```

---

## Решение 2: Оптимизация длиного промпта

### Задача
Промпт слишком длинный и не помещается в контекстное окно.

### Шаги решения

**Шаг 1: Анализ промпта**
- Найти дублирующуюся информацию
- Найти длинные описания
- Найти слишком много примеров Few-Shot

**Шаг 2: Минимизация примеров**
```markdown
# ДО (10 примеров):
Пример 1: ...
Пример 2: ...
...
Пример 10: ...

# ПОСЛЕ (3 примера):
Пример 1: ...
Пример 2: ...
Пример 3: ...
```

**Шаг 3: Использование символов-сокращений**
```markdown
# ДО (полное описание):
В этом проекте мы используем Node.js версии 20, Express версии 4.18, 
TypeScript версии 5.0, PostgreSQL версии 15, Prisma ORM версии 5.0.

# ПОСЛЕ (@Symbol):
@PROJECT_CONFIG.md  // содержит весь стек технологий
```

**Шаг 4: Удаление дубликатов**
```markdown
# ДО (дублирование):
## Инструкции для A
- Сделай X
- Сделай Y

## Инструкции для B
- Сделай X
- Сделай Y

# ПОСЛЕ (объединение):
## Общие инструкции
- Сделай X
- Сделай Y

## Для A
[специфичные для A]

## Для B
[специфичные для B]
```

**Шаг 5: Разделение на этапы**
```markdown
# ДО (один длинный промпт):
Создай систему аутентификации:
1. Регистрация
2. Логин
3. Восстановление пароля
4. 2FA
...

# ПОСЛЕ (несколько этапов):
## Этап 1: Базовая аутентификация
Создай регистрацию и логин.

## Этап 2: Восстановление пароля
Добавь восстановление пароля.

## Этап 3: 2FA
Добавь двухфакторную аутентификацию.
```

### Результат
Промпт сокращён на 60-70% без потери качества.

---

## Решение 3: Создание промпта для генерации тестов

### Задача
Создать промпт для автоматической генерации Jest тестов.

### Шаги решения

**Шаг 1: Определить роль**
```markdown
# Ты — Senior QA Engineer
```

**Шаг 2: Сформулировать задачу**
```markdown
## Твоя задача
Генерируй качественные Jest тесты для предоставленного TypeScript кода.
```

**Шаг 3: Определить типы тестов**
```markdown
## Типы тестов
1. Unit tests — тестируют отдельные функции
2. Integration tests — тестируют интеграцию модулей
3. E2E tests — тестируют полный пользовательский сценарий
```

**Шаг 4: Создать шаблон теста**
```markdown
## Шаблон теста
```typescript
describe('[ComponentName]', () => {
  beforeEach(() => {
    // Setup
  });

  afterEach(() => {
    // Cleanup
  });

  describe('[FeatureName]', () => {
    it('[TestDescription]', () => {
      // Arrange
      // Act
      // Assert
    });
  });
});
```

**Шаг 5: Добавить требования к покрытию**
```markdown
## Требования
- Покрытие > 80%
- Тестируй все публичные методы
- Включай edge cases
- Тестируй обработку ошибок
```

**Шаг 6: Добавить Few-Shot примеры**
```markdown
## Примеры

### Пример 1: Unit test для функции
```typescript
// Код
const sum = (a: number, b: number): number => a + b;

// Тест
describe('sum', () => {
  it('should return sum of two numbers', () => {
    expect(sum(2, 3)).toBe(5);
  });

  it('should handle negative numbers', () => {
    expect(sum(-2, 3)).toBe(1);
  });
});
```
```

### Результат
Эффективный промпт для генерации качественных тестов.

---

## Решение 4: Создание промпта для рефакторинга

### Задача
Создать промпт для пошагового рефакторинга сложного кода.

### Шаги решения

**Шаг 1: Определить роль**
```markdown
# Ты — Senior Software Architect
```

**Шаг 2: Сформулировать задачу**
```markdown
## Твоя задача
Выполни рефакторинг предоставленного кода, сохраняя функциональность.
```

**Шаг 3: Определить критерии рефакторинга**
```markdown
## Критерии
- Упрощение логики
- Улучшение читаемости
- Уменьшение сложности (cyclomatic complexity)
- Следование SOLID принципам
- Сохранение функциональности
```

**Шаг 4: Создать процесс рефакторинга**
```markdown
## Процесс
1. Анализ текущего кода
2. Выявление проблем
3. Предложение улучшений
4. Пошаговое применение изменений
5. Проверка после каждого шага
```

**Шаг 5: Добавить Chain-of-Thought**
```markdown
## Пошаговый анализ

### Шаг 1: Анализ
Проанализируй код:
- Какова сложность? (функциональная/цикломатическая)
- Есть ли дублирование?
- Нарушает ли принципы?

### Шаг 2: Проблемы
Перечисли найденные проблемы с приоритетами:
1. Критические
2. Высокие
3. Средние
4. Низкие

### Шаг 3: Решения
Для каждой проблемы предложи решение.

### Шаг 4: Рефакторинг
Применяй изменения пошагово.
```

**Шаг 6: Определить формат ответа**
```markdown
## Формат ответа
```markdown
## Рефакторинг: [Имя файла]

### Анализ
[Детали анализа]

### Найденные проблемы
1. [Проблема] - [Приоритет]
2. ...

### Предложенные решения
1. [Решение для проблемы 1]
2. ...

### Пошаговое применение
#### Шаг 1: [Название]
[Код до]
↓
[Код после]
[Обоснование]

#### Шаг 2: [Название]
...
```

### Результат
Промпт для систематического рефакторинга с пошаговым подходом.

---

## Решение 5: Создание промпта для документации

### Задача
Создать промпт для автоматической генерации документации.

### Шаги решения

**Шаг 1: Определить роль**
```markdown
# Ты — Technical Writer с экспертизой в [Язык/Фреймворк]
```

**Шаг 2: Сформулировать задачу**
```markdown
## Твоя задача
Создай качественную документацию для предоставленного кода.
```

**Шаг 3: Определить типы документации**
```markdown
## Типы документации
1. JSDoc — для TypeScript кода
2. README — для проекта/модуля
3. API Reference — для публичного API
4. Guides — для пользовательских руководств
```

**Шаг 4: Создать шаблон JSDoc**
```markdown
## Шаблон JSDoc
```typescript
/**
 * [Краткое описание (1 предложение)]
 *
 * [Детальное описание]
 *
 * @param {Type} paramName - [Описание]
 * @returns {Type} [Описание]
 * @throws {ErrorType} [Когда]
 *
 * @example
 * ```typescript
 * // [Пример]
 * ```
 */
```

**Шаг 5: Добавить требования к качеству**
```markdown
## Требования к качеству
- Краткие, но полные описания
- Примеры использования для всех публичных API
- @example тег обязателен
- Следуй Google JSDoc Style Guide
- Избегай очевидных описаний ("This function returns...")
```

**Шаг 6: Добавить Few-Shot примеры**
```markdown
## Примеры

### Пример 1: Функция с параметрами
```typescript
/**
 * Calculates the total price including tax.
 *
 * Multiplies the base price by (1 + tax rate) to calculate the final amount.
 *
 * @param {number} basePrice - The original price before tax
 * @param {number} taxRate - The tax rate as a decimal (e.g., 0.1 for 10%)
 * @returns {number} The total price including tax
 *
 * @example
 * ```typescript
 * const total = calculateTotalPrice(100, 0.2);
 * console.log(total); // 120
 * ```
 */
function calculateTotalPrice(basePrice: number, taxRate: number): number {
  return basePrice * (1 + taxRate);
}
```
```

### Результат
Эффективный промпт для генерации качественной документации.

---

## Добавление новых решений

Если у вас есть типичное решение для prompt engineering:

1. Опишите задачу
2. Укажите шаги для решения
3. Добавьте примеры если нужно
4. Добавьте сюда для сообщества

Это поможет другим разработчикам создавать лучшие промпты!