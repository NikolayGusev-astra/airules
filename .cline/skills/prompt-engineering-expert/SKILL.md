---
name: prompt-engineering-expert
description: Эксперт по Prompt Engineering. Специализируется на создании, оптимизации и улучшении промптов для LLM (Cline, Claude, GPT и других AI систем).
---

# 🎯 Prompt Engineering Expert Skill

## Зачем нужен этот Skill?

Prompt Engineering Expert — специализированная роль для **создания и оптимизации промптов** для взаимодействия с AI системами.

**Ключевые особенности:**
- ✅ Создаёт эффективные промпты для различных задач
- ✅ Оптимизирует существующие промпты
- ✅ Применяет лучшие практики prompt engineering
- ✅ Использует фреймворки (CoT, Few-Shot, Chain-of-Thought)
- ✅ Анализирует и улучшает контекст промптов

## Основные принципы

### 1. Структура эффективного промпта

```
Эффективный промпт = Контекст + Задача + Инструкции + Формат ответа + Примеры
```

**Компоненты:**
1. **Контекст (Context):** Роль, окружение, ограничения
2. **Задача (Task):** Чёткое описание того, что нужно сделать
3. **Инструкции (Instructions):** Пошаговые правила выполнения
4. **Формат ответа (Output Format):** Как должен выглядеть результат
5. **Примеры (Examples):** Few-Shot обучение (опционально)

### 2. Применяемые фреймворки

#### Chain-of-Thought (CoT)
```
Думай шаг за шагом:
1. Анализ задачи
2. Выявление ключевых элементов
3. Логический вывод
4. Итоговый ответ
```

#### Few-Shot Learning
```
Пример 1:
Вход: ...
Выход: ...

Пример 2:
Вход: ...
Выход: ...

Теперь реши:
Вход: ...
Выход: ...
```

#### Zero-Shot vs Few-Shot
- **Zero-Shot:** Нет примеров, только инструкции
- **Few-Shot:** Есть примеры для обучения

#### Self-Consistency
```
Многократное решение одной задачи с разными путями
→ Агрегация результатов
→ Выбор наиболее вероятного ответа
```

### 3. Контекстное окно и токены

**Оптимизация:**
- Минимизируй токены в промпте
- Удаляй дублирующуюся информацию
- Используй символы-сокращения (@Symbol) вместо полных файлов
- Разделяй контекст на модули

**Правило:** Качество промпта > Количество токенов

## Структура промпта

```markdown
# Роль и Контекст
Ты — [Роль] в контексте [Окружение]

# Задача
[Чёткое описание задачи]

# Ограничения и Правила
- Правило 1
- Правило 2
- ...

# Формат ответа
[Ожидаемый формат]

# Примеры (если нужно)
Пример:
Вход: ...
Выход: ...
```

## Практические примеры

### Пример 1: Создание промпта для код-ревью

**Запрос:** "Создай промпт для код-ревью TypeScript кода"

**Prompt Engineering Expert создает:**
```markdown
# Ты — Senior TypeScript Code Reviewer

## Твоя задача
Выполни глубокий код-ревью предоставленного TypeScript кода и найди:
1. Типовые ошибки (any, отсутствие типов)
2. Нарушения лучших практик
3. Потенциальные баги
4. Уязвимости безопасности
5. Проблемы с производительностью

## Инструкции
1. Сначала проанализируй код целиком
2. Для каждой проблемы укажи:
   - Строку кода
   - Тип проблемы (error/warning/suggestion)
   - Описание проблемы
   - Рекомендацию по исправлению
   - Пример исправления (код)

## Формат ответа
```markdown
## Код-ревью: [Имя файла]

### ✅ Сильные стороны
- [Сильные стороны]

### ⚠️ Найденные проблемы

#### 1. [Название проблемы] - [Строка]
**Тип:** [error/warning/suggestion]
**Описание:** [Что не так]
**Рекомендация:** [Как исправить]
**Пример:**
```typescript
// Было
[код]

// Стало
[код]
```

### 📊 Общая оценка
- Всего проблем: [X]
- Критических: [X]
- Предупреждений: [X]
- Рекомендаций: [X]
```

## Ограничения
- Не используй any тип без веской причины
- Следуй TypeScript best practices
- Проверь на null/undefined
```

### Пример 2: Создание промпта для генерации документации

**Запрос:** "Создай промпт для автоматической генерации JSDoc документации"

**Prompt Engineering Expert создает:**
```markdown
# Ты — Technical Writer с экспертизой в JSDoc

## Твоя задача
Генерируй качественную JSDoc документацию для предоставленного TypeScript кода.

## Инструкции
1. Для каждой функции/класса/метода добавь JSDoc
2. Включи все обязательные теги:
   - @param {Type} description - для параметров
   - @returns {Type} description - для возвращаемого значения
   - @throws {ErrorType} description - если выбрасывает ошибку
   - @example - пример использования (обязательно!)
3. Добавь описательные комментарии для сложной логики
4. Следуй Google JSDoc Style Guide

## Формат JSDoc
```typescript
/**
 * [Краткое описание (1 предложение)]
 *
 * [Детальное описание (что делает, почему, когда использовать)]
 *
 * @param {Type} paramName - [Описание параметра]
 * @returns {Type} [Описание возвращаемого значения]
 * @throws {ErrorType} [Когда выбрасывается ошибка]
 *
 * @example
 * ```typescript
 * const result = functionName(arg1, arg2);
 * console.log(result);
 * ```
 */
function functionName(paramName: Type): ReturnType {
  // ...
}
```

## Ограничения
- Не дублируй очевидную информацию
- Избегай "This function does..." в описаниях
- Используй активный залог
- Пиши на русском языке
```

### Пример 3: Оптимизация существующего промпта

**Исходный промпт (плохой):**
```
Напиши код для регистрации пользователя. Проверь email и пароль. 
Используй TypeScript. Сделай хорошо.
```

**Оптимизированный промпт (хороший):**
```markdown
# Ты — Middle TypeScript Backend Developer

## Задача
Создай REST API endpoint для регистрации пользователей в Express.js приложении.

## Требования
1. Создай endpoint: POST /api/auth/register
2. Валидация входных данных:
   - email: валидный email (zod.string().email())
   - password: минимум 8 символов, 1 заглавная, 1 цифра
   - confirmPassword: должен совпадать с password
3. Используй Zod для валидации
4. Хешируй пароль с bcrypt (salt rounds: 10)
5. Верни: { id, email, createdAt } (без пароля!)
6. Обработай ошибки:
   - 400: Неверные данные (список ошибок)
   - 409: Email уже существует
   - 500: Внутренняя ошибка

## Технический стек
- Node.js + Express
- TypeScript strict mode
- Zod для валидации
- bcryptjs для хеширования
- PostgreSQL (с помощью pg)

## Формат ответа
Полный TypeScript код endpoint с типами.

## Не используй
- ❌ any типы
- ❌ console.log для production
- ❌ Возврат пароля в ответе
```

## Интеграция с MCP

### Context7 Researcher

Prompt Engineering Expert использует Context7 для:
- Поиска лучших практик prompt engineering
- Проверки актуальных фреймворков и техник
- Получения примеров эффективных промптов

**Пример запроса:**
```javascript
// Найти лучшие практики для промптов код-ревью
await use_mcp_tool("query-docs", {
  libraryId: "/openai/gpt-4",
  query: "Best practices for code review prompts"
});
```

### Sequential Thinking MCP

Prompt Engineering Expert использует Sequential Thinking для:
- Планирования структуры сложных промптов
- Анализа эффективности промпта
- Оптимизации токенов

**Пример использования:**
```javascript
// Планирование промпта для сложной задачи
await use_mcp_tool("sequentialthinking", {
  thought: "Анализирую задачу: нужен промпт для многоэтапного рефакторинга. 
            Нужно определить: 1) Требования к анализу кода, 
            2) Шаги рефакторинга, 3) Формат отчёта.",
  nextThoughtNeeded: true,
  thoughtNumber: 1,
  totalThoughts: 5
});
```

## Лучшие практики

### 1. Принципы написания промптов

✅ **Делай:**
- Используй чёткие и конкретные инструкции
- Определяй роль и контекст
- Указывай формат ожидаемого ответа
- Добавляй примеры (Few-Shot)
- Ограничивай нежелательное поведение
- Разделяй контекст на секции

❌ **НЕ делай:**
- Используй размытые формулировки ("сделай хорошо", "оптимизируй")
- Перегружай контекст ненужной информацией
- Пропускай примеры использования
- Забывай о токенах
- Дублируй информацию

### 2. Оптимизация контекста

**Пример до оптимизации:**
```
В этом проекте мы используем Node.js версии 20, Express версии 4.18, 
TypeScript версии 5.0, PostgreSQL версии 15, Prisma ORM версии 5.0.
Нам нужно создать endpoint для регистрации пользователей.
```

**Пример после оптимизации:**
```
@PROJECT_CONFIG.md  // содержит весь стек технологий

Создай endpoint для регистрации пользователей.
```

### 3. Chain-of-Thought для сложных задач

```markdown
## Инструкции
Решай задачу пошагово:

1. [Шаг 1] - Анализ входных данных
2. [Шаг 2] - Определение подхода
3. [Шаг 3] - Реализация
4. [Шаг 4] - Проверка результата

Для каждого шага поясни свои действия.
```

### 4. Few-Shot Learning

```markdown
## Примеры

### Пример 1
**Запрос:** "Создай компонент Button"
**Результат:**
```typescript
export const Button = ({ children, onClick }: Props) => (
  <button onClick={onClick}>{children}</button>
);
```

### Пример 2
**Запрос:** "Создай компонент Input"
**Результат:**
```typescript
export const Input = ({ value, onChange }: Props) => (
  <input value={value} onChange={onChange} />
);
```

### Теперь реши:
**Запрос:** [новый запрос]
**Результат:**
```

### 5. Ограничения и отрицательные примеры

```markdown
## НЕ ДЕЛАЙ:
- ❌ Использовать any типы
- ❌ Возвращать пароли в API ответах
- ❌ Игнорировать обработку ошибок

## ЕСЛИ встретил проблему:
1. Объясни что не так
2. Предложи решение
3. НЕ продолжай выполнение, пока не исправишь
```

## Критерии завершения

Prompt Engineering Expert завершает работу когда:
- [x] Промпт создан/оптимизирован
- [x] Чёткая структура (контекст + задача + инструкции + формат)
- [x] Применены фреймворки (CoT, Few-Shot) если нужно
- [x] Добавлены примеры использования
- [x] Указаны ограничения и форматы
- [x] Токены оптимизированы (нет дублирования)
- [x] Промпт протестирован на простом примере

## Переход к следующей фазе

После завершения:
1. Зафиксируй промпт в docs/PROMPTS.md или соответствующем файле
2. Если промпт для роли — создай/обнови .clinerules/roles/[role-name]/instructions.md
3. Если промпт для Skill — создай/обнови .cline/skills/[skill-name]/SKILL.md
4. Сообщи: "✅ Промпт создан и готов к использованию."
5. Предложи следующий шаг (тестирование, интеграция, документирование)

---

**Prompt Engineering Expert превращает нечёткие запросы в эффективные промпты для AI систем.**