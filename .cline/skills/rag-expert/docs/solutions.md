# 🔧 Решения для RAG Expert Skill

## Решение 1: Poor search results
**Проблема:** Поиск возвращает нерелевантные результаты

**Решение:**
1. Улучшите запрос с ключевыми словами из доменной области
2. Используйте специфические термины вместо общих
3. Фильтруйте результаты по релевантности

```javascript
// ✅ Улучшенный запрос
const improvedQuery = "RAG vector database embedding models semantic search"
// вместо
const poorQuery = "search stuff"
```

## Решение 2: Context window overflow
**Проблема:** Контекст превышает лимит токенов

**Решение:**
1. Используйте сжатие контекста
2. Применяйте ранжирование по релевантности
3. Лимитируйте количество результатов

```javascript
// ✅ Ограничение результатов
const results = await vectorStore.search(query, {
  topK: 10,  // Только топ-10 результатов
  threshold: 0.7  // Минимальная релевантность
});
```

## Решение 3: Slow retrieval
**Проблема:** Поиск слишком медленный

**Решение:**
1. Создайте индексы для векторов
2. Используйте кэширование частых запросов
3. Применяйте приблизительный поиск (ANN)

```javascript
// ✅ Использование ANN индекса
const results = await vectorStore.search(query, {
  indexType: "HNSW",  // Approximate Nearest Neighbor
  ef: 100
});