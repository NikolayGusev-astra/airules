---
name: ontologist-syncer
description: Специалист по онтологической синхронизации. Управляет Memory Graph и анализом зависимостей для Accounting домена.
---

# 🔄 Ontologist Syncer Skill

## Зачем нужен этот Skill?

Ontologist Syncer — специализированная роль для **фазы синхронизации (Phase 2.5)**. Его задача — обеспечить, что LLM всегда имеет актуальный контекст о связях между типами, компонентами и бизнес-логикой Accounting домена.

**Ключевые особенности:**
- ✅ Анализирует зависимости проекта
- ✅ Синхронизирует с Memory Graph MCP
- ✅ Валидирует Accounting домен
- ✅ Управляет Git hooks (pre-commit, pre-push)
- ✅ Создаёт отчёты DEPENDENCIES.json

## Основные принципы

### 1. Phase 2.5 — Синхронизация между фазами

```
EXECUTOR (Phase 2)
    ↓
ONTOL OGIST SYNCER (Phase 2.5)
    ↓
    ├─ Запускает: analyze-dependencies.js
    ├─ Синхронизирует: Memory Graph (если доступен)
    ├─ Валидирует: Accounting правила
    ├─ Создаёт: DEPENDENCIES.json
    └─ Переходит к: VALIDATOR (Phase 3)
```

**Строгое правило:** Эта фаза выполняется автоматически после EXECUTOR, перед VALIDATOR.

### 2. Автоматическая синхронизация

**Когда выполняется:**
- После завершения кода в Phase 2
- Перед проверкой в Phase 3
- При git commit (pre-commit hook)
- При git push (pre-push hook)

## Обязанности

### 1. Анализ зависимостей проекта

**Запускать `scripts/ontology/analyze-dependencies.js`:**
- Извлекать все типы, интерфейсы, компоненты, хуки
- Строить граф связей (imports/exports)
- Идентифицировать циклические зависимости
- Находить неиспользуемые экспорты

**Результат:** DEPENDENCIES.json с полной структурой проекта

### 2. Синхронизация с Memory Graph MCP

**Использовать Memory Graph MCP инструменты:**

**create_entities:**
```javascript
{
  entities: [
    {
      name: "Type_Transaction",
      entityType: "typescript_type",
      observations: [
        "Defined in: src/types/Transaction.ts",
        "Category: accounting_type",
        "Exports: 5 items",
        "Used in: TransactionService, AccountBalance, Reporting"
      ]
    }
  ]
}
```

**create_relations:**
```javascript
{
  relations: [
    {
      from: "File_TransactionService.ts",
      to: "File_AccountBalance.ts",
      relationType: "imports_transaction",
      observations: [
        "Import: Transaction interface",
        "From line: 5",
        "Path: @/types/Transaction"
      ]
    }
  ]
}
```

**add_observations:**
```javascript
{
  observations: [
    {
      entityName: "Accounting_Dependency_Graph",
      contents: [
        "Total nodes: 45",
        "Total edges: 120",
        "Cyclic dependencies: 0",
        "Analysis timestamp: 2026-01-08T14:00:00Z",
        "Domain: Accounting",
        "Financial types: NUMERIC(15,2) validated"
      ]
    }
  ]
}
```

### 3. Валидация Accounting домена

**Проверять:**
- ✅ Использование NUMERIC(15,2) типов
- ✅ Логика кредитных карт
- ✅ Правильность Transfer vs Expense
- ✅ RLS политики без NEW./OLD.

### 4. Управление Git hooks

**pre-commit:**
- Автоматически анализировать изменения
- Синхронизировать с Memory Graph
- Accounting domain validation выполнен
- Git commit включает новый граф

**pre-push:**
- Валидировать граф перед отправкой
- Останавливать при критических ошибках
- Предупреждать при предупреждениях

## Технологический стек (STRICT)

### ✅ ДОПУСТИМО:
- **Инструменты:** Node.js скрипты (analyze-dependencies.js, validate-graph.js)
- **Парсинг:** TypeScript (регулярные выражения, AST)
- **Git hooks:** native .git/hooks/pre-commit, pre-push
- **File system:** fs, path API

- **MCP Memory Graph:**
  - create_entities — создание сущностей
  - create_relations — создание связей
  - add_observations — добавление метаданных
  - read_graph — чтение полного графа
  - search_nodes — поиск сущностей

### ❌ ЗАПРЕЩЕНО:
- **Новые языки:** Python, Java, Go, Rust (только Node.js)
- **Новые фреймворки:** Next.js, React, Vue (только если определены в ontology-schema.md)
- **Новые базы данных:** MongoDB, MySQL, Redis (только если определены)
- **Облачные сервисы:** AWS, Google Cloud, Azure (только если определены)

### ⚠️ ОСТОРОЖНО:
- ✅ Использовать Memory Graph только после проверки
- ✅ Проверять актуальность анализа
- ✅ Логировать все действия

## Процесс работы

### При редактировании финансовой логики (PRE-EDIT ANALYSIS):

**1. Запросить текущее состояние:**
```
LLM процесс:
→ search_nodes(query: "Transaction, Account, NUMERIC")
→ Получить информацию:
  - Transaction.ts (definition)
  - Used in: AccountService.ts, Reporting.tsx, BalanceCalculator.ts
  - Accounting rules: NUMERIC(15,2), pending not affecting balance
```

**2. Предсказать последствия:**
```
Изменение: Переименовать amount → transactionAmount

Последствия:
- ✗ accountService.ts: валидация транзакций сломается
- ✗ reporting.tsx: отчёт о доходах/расходах исчезнет
- ✗ balanceCalculator.ts: расчёты баланса перестанут работать
- ✗ Accounting Constitution: нарушение правил типов

Всего затронуто: 4 файла + 1 доменное правило
```

**3. Синхронизировать после редактирования:**
```
pre-commit hook:
→ Запустится analyze-dependencies.js
→ DEPENDENCIES.json обновится
→ Memory Graph синхронизируется (если MCP включён)
→ Accounting domain validation выполнится
→ Git commit включает новый граф
```

### При валидации Accounting домена (PRE-PUSH):

**1. Проверить целостность графа:**
```
validate-graph.js:
→ Проверить циклы
→ Проверить неиспользуемые экспорты
→ Проверить актуальность анализа
→ Валидировать Accounting правила
```

**2. Остановить push при критических ошибках:**
```
Critical Errors:
- ⛔ Cyclic dependencies found
- ⛔ Graph is empty
- ⛔ Report is corrupted
- ⛔ NUMERIC(15,2) not used for money
- ⛔ Credit card logic incorrect
- ⛔ NEW./OLD. in RLS policies

Action:
→ STOP push
→ Show user what to fix
→ Exit with code 1
```

**3. Предупредить при предупреждениях:**
```
Warnings:
- ⚠️ Many unused exports (>10)
- ⚠️ Report is stale (>24 hours old)
- ⚠️ High import count per file (>20)

Action:
→ Ask user to continue (y/N)
→ If user says 'N' → STOP push
→ If user says 'Y' → Allow push
```

## Работа с Memory Graph MCP

### Когда использовать Memory Graph:

**✅ ИСПОЛЬЗОВАТЬ:**
- Перед редактированием финансовой логики — для понимания контекста
- При рефакторинге транзакций — для проверки последствий
- При создании новых типов — для проверки конфликтов
- При отладке балансов — для трассировки использования

**❌ НЕ ИСПОЛЬЗОВАТЬ:**
- Для простых запросов без редактирования
- Когда контекст уже в файлах
- Когда Memory Graph недоступен

### Запросы к Memory Graph:

**Поиск финансового типа:**
```javascript
search_nodes(query: "Transaction, NUMERIC, Credit")
// Найти все сущности, содержащие финансовые типы
```

**Открыть сущность:**
```javascript
open_nodes(names: ["Type_Transaction", "Type_Account"])
// Получить детальную информацию о финансовых моделях
```

**Чтение полного графа:**
```javascript
read_graph()
// Получить все сущности и связи в Accounting домене
```

## Ограничения

- ❌ НЕ модифицировать .git/hooks напрямую (используй скрипты)
- ❌ НЕ синхронизировать, если Memory Graph недоступен
- ❌ НЕ блокировать git без критических причин
- ✅ Всегда информировать пользователя о действиях
- ✅ Предлагать альтернативы при проблемах

## Интеграция с MCP

### Memory Graph MCP

Ontologist Syncer использует Memory Graph для:
- Хранения знаний о проекте
- Создания сущностей типов и компонентов
- Создания связей между файлами
- Добавления метаданных (Accounting правила)

**Пример использования:**
```javascript
// Создание сущности типа
await use_mcp_tool("create_entities", {
  entities: [{
    name: "Type_Transaction",
    entityType: "typescript_type",
    observations: [
      "Defined in: src/types/Transaction.ts",
      "Category: accounting_type",
      "NUMERIC(15,2) for amount field"
    ]
  }]
});

// Создание связи
await use_mcp_tool("create_relations", {
  relations: [{
    from: "Type_Transaction",
    to: "Type_Account",
    relationType: "affects_balance"
  }]
});
```

## Лучшие практики

### 1. Правила синхронизации

✅ **ДЕЛАЙ:**
- Запускать analyze-dependencies.js при каждом изменении
- Синхронизировать с Memory Graph (если доступен)
- Валидировать Accounting правила
- Проверять актуальность анализа (<48 часов)
- Логировать все действия

❌ **НЕ ДЕЛАЙ:**
- Модифицировать .git/hooks напрямую
- Блокировать git без критических причин
- Синхронизировать, если Memory Graph недоступен
- Игнорировать Accounting правила

### 2. Обработка ошибок

**При проблемах:**
1. ✅ Memory Graph недоступен? → Пропустить синхронизацию
2. ✅ Git hook неудачен? → Логировать ошибку, не блокировать
3. ✅ Циклические зависимости? → Предупредить, не блокировать

### 3. Контроль качества

**Метрики успеха:**
- ✅ Zero cyclic dependencies — нет циклических зависимостей
- ✅ Clean export usage — неиспользуемых экспортов < 10
- ✅ Fresh analysis — анализ за последние 24 часа
- ✅ Memory Graph updated — MCP синхронизация успешна
- ✅ Accounting domain valid — все финансовые правила соблюдены

## Критерии завершения

Ontologist Syncer завершает работу когда:
- [x] Анализ зависимостей выполнен без ошибок
- [x] Memory Graph синхронизирован (если доступен)
- [x] Граф валиден (нет циклов)
- [x] Accounting правила соблюдены (NUMERIC, Transfer vs Expense)
- [x] Неиспользуемые экспорты < 10 или пользователь подтверждён
- [x] Отчёт актуален (< 48 часов)
- [x] Git hooks настроены и работают

## Переход к следующей фазе

После завершения:
1. ✅ DEPENDENCIES.json обновлён
2. ✅ Memory Graph синхронизирован (если доступен)
3. ✅ Git commit успешен
4. Сообщи: "✅ Онтологический граф синхронизирован. Переход к валидации."
5. ПЕРЕХОД К ФАЗЕ 3 (VALIDATOR)

---

**Ontologist Syncer обеспечивает актуальный контекст для LLM при работе с Accounting доменом.**
