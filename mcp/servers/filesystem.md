# 📁 Filesystem MCP Server

**Тип:** File System Operations  
**Источник:** Model Context Protocol Reference Implementation  
**Статус:** ✅ Стабильный

## 📖 Описание

Filesystem - это мощный MCP сервер для безопасной работы с файловой системой. Предоставляет полный набор операций для чтения, записи, управления файлами и директориями с гибкой системой контроля доступа через Roots протокол.

## 🛠️ Возможности

### Инструменты (Tools)

#### `read_text_file` - Чтение текстовых файлов
**Параметры:**
- `path` (string): Путь к файлу
- `head` (number, optional): Первые N строк
- `tail` (number, optional): Последние N строк

#### `read_media_file` - Чтение медиа-файлов
**Параметры:**
- `path` (string): Путь к файлу (изображения/аудио)

#### `read_multiple_files` - Чтение нескольких файлов
**Параметры:**
- `paths` (string[]): Массив путей к файлам

#### `write_file` - Запись файла
**Параметры:**
- `path` (string): Путь к файлу
- `content` (string): Содержимое файла

#### `edit_file` - Редактирование файла
**Параметры:**
- `path` (string): Путь к файлу
- `edits` (array): Массив изменений
- `dryRun` (boolean, optional): Предварительный просмотр

**Особенности редактирования:**
- Поиск по шаблонам
- Многострочное редактирование
- Сохранение отступов
- Git-style diff вывод
- Безопасный предварительный просмотр

#### `create_directory` - Создание директории
**Параметры:**
- `path` (string): Путь к директории

#### `list_directory` - Список содержимого директории
**Параметры:**
- `path` (string): Путь к директории

#### `list_directory_with_sizes` - Список с размерами
**Параметры:**
- `path` (string): Путь к директории
- `sortBy` (string, optional): Сортировка по "name" или "size"

#### `move_file` - Перемещение/переименование
**Параметры:**
- `source` (string): Исходный путь
- `destination` (string): Целевой путь

#### `search_files` - Поиск файлов
**Параметры:**
- `path` (string): Начальная директория
- `pattern` (string): Шаблон поиска
- `excludePatterns` (string[], optional): Исключаемые шаблоны

#### `directory_tree` - Дерево директории (JSON)
**Параметры:**
- `path` (string): Начальная директория
- `excludePatterns` (string[], optional): Исключаемые шаблоны

#### `get_file_info` - Информация о файле
**Параметры:**
- `path` (string): Путь к файлу/директории

#### `list_allowed_directories` - Разрешенные директории
Показывает все директории, доступные серверу

## 🔒 Система контроля доступа

### Roots протокол (рекомендуемый)
MCP клиенты могут динамически задавать разрешенные директории:

```json
{
  "roots": [
    {
      "uri": "file:///Users/username/Projects",
      "name": "Projects"
    }
  ]
}
```

### Аргументы командной строки
```bash
mcp-server-filesystem /path/to/dir1 /path/to/dir2
```

### Правила доступа
- Все операции ограничены разрешенными директориями
- Сервер требует минимум одной разрешенной директории
- Roots имеют приоритет над аргументами командной строки

## 🚀 Установка

### NPX
```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/Users/username/Desktop"
      ]
    }
  }
}
```

### Docker
```json
{
  "mcpServers": {
    "filesystem": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--mount", "type=bind,src=/Users/username/Desktop,dst=/projects/Desktop",
        "mcp/filesystem",
        "/projects"
      ]
    }
  }
}
```

## 📋 Использование

### Чтение файла
```javascript
await callTool("filesystem", "read_text_file", {
  path: "/path/to/file.txt"
});
```

### Запись файла
```javascript
await callTool("filesystem", "write_file", {
  path: "/path/to/newfile.txt",
  content: "Hello World!"
});
```

### Поиск файлов
```javascript
await callTool("filesystem", "search_files", {
  path: "/projects",
  pattern: "*.js",
  excludePatterns: ["node_modules"]
});
```

### Редактирование с предварительным просмотром
```javascript
await callTool("filesystem", "edit_file", {
  path: "/path/to/file.js",
  edits: [{
    oldText: "console.log('old');",
    newText: "console.log('new');"
  }],
  dryRun: true
});
```

## ⚙️ Конфигурация

### Tool Annotations
Сервер поддерживает MCP Tool Annotations для указания:
- `readOnlyHint`: инструменты только для чтения
- `idempotentHint`: безопасные для повторения операции
- `destructiveHint`: операции, изменяющие данные

### Windows поддержка
```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "C:\\Users\\username\\Desktop"
      ]
    }
  }
}
```

## 🔧 Разработка

### Сборка Docker
```bash
docker build -t mcp/filesystem -f src/filesystem/Dockerfile .
```

## 📚 Ссылки

- [Исходный код](https://github.com/modelcontextprotocol/servers/tree/main/src/filesystem)
- [Roots протокол](https://modelcontextprotocol.io/docs/learn/client-concepts#roots)

---

**Назначение:** Безопасное управление файловой системой с гибким контролем доступа
