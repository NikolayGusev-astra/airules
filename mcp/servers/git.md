# 🐙 Git MCP Server

**Тип:** Version Control Operations  
**Источник:** Model Context Protocol Reference Implementation  
**Статус:** ✅ Стабильный

## 📖 Описание

Git - это MCP сервер для работы с Git репозиториями. Позволяет AI ассистентам выполнять полный спектр Git операций: от базового статуса и diff до сложного управления ветками и коммитов с фильтрацией по времени.

## 🛠️ Возможности

### Инструменты (Tools)

#### `git_status` - Статус рабочей директории
**Параметры:**
- `repo_path` (string): Путь к Git репозиторию
**Возвращает:** Текущий статус рабочей директории

#### `git_diff_unstaged` - Незастейженные изменения
**Параметры:**
- `repo_path` (string): Путь к репозиторию
- `context_lines` (integer, optional): Количество контекстных строк (default: 3)
**Возвращает:** Diff незакоммиченных изменений

#### `git_diff_staged` - Застейженные изменения
**Параметры:**
- `repo_path` (string): Путь к репозиторию
- `context_lines` (integer, optional): Количество контекстных строк (default: 3)
**Возвращает:** Diff застейженных изменений

#### `git_diff` - Сравнение веток/коммитов
**Параметры:**
- `repo_path` (string): Путь к репозиторию
- `target` (string): Целевая ветка или коммит
- `context_lines` (integer, optional): Количество контекстных строк (default: 3)
**Возвращает:** Diff между текущим состоянием и целью

#### `git_commit` - Создание коммита
**Параметры:**
- `repo_path` (string): Путь к репозиторию
- `message` (string): Сообщение коммита
**Возвращает:** Подтверждение с хэшем нового коммита

#### `git_add` - Добавление в индекс
**Параметры:**
- `repo_path` (string): Путь к репозиторию
- `files` (string[]): Массив путей к файлам
**Возвращает:** Подтверждение добавленных файлов

#### `git_reset` - Сброс индекса
**Параметры:**
- `repo_path` (string): Путь к репозиторию
**Возвращает:** Подтверждение сброса

#### `git_log` - История коммитов
**Параметры:**
- `repo_path` (string): Путь к репозиторию
- `max_count` (integer, optional): Максимальное количество коммитов (default: 10)
- `start_timestamp` (string, optional): Начальная дата (ISO 8601, relative dates)
- `end_timestamp` (string, optional): Конечная дата (ISO 8601, relative dates)
**Возвращает:** Массив объектов коммитов

#### `git_create_branch` - Создание ветки
**Параметры:**
- `repo_path` (string): Путь к репозиторию
- `branch_name` (string): Имя новой ветки
- `base_branch` (string, optional): Базовая ветка (default: текущая)
**Возвращает:** Подтверждение создания ветки

#### `git_checkout` - Переключение ветки
**Параметры:**
- `repo_path` (string): Путь к репозиторию
- `branch_name` (string): Имя ветки для переключения
**Возвращает:** Подтверждение переключения

#### `git_show` - Просмотр коммита
**Параметры:**
- `repo_path` (string): Путь к репозиторию
- `revision` (string): Коммит, ветка или тег
**Возвращает:** Содержимое указанного коммита

#### `git_branch` - Список веток
**Параметры:**
- `repo_path` (string): Путь к репозиторию
- `branch_type` (string): "local", "remote", или "all"
- `contains` (string, optional): SHA коммита, который должна содержать ветка
- `not_contains` (string, optional): SHA коммита, который НЕ должна содержать ветка
**Возвращает:** Список веток

## 🚀 Установка

### UVX (рекомендуемый)
```json
{
  "mcpServers": {
    "git": {
      "command": "uvx",
      "args": ["mcp-server-git", "--repository", "path/to/git/repo"]
    }
  }
}
```

### PIP
```json
{
  "mcpServers": {
    "git": {
      "command": "python",
      "args": ["-m", "mcp_server_git", "--repository", "path/to/git/repo"]
    }
  }
}
```

### Docker
```json
{
  "mcpServers": {
    "git": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "--mount", "type=bind,src=/Users/username,dst=/Users/username",
        "mcp/git"
      ]
    }
  }
}
```

## 📋 Использование

### Проверка статуса репозитория
```javascript
await callTool("git", "git_status", {
  repo_path: "/path/to/repo"
});
```

### Создание коммита
```javascript
// Добавление файлов
await callTool("git", "git_add", {
  repo_path: "/path/to/repo",
  files: ["file1.txt", "file2.js"]
});

// Создание коммита
await callTool("git", "git_commit", {
  repo_path: "/path/to/repo",
  message: "Add new features"
});
```

### Работа с ветками
```javascript
// Создание новой ветки
await callTool("git", "git_create_branch", {
  repo_path: "/path/to/repo",
  branch_name: "feature/new-feature",
  base_branch: "main"
});

// Переключение на ветку
await callTool("git", "git_checkout", {
  repo_path: "/path/to/repo",
  branch_name: "feature/new-feature"
});
```

### Просмотр изменений
```javascript
// Diff незакоммиченных изменений
await callTool("git", "git_diff_unstaged", {
  repo_path: "/path/to/repo",
  context_lines: 5
});

// Diff между ветками
await callTool("git", "git_diff", {
  repo_path: "/path/to/repo",
  target: "main",
  context_lines: 3
});
```

### История коммитов с фильтрацией
```javascript
// Последние 20 коммитов
await callTool("git", "git_log", {
  repo_path: "/path/to/repo",
  max_count: 20
});

// Коммиты за последнюю неделю
await callTool("git", "git_log", {
  repo_path: "/path/to/repo",
  start_timestamp: "1 week ago"
});
```

## ⚙️ Конфигурация

### VS Code
```json
{
  "servers": {
    "git": {
      "command": "uvx",
      "args": ["mcp-server-git"]
    }
  }
}
```

### Zed
```json
"context_servers": {
  "git": {
    "command": {
      "path": "uvx",
      "args": ["mcp-server-git"]
    }
  }
}
```

### Zencoder
```json
{
  "command": "uvx",
  "args": ["mcp-server-git", "--repository", "path/to/git/repo"]
}
```

## 🔧 Разработка

### Установка зависимостей
```bash
pip install mcp-server-git
```

### Запуск
```bash
python -m mcp_server_git --repository /path/to/repo
```

### Дебаггинг
```bash
npx @modelcontextprotocol/inspector uvx mcp-server-git
```

## 📚 Ссылки

- [Исходный код](https://github.com/modelcontextprotocol/servers/tree/main/src/git)
- [Документация Python пакета](https://pypi.org/project/mcp-server-git/)

---

**Назначение:** Полнофункциональное управление Git репозиториями через AI интерфейс
