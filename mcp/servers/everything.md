# 🤖 Everything MCP Server

**Тип:** Reference / Test Server  
**Источник:** Model Context Protocol Reference Implementation  
**Статус:** ✅ Стабильный

## 📖 Описание

Everything - это референсный MCP сервер, который демонстрирует все возможные функции Model Context Protocol. Он не предназначен для практического использования, а служит для тестирования и демонстрации возможностей MCP для разработчиков клиентов.

## 🛠️ Возможности

### Инструменты (Tools)
- **Все возможные типы инструментов MCP** для демонстрации протокола

### Ресурсы (Resources)
- **Все возможные типы ресурсов MCP** для демонстрации протокола

### Промпты (Prompts)
- **Все возможные типы промптов MCP** для демонстрации протокола

### Другие возможности
- Sampling - демонстрация сэмплирования
- Различные транспорты (stdio, HTTP+SSE, Streamable HTTP)

## 🚀 Установка

### NPX
```json
{
  "mcpServers": {
    "everything": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-everything"]
    }
  }
}
```

### Docker
```json
{
  "mcpServers": {
    "everything": {
      "command": "docker",
      "args": ["run", "-i", "--rm", "mcp/everything"]
    }
  }
}
```

## 📋 Использование

### Для тестирования MCP клиентов
```javascript
// Пример вызова инструментов
await callTool("everything", "example_tool", { param: "value" });
```

### Для изучения MCP протокола
- Изучите исходный код для понимания всех возможностей MCP
- Используйте для тестирования новых MCP клиентов

## ⚙️ Конфигурация

### Транспорты
- **stdio** (рекомендуемый)
- **HTTP+SSE** (deprecated)
- **Streamable HTTP** (новый)

## 🔧 Разработка

```bash
cd src/everything
npm install
npm run start:sse          # HTTP+SSE transport
npm run start:streamableHttp  # Streamable HTTP transport
```

## 📚 Ссылки

- [Исходный код](https://github.com/modelcontextprotocol/servers/tree/main/src/everything)
- [MCP Inspector](https://github.com/modelcontextprotocol/inspector)

---

**Назначение:** Тестирование и демонстрация всех возможностей MCP протокола
