# 🎭 Ролевая разработка с LLM

Полное руководство по созданию и использованию ролевых систем для LLM.

## 📖 Оглавление

- [Что такое ролевая разработка](#что-такое-ролевая-разработка)
- [Почему это работает](#почему-это-работает)
- [Анатомия роли](#анатомия-роли)
- [Паттерны ролевого взаимодействия](#паттерны-ролевого-взаимодействия)
- [Проектирование эффективных ролей](#проектирование-эффективных-ролей)
- [Распространённые роли](#распространённые-роли)
- [Примеры реализаций](#примеры-реализаций)

## 🤔 Что такое ролевая разработка?

Ролевая разработка — подход, при котором LLM задаётся конкретная "роль" с чётко определённой ответственностью, компетенциями и ограничениями.

### Аналогия с командой разработки

```
Традиционный подход:
┌─────────────────────────────────┐
│         LLM (один на всех)      │
│   "Напиши полноценное API"      │
└─────────────────────────────────┘
         ↓
   Проблемы:
   • Нет структуры
   • Разные стили
   • Пропущенные детали
   • Нет контроля качества

Ролевой подход:
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│  Architect  │→ │  Developer  │→ │  Validator  │
│ (планирует) │  │ (реализует) │  │ (проверяет) │
└─────────────┘  └─────────────┘  └─────────────┘
         ↓
   Преимущества:
   • Чёткая структура
   • Последовательный стиль
   • Все детали учтены
   • Автоматический контроль
```

## ✨ Почему это работает?

### 1. Привязка к контексту

Роль активирует специфический набор знаний и паттернов мышления.

**Пример:**
```
Роль "Frontend Developer":
→ Активирует: React, CSS, accessibility, responsive design
→ Игнорирует: SQL, Docker, system architecture

Роль "Backend Developer":
→ Активирует: API, database, authentication, performance
→ Игнорирует: CSS, UI/UX, browser compatibility
```

### 2. Снижение когнитивной нагрузки

Одна роль — одна ответственность.

```
Без ролей:
LLM должен одновременно:
✓ Проектировать архитектуру
✓ Выбирать технологии
✓ Писать код
✓ Тестировать
✓ Оптимизировать
→ Перегрузка → ошибки

С ролями:
Архитектор: только проектирование
Разработчик: только кодирование
Тестировщик: только тестирование
→ Фокус на одном → качество выше
```

### 3. Чёткие границы ожиданий

Роль определяет:
- Что ДОЛЖНО быть сделано
- Что НЕЛЬЗЯ делать
- Какова форма результата

## 🔬 Анатомия роли

### Структура эффективной роли

```yaml
role:
  name: "Frontend Architect"
  description: "Старший фронтенд-архитектор"
  
  identity:
    mindset: "Я инженер-архитектор. Моя задача — создать надёжную, масштабируемую систему."
    tone: "профессиональный, технический, точный"
    
  capabilities:
    expertise: ["React", "TypeScript", "State Management"]
    tools: ["VS Code", "Chrome DevTools", "Git"]
    
  responsibilities:
    primary: "Создание архитектуры приложения"
    secondary: ["Выбор технологий", "Определение паттернов"]
    
  constraints:
    allowed: ["React", "Next.js", "TypeScript"]
    forbidden: ["jQuery", "Vanilla JS"]
    strict_rules: [
      "Не писать код в фазе планирования",
      "Всегда учитывать performance"
    ]
    
  output_format:
    deliverables: ["Архитектурный документ", "Техническое задание"]
    structure: "Markdown с диаграммами"
```

### Ключевые компоненты

#### 1. Identity (Идентичность)
```markdown
# Ты — Senior Frontend Architect

## Твоя личность
- Ты опытный архитектор с 10+ лет опыта
- Ты мыслишь системно и масштабно
- Ты всегда учитываю long-term последствия
- Ты ценишь простоту над сложностью

## Твой стиль
- Точные и конкретные решения
- Обоснование каждого выбора
- Учет trade-offs
```

#### 2. Capabilities (Возможности)
```markdown
## Твоя экспертиза
✅ React, Next.js, TypeScript
✅ State Management (Redux, Zustand, Jotai)
✅ Performance Optimization
✅ Accessibility (WCAG 2.1)

## Твои ограничения
❌ Backend (databases, APIs)
❌ DevOps (Docker, Kubernetes)
❌ Mobile (React Native)
```

#### 3. Constraints (Ограничения)
```markdown
## Технологические ограничения (STRICT)
- ДОПУСТИМО: React 18+, TypeScript, Tailwind
- ЗАПРЕЩЕНО: jQuery, Angular 1.x, CoffeeScript

## Архитектурные ограничения (STRICT)
- Максимальная глубина компонента: 5 уровней
- Размер файла: не более 300 строк
- Связи между модулями: только через public API
```

#### 4. Output Format (Формат вывода)
```markdown
## Формат результата
Твой ответ должен включать:

1. **Архитектурное решение**
   - Диаграмма компонентов
   - Flow данных
   - State management стратегия

2. **Обоснование**
   - Почему выбран этот подход
   - Trade-offs
   - Альтернативы (и почему отклонены)

3. **Техническое задание**
   - Чеклист для разработчика
   - Критерии успеха
```

## 🔄 Паттерны ролевого взаимодействия

### Паттерн 1: Последовательный (Waterfall)

```
Architect → Developer → Validator → Tester
```

**Когда использовать:**
- Строго определённые задачи
- Линейный процесс разработки
- Высокие требования к качеству

**Пример:**
```markdown
ФАЗА 1: Architect
- Проектирует архитектуру
- Создаёт ТЗ
- ❌ НЕ пишет код

ФАЗА 2: Developer  
- Реализует по ТЗ
- Следует ограничениям
- ❌ НЕ меняет архитектуру

ФАЗА 3: Validator
- Проверяет соответствие ТЗ
- При ошибке → возврат к Фазе 2
- ✅ OK → переход к Фазе 4

ФАЗА 4: Tester
- Пишет тесты
- Проверяет edge cases
- ✅ OK → завершение
```

### Паттерн 2: Итеративный (Agile)

```
Architect ↔ Developer ↻
     ↓
  Validator
```

**Когда использовать:**
- Быстрый прототипинг
- Неопределённые требования
- Гибкий процесс

### Паттерн 3: Параллельный (Multi-Agent)

```
          → Developer A (Frontend) ↘
Architect                            → Validator
          → Developer B (Backend)  ↗
```

**Когда использовать:**
- Независимые подзадачи
- Разные домены знаний
- Оптимизация по времени

### Паттерн 4: Круговой (Continuous)

```
Architect → Developer → Validator
     ↑                    ↓
     └────────────────────┘
```

**Когда использовать:**
- Рефакторинг
- Оптимизация
- Постоянное улучшение

## 🎯 Проектирование эффективных ролей

### Принцип 1: Единая ответственность (SRP)

❌ **Плохо:**
```markdown
Ты — Fullstack Developer.
Делай всё: фронтенд, бэкенд, тесты, деплой, дизайн.
```

✅ **Хорошо:**
```markdown
Ты — Backend Developer.
Твоя ответственность:
- API дизайн и реализация
- Работа с базой данных
- Аутентификация и авторизация
Больше ничего не делай.
```

### Принцип 2: Чёткие границы

❌ **Плохо:**
```markdown
Реализуй авторизацию.
```

✅ **Хорошо:**
```markdown
## Твоя задача
Реализовать JWT-based аутентификацию.

## В рамках роли
- Создать endpoints: /login, /refresh
- Валидация токенов
- Обработка ошибок аутентификации

## ВНЕ роли (не делай)
❌ UI для входа (делает Frontend Developer)
❌ Хеширование паролей (делает Security Expert)
❌ Rate limiting (делает DevOps Engineer)
```

### Принцип 3: Измеримые результаты

❌ **Плохо:**
```markdown
Напиши качественный код.
```

✅ **Хорошо:**
```markdown
## Критерии качества
- Code coverage > 80%
- Cyclomatic complexity < 10
- Zero ESLint errors
- Max function length: 50 lines
- All functions have JSDoc
```

### Принцип 4: Обратная связь

```markdown
## Механизм валидации
Если Validator обнаруживает ошибку:
1. Опиши конкретную проблему
2. Укажи правило, которое нарушено
3. Отправь обратно на переделку
4. НЕ продолжай, пока не исправлено

## Формат ошибки
⛔ VALIDATION FAILED
Причина: [конкретная причина]
Нарушение: [правило из constraints.md]
Действие: Исправить код
```

## 🧑‍💻 Распространённые роли

### Архитекторы

| Роль | Специализация | Типовые задачи |
|------|---------------|----------------|
| System Architect | Общая архитектура | Дизайн системы, выбор технологий |
| Frontend Architect | Frontend | Компоненты, state, routing |
| Backend Architect | Backend | API, database, microservices |
| Data Architect | Данные | ERD, ETL, data pipelines |
| Security Architect | Безопасность | Threat modeling, compliance |

### Разработчики

| Роль | Специализация | Типовые задачи |
|------|---------------|----------------|
| Frontend Developer | React/Vue/Angular | UI компоненты, pages, hooks |
| Backend Developer | Node.js/Python/Go | Endpoints, бизнес-логика |
| Mobile Developer | iOS/Android | Native components, navigation |
| Data Engineer | ETL/Pipelines | Data transformation, storage |

### Специалисты качества

| Роль | Специализация | Типовые задачи |
|------|---------------|----------------|
| Code Reviewer | Code quality | Review, linting, formatting |
| Test Engineer | Тестирование | Unit tests, integration tests |
| Security Validator | Безопасность | Vulnerability scanning |
| Performance Expert | Оптимизация | Profiling, benchmarking |

### Специалисты

| Роль | Специализация | Типовые задачи |
|------|---------------|----------------|
| DevOps Engineer | Инфраструктура | CI/CD, Docker, K8s |
| UI/UX Designer | Дизайн | Wireframes, prototyping |
| Documentation Writer | Документация | API docs, guides |
| Accessibility Expert | a11y | WCAG compliance |

## 📝 Примеры реализаций

### Пример 1: React Component Developer

```markdown
# Ты — React Component Developer

## Твоя задача
Разрабатывать React компоненты в соответствии с дизайном.

## Твой стек (STRICT)
- React 18+ с hooks
- TypeScript strict mode
- Tailwind CSS для стилей
- Zustand для state (если нужен)

## Правила разработки
1. Каждый компонент в отдельном файле
2. Пропсы типизированы через interface
3. Используй forwardRef если нужен ref
4. События命名: onAction (onSubmit, onClick)
5. Children проп если есть слот

## Структура компонента
```tsx
// 1. Imports (React + dependencies)
// 2. Types/Interfaces
// 3. Helper functions
// 4. Component definition
// 5. Default export
```

## Чеклист перед завершением
- [ ] Типизированы все props
- [ ] Нет implicit any
- [ ] Использованы правильные types (FC не использовать)
- [ ] Key props где нужно
- [ ] Error boundary (если нужно)
- [ ] Документация через JSDoc

## Контрпримеры (НЕ ДЕЛАЙ)
❌ Вложенные тернарные операторы
❌ Копипаст кода > 2 раз → вынести в компонент
❌ Пропсы drilling глубже 3 уровней
❌ useState для derived state → useMemo
```

### Пример 2: API Endpoint Developer

```markdown
# Ты — API Endpoint Developer

## Твоя задача
Разрабатывать REST API endpoints в Express.js.

## Твой стек (STRICT)
- Node.js 18+
- Express.js
- TypeScript strict mode
- Zod для валидации
- Prisma ORM

## Правила разработки
1. Каждый endpoint — отдельная функция
2. Валидация входных данных через Zod
3. Статусы ответов по HTTP стандарту
4. Error handling единообразный
5. Логирование всех ошибок

## Шаблон endpoint
```typescript
export const createEndpoint = async (req: Request, res: Response) => {
  try {
    // 1. Валидация
    const data = createSchema.parse(req.body);
    
    // 2. Бизнес-логика
    const result = await service.create(data);
    
    // 3. Ответ
    res.status(201).json(result);
  } catch (error) {
    // 4. Обработка ошибок
    handleError(error, res);
  }
};
```

## Чеклист перед завершением
- [ ] Валидация входных данных
- [ ] Правильный HTTP статус
- [ ] Обработка всех ошибок
- [ ] Логирование
- [ ] JSDoc документация
- [ ] Типизация request/response

## Контрпримеры (НЕ ДЕЛАЙ)
❌ Валидация "на глазок" — использовать Zod
❌ Прямой доступ к БД — через service layer
❌ Silent failures — всегда логировать
❌ 500 на все ошибки — разные статусы
```

### Пример 3: Unit Test Writer

```markdown
# Ты — Unit Test Writer

## Твоя задача
Писать unit тесты с высокой coverage.

## Твой стек (STRICT)
- Jest или Vitest
- TypeScript
- Testing Library (React: @testing-library/react)

## Правила написания тестов
1. Arrange-Act-Assert pattern
2. Описательные имена тестов (describe what, not how)
3. Изолированные тесты (no dependencies between)
4. Mock только внешних зависимостей
5. Coverage target: 80%

## Шаблон теста
```typescript
describe('ComponentName', () => {
  describe('when [condition]', () => {
    it('should [expected behavior]', () => {
      // Arrange
      const props = { ... };
      
      // Act
      const result = render(<Component {...props} />);
      
      // Assert
      expect(result.getByText('...')).toBeInTheDocument();
    });
  });
});
```

## Чеклист перед завершением
- [ ] Каждый public function протестирован
- [ ] Edge cases покрыты
- [ ] Нет hardcoded values → use fixtures
- [ ] Тесты изолированы
- [ ] Clear failure messages

## Контрпримеры (НЕ ДЕЛАЙ)
❌ Тестирование приватных методов
❌ Implementation details → тестировать behavior
❌ Тесты зависят друг от друга
❌ Громоздкие setup → beforeEach helpers
```

## 🚀 Советы по созданию ролей

1. **Начните с простого**
   - Не усложняйте с первой роли
   - Добавляйте правила по мере необходимости

2. **Итерируйте**
   - Используйте роли → собирайте обратную связь
   - Обновляйте правила по результатам

3. **Документируйте**
   - Записывайте что работает, а что нет
   - Создавайте библиотеку шаблонов

4. **Тестируйте**
   - Проверяйте роли на реальных задачах
   - Сравнивайте результаты с/без ролей

---

## 🎯 Ролевая система + AI Best Practices

**Ролевая система и Spec-Driven Development — это "скелет" процесса. Но чтобы AI-инструменты (Cursor, Cline, Claude) работали как стабильная IDE, а не как генератор случайного кода, нужны практики работы с контекстом и итерациями.**

Подробнее: [📖 AI_BEST_PRACTICES.md](../AI_BEST_PRACTICES.md)

### Интеграция ролевой системы с Best Practices

#### 1. Управление Контекстом (Context Hygiene) + Роли

**Принцип:** Каждая роль должна работать только с релевантным контекстом.

```bash
# ❌ НЕПРАВИЛЬНО — Architect добавляет весь проект
ФАЗА 1: Architect
→ /add backend/
→ /add frontend/
→ /add docs/
→ /add node_modules/

# ✅ ПРАВИЛЬНО — Architect добавляет только спецификацию
ФАЗА 1: Architect
→ /add docs/TECH_STACK.md
→ /add docs/REQUIREMENTS.md
→ @symbol systemArchitecture
```

**Правило для ролей:**
```markdown
## Context Management (STRICT)

Architect:
- Добавлять только: TECH_STACK.md, REQUIREMENTS.md, существующий план
- НЕ добавлять: реализацию, тесты, конфиги

Executor:
- Добавлять только: файлы из текущей задачи (2-3 файла)
- НЕ добавлять: весь проект, node_modules, build/

Validator:
- Добавлять только: изменённые файлы + PLAN.md
- НЕ добавлять: весь проект
```

#### 2. Вертикальные Срезы (Vertical Slicing) + Роли

**Принцип:** Каждая роль создаёт срез, а не абстракцию.

```
Традиционный подход:
Architect: "Создай архитектуру всей системы"
Executor: Реализует 50 файлов за раз
Validator: Проверяет 50 файлов
→ Проблема: невозможно быстро найти ошибку

Ролевой подход + Vertical Slicing:
Architect: "Создай архитектуру для авторизации"
Executor: Реализует login API + UI
Validator: Проверяет только авторизацию
→ Преимущество: быстрая итерация, ошибки видны сразу
```

**Правило для ролей:**
```markdown
## Task Isolation (STRICT)

Architect:
- Разделяй задачи на независимые срезы
- Каждый срез: 2-3 компонента/функции
- Пример: "Авторизация" → login API, login UI, refresh tokens

Executor:
- Работай с одним срезом за раз
- Не переходи к следующему срезу пока текущий не протестирован
- Пример: Сначала login API, потом login UI

Validator:
- Проверяй один срез за раз
- ✅ OK → переходи к следующему
- ❌ Error → остановись и исправь
```

#### 3. AI-Driven TDD + Роли

**Принцип:** Каждая роль следует TDD подходу.

```
ФАЗА 1: Architect
→ Создаёт план с чеклистом тестов

ФАЗА 2: Executor
→ Пишет тесты СНАЧАЛА
→ Потом реализацию

ФАЗА 3: Validator
→ Запускает тесты (npm test)
→ При ошибке → возврат к Фазе 2
```

**Правило для ролей:**
```markdown
## TDD Approach (STRICT)

Architect:
- Включай в план: "Требуемые тесты"
- Пример: "Unit tests for auth service, Integration tests for login API"

Executor:
- Шаг 1: Напиши тесты (если есть в плане)
- Шаг 2: Запусти тесты (npm test)
- Шаг 3: Пиши реализацию
- Шаг 4: Запусти тесты снова
- Self-healing: если тесты упали → исправь код автоматически

Validator:
- Запускать npm test обязательно
- Не принимать код без проходящих тестов
```

#### 4. Резиновая Утка 2.0 (Rubber Ducking) + Роли

**Принцип:** Обсуждение идеи через роли.

```
❌ НЕПРАВИЛЬНО — Прямой запрос
"Напиши кэширование для API"

✅ ПРАВИЛЬНО — Обсуждение через Architect
ФАЗА 1: Architect
"У меня задача кэширования. Вот мой план:
1. Redis для хранения
2. Middleware для кэширования
3. TTL 5 минут

Найди проблемы в плане или предложи лучший паттерн."

→ Architect находит архитектурные ошибки ДО написания кода

ФАЗА 2: Executor
Реализует по исправленному плану
```

**Правило для ролей:**
```markdown
## Rubber Ducking Mode (STRICT)

Architect:
- Обсуждай план ПЕРЕД написанием кода
- Находи архитектурные ошибки в планах пользователя
- Предлагай улучшения паттернов

Executor:
- НЕ "додумывай" архитектуру
- СЛЕДУЙ плану от Architect
- При неясности → попроси уточнение через Architect

Validator:
- Проверяй соответствие плану, а не "как я бы сделал"
```

#### 5. Diff-Driven Workflow + Роли

**Принцип:** Постепенное применение изменений каждой роли.

```
❌ НЕПРАВИЛЬНО — Apply All
Architect создаёт план на 50 файлов
Executor меняет 50 файлов
→ Apply All
→ Всё сломано

✅ ПРАВИЛЬНО — Постепенное применение
Architect: создаёт план на авторизацию
Executor: меняет login API (1 файл)
→ Apply → Test → OK
Executor: меняет login UI (1 файл)
→ Apply → Test → OK
Validator: проверяет весь срез
→ OK → Commit
```

**Правило для ролей:**
```markdown
## Diff-Driven Workflow (STRICT)

Architect:
- Разделяй план на части по 2-3 файла
- Каждая часть должна быть независимой

Executor:
- Применяй изменения по 2-3 файла за раз
- После каждой части: save → test → только затем продолжай
- При ошибке: Ctrl+Z → переформулируй запрос

Validator:
- Проверяй каждую часть отдельно
- Не ждать пока все файлы изменены
```

#### 6. Аннотации и Якоря (Anchoring) + Роли

**Принцип:** TODO-комментарии связывают код с планом.

```typescript
// В PLAN.md от Architect:
## Реализация авторизации
- [ ] JWT tokens
- [ ] Refresh tokens
- [ ] Rate limiting

// В коде от Executor:
// TODO: Implement JWT tokens as per PLAN.md-AUTH-01
export const authService = {
  generateToken: () => { /* ... */ }
};

// TODO: Implement refresh tokens as per PLAN.md-AUTH-02
export const refreshService = {
  refresh: () => { /* ... */ }
};
```

**Правило для ролей:**
```markdown
## Anchoring System (STRICT)

Architect:
- Добавляй TODO-комментарии в план
- Пример: "TODO: [описание] as per PLAN-ID"

Executor:
- Ищи TODO-комментарии в плане
- Реализуй по очереди
- Не удаляй TODO пока не завершено

Validator:
- Проверяй что все TODO реализованы
- Не принимай код с незавершёнными TODO
```

#### 7. Строгая типизация (Type-Safety as Prompt) + Роли

**Принцип:** Архитектор задаёт типы, Executor реализует.

```typescript
// ФАЗА 1: Architect — задаёт типы
interface ApiResponse<T> {
  data: T[];
  meta: {
    total: number;
    page: number;
  };
}

// ФАЗА 2: Executor — реализует по типам
export const fetchData = async <T>(url: string): Promise<ApiResponse<T>> => {
  // Архитектор уже задал сигнатуру
  // Executor только заполняет тело
};
```

**Правило для ролей:**
```markdown
## Type-Safety as Prompt (STRICT)

Architect:
- СНАЧАЛА задавай interfaces/types
- Потом описывай реализацию
- Пример: "Вот interface ApiResponse. Реализуй функцию."

Executor:
- НЕ "додумывай" типы
- Используй точные типы от Architect
- TypeScript strict mode обязателен

Validator:
- Проверяй что типы от Architect соблюдены
- Отклоняй если есть any или типы не соответствуют API
```

### Профиль "Супер-юзера": Роли + Best Practices

Идеальный workflow объединяет ролевую систему и Best Practices:

```
1. ПОДГОТОВКА
   .cursorrules настроены
   .cursorignore создан
   SPEC.md准备好了

2. ПЛАНИРОВАНИЕ (Architect + Rubber Ducking)
   Пользователь объясняет идею через Architect
   Architect обсуждает и находит проблемы
   Architect создаёт план с TODO-комментариями

3. ДЕКОМПОЗИЦИЯ (Architect + Vertical Slicing)
   Architect разбивает задачу на срезы
   Каждый срез: 2-3 файла, независим

4. TDD (Executor + TDD)
   Executor пишет тесты СНАЧАЛА
   Запускает npm test
   Потом реализацию
   Self-healing если тесты упали

5. РЕАЛИЗАЦИЯ (Executor + Context Hygiene)
   Executor добавляет только нужные файлы (/add)
   Работает с одним срезом за раз
   Следует TODO-комментариям

6. ВЕРИФИКАЦИЯ (Validator + Diff-Driven)
   Validator проверяет один срез
   Постепенно применяет изменения
   Запускает тесты
   ✅ OK → Commit

7. ЦИКЛ
   Повтор для следующего среза
   Постепенное наращивание функционала
```

### Ключевые преимущества комбинации

| Аспект | Ролевая система | Best Practices | Комбинация |
|---------|------------------|-------------------|----------------|
| Контекст | Роли имеют фокус | @Symbol, /add | Идеальная изоляция |
| Задачи | Разделение ответственности | Вертикальные срезы | Быстрая итерация |
| Качество | Validator проверяет | TDD, типизация | Multi-layer защита |
| Ошибки | Rabbit Hole Detection | Rubber Ducking | Предотвращение заранее |
| Применение | Последовательные фазы | Diff-Driven | Безопасные изменения |

> **Результат:** AI превращается из "генератора случайного кода" в "интеллектуальный компилятор ваших идей" с архитекторским контролем качества.

---

## 📚 Дополнительные ресурсы

### Специализированные руководства

- [📋 YAML Role Configurations](./yaml-role-configurations.md) — Создание YAML конфигураций для ролей
- [🔍 Chrome MCP Integration](./chrome-mcp-integration.md) — Использование Chrome MCP для E2E тестирования
- [📖 Pattern Library](./pattern-library.md) — Библиотека паттернов разработки

### Основные концепции

- [Role-Based Development](#) — Этот документ
- [AI Best Practices](../AI_BEST_PRACTICES.md) — Best practices для работы с AI
- [Protocol Examples](../claude-rules/) — Примеры протоколов для разных доменов

---

**Следующая тема:** [Библиотека паттернов](./pattern-library.md)
