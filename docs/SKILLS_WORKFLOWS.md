# 🔄 Workflows в AIRules Skills

Workflows — это явные последовательности задач, которые вы вызываете специальной командой (например, `/workflow.md`). Отличаются от Skills тем, что Workflows:

- **Skills:** Активируются автоматически по описанию запроса
- **Workflows:** Вызываются явным образом командой

---

## 🎯 Зачем нужны Workflows?

Workflows решают задачи, которые:
- Требуют многошагового процесса
- Имеют чёткую последовательность действий
- Повторяются (релизы, деплои, code review)

### Примеры использования

1. **Релиз проекта** — version bump, changelog, тесты, git tag, публикация
2. **Деплой** — build, upload, verify
3. **Code Review** — lint, type-check, тесты, review checklist

---

## 🏗 Структура workflow файла

Workflows хранятся в `.cline/workflows/*.md`:

```markdown
---
name: workflow-name
description: Краткое описание.
steps:
  - step-1
  - step-2
  - step-3
```

### Обязательные поля

- `name` — Имя workflow
- `description` — Краткое описание (max 1024 символа)
- `steps` — Список шагов в формате YAML списка

---

## 📄 Создание workflow файла

### Шаг 1: Создать файл

```bash
touch .cline/workflows/my-workflow.md
```

### Шаг 2: Добавить frontmatter

```markdown
---
name: my-workflow
description: Краткое описание того, что делает этот workflow.
---

# Название Workflow

## Описание workflow

Детальное описание процесса.

## Шаги

### Шаг 1: Название

Описание первого шага.

### Шаг 2: Название

Описание второго шага.

### Шаг 3: Название

Описание третьего шага.

---
```

---

## 🎯 Примеры Workflows

### Пример 1: Release Workflow

Создайте `.cline/workflows/release.md`:

```markdown
---
name: release
description: Выполнить полный релиз проекта.
steps:
  - version-bump
  - changelog
  - tests
  - git-tag
  - publish
---

# Release Workflow

Этот workflow выполняет полный релиз проекта.

## Шаги

### Шаг 1: Version Bump

Увеличить версию в `package.json`:

```bash
npm version patch  # 1.2.3 → 1.2.4
```

### Шаг 2: Changelog

Добавить запись в `CHANGELOG.md`:

```markdown
## [1.2.4] - 2026-01-11

### Добавлено
- Новая функция X
- Исправлен баг Y

### Изменено
- Обновлён API Z
```

### Шаг 3: Tests

Запустить тесты:

```bash
npm test
```

### Шаг 4: Git Tag

Создать git tag:

```bash
git tag -a v1.2.4 -m "Release v1.2.4"
git push origin v1.2.4
```

### Шаг 5: Publish

Опубликовать в npm (если применимо):

```bash
npm publish
```

---

**Вызов:**
```bash
/workflow release
```

---

### Пример 2: Deployment Workflow

Создайте `.cline/workflows/deploy.md`:

```markdown
---
name: deploy
description: Задеплоить проект на Vercel.
steps:
  - build
  - vercel-deploy
  - verify
---

# Deployment Workflow

Этот workflow деплоит проект на production.

## Шаги

### Шаг 1: Build

Собрать проект:

```bash
npm run build
```

### Шаг 2: Vercel Deploy

Задеплоить на Vercel:

```bash
vercel --prod
```

### Шаг 3: Verify

Проверить деплой:

```bash
curl https://my-project.vercel.app
```

---

**Вызов:**
```bash
/workflow deploy
```

---

### Пример 3: Code Review Workflow

Создайте `.cline/workflows/code-review.md`:

```markdown
---
name: code-review
description: Провести систематический code review.
steps:
  - lint
  - type-check
  - tests
  - review-checklist
---

# Code Review Workflow

Этот workflow проводит систематический review изменений.

## Шаги

### Шаг 1: Lint

Проверить код на соответствие style guide:

```bash
npm run lint
```

### Шаг 2: Type Check

Проверить TypeScript типы:

```bash
npm run type-check
```

### Шаг 3: Tests

Запустить тесты:

```bash
npm test
```

### Шаг 4: Review Checklist

Проверить:
- [ ] Код соответствует style guide
- [ ] Нет `any` типов
- [ ] Тестовое покрытие >80%
- [ ] Документация обновлена

---

**Вызов:**
```bash
/workflow code-review
```

---

## 🔄 Интеграция с Skills

### В Skill

Skill может вызывать workflows из своих инструкций:

```markdown
## Интеграция с Workflows

Для выполнения code review:
1. Запустите `/workflow code-review`
2. Ожидайте выполнения всех шагов
3. Проверьте результаты

**Примечание:** Skill может использовать результаты workflow для финальной валидации.
```

### Multi-Agent

При многоагентной координации workflows могут выполнять несколько агентов:

```markdown
## Кросс-агентные workflows

**Phase 1 (Architect):** Создание плана релиза
**Phase 2 (Executor):** Реализация изменений
**Phase 3 (Validator):** Проверка качества
```

---

## 📊 Метрики успеха

Workflow успешен когда:
- [x] Все шаги выполнены последовательно
- [x] Каждый шаг имеет чёткие критерии завершения
- [x] Ошибки обрабатываются корректно
- [x] Progress виден пользователю

---

## ⚠️ Лучшие практики

### 1. Чёткие шаги

✅ **Делайте:**
- Разбейте сложную задачу на атомарные шаги
- Каждый шаг должен иметь чёткий критерий завершения
- Указывайте команды и ожидаемые результаты

❌ **НЕ делайте:**
- Описывайте процесс абстрактно
- Создавайте слишком длинные шаги
- Пропускайте критерии завершения

### 2. Обработка ошибок

✅ **Делайте:**
- Останавливайтесь при критической ошибке
- Предлагайте решения
- Логируйте все действия

❌ **НЕ делайте:**
- Игнорируйте ошибки
- Продолжайте при отказе
- Не логируйте действия

### 3. Прогресс

✅ **Делайте:**
- Отображайте текущий шаг пользователю
- Показывайте процент завершения
- Позволяйте прервать workflow

---

## 🚀 Быстрый старт

### Создать первый workflow:

```bash
mkdir -p .cline/workflows
touch .cline/workflows/first-workflow.md
```

### Редактировать:

```bash
# Добавить frontmatter и шаги
```

### Вызвать:

```bash
/workflow first-workflow
```

---

**Версия:** 1.0.0  
**Последнее обновление:** 2026-01-11  
**Связано с:** Skills Feature в Cline
