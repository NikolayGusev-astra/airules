# 📚 Примеры использования Skills в AIRules

Этот документ содержит практические примеры использования различных Skills из `.cline/skills/` в контексте AIRules.

---

## 📋 Таблица Skills

| Skill | Когда использовать | Ключевые возможности |
|--------|-----------------|---------------------|
| **documentation-helper** | Работа с README, форматирование, обновление структуры | Поиск, форматирование, создание документации |
| **ontology-expert** | Анализ зависимостей, валидация графов | analyze-dependencies.js, validate-graph.js |
| **context7-researcher** | Новые библиотеки, проверки API | Поиск актуальной документации |
| **memory-graph-expert** | Хранение знаний о проекте, поиск типов | Работа с MCP Memory Graph |
| **rag-expert** | Поиск контекста, синтез информации | Векторный поиск, семантический поиск |
| **multi-agent-workflow** | Релизы, деплои, координация | Автоматизация процессов |
| **mcp-tools-expert** | Настройка MCP, интеграция серверов | Проверка совместимости |
| **librarian** | Валидация документации, упорядочивание, аудит ссылок, исправление стиля | Diátaxis framework, Docs-as-Code |

---

## 🎯 Пример 1: Documentation Helper

### Сценарий: Обновление README.md

**Запрос:**
> Обнови README.md с информацией о новых Skills

**Активация:**
Cline автоматически определит запрос как связанный с документацией и загрузит `documentation-helper`.

**Выполнение:**
1. **Поиск разделов** — найди разделы для обновления
2. **Добавление нового раздела** — добавь информацию о Skills
3. **Проверка форматирования** — используй markdown формат
4. **Проверка согласованности** — ссылки на соответствующие файлы

**Результат:**
```markdown
## Skills

AIRules поддерживает гибридную модель с Skills, которые загружаются по требованию.

Доступные Skills:
- documentation-helper — Помощник по документации
- ontology-expert — Эксперт по онтологическим инструментам
- ...
```

---

## 🧠 Пример 2: Ontology Expert

### Сценарий: Анализ зависимостей проекта

**Запрос:**
> Проверь структуру проекта на циклические зависимости

**Активация:**
Cline загрузит `ontology-expert` для анализа структуры проекта.

**Выполнение:**
1. **Запуск analyze-dependencies.js**
```bash
node scripts/ontology/analyze-dependencies.js
```

2. **Анализ результатов:**
```json
{
  "totalNodes": 45,
  "totalEdges": 120,
  "cyclicDependencies": 0,
  "unusedExports": 5
}
```

3. **Валидация графа:**
```bash
node scripts/ontology/validate-graph.js
```

4. **Создание отчета:**
```markdown
## Dependency Analysis Report

✅ Cyclic dependencies: 0
⚠️ Unused exports: 5
📊 Total nodes: 45
```

---

## 🔗 Пример 3: Context7 Researcher

### Сценарий: Интеграция новой библиотеки

**Запрос:**
> Добавь JWT аутентификацию в Express.js. use context7

**Активация:**
Cline загрузит `context7-researcher` для поиска актуальной документации.

**Выполнение:**
1. **resolve-library-id** — найди библиотеку
```javascript
await use_mcp_tool("resolve-library-id", {
  query: "JWT authentication for Express.js",
  libraryName: "express-jwt"
});
```

2. **query-docs** — получи актуальную документацию
```javascript
await use_mcp_tool("query-docs", {
  libraryId: "/auth0/express-jwt",
  query: "How to implement JWT authentication middleware"
});
```

3. **Синтез ответа** — на основе найденной документации

**Результат:**
```typescript
import jwt from 'express-jwt';

export const authenticate = (req, res, next) => {
  const token = req.headers.authorization?.split(' ')[1];
  
  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    req.user = decoded;
    next();
  } catch (error) {
    res.status(401).json({ error: 'Invalid token' });
  }
};
```

---

## 📊 Пример 4: Memory Graph Expert

### Сценарий: Поиск типов в проекте

**Запрос:**
> Найди все типы связанные с Transaction

**Активация:**
Cline загрузит `memory-graph-expert` для работы с MCP Memory Graph.

**Выполнение:**
1. **search_nodes** — найди сущности
```javascript
await use_mcp_tool("memory_graph", "search_nodes", {
  query: "Transaction"
});
```

2. **open_nodes** — получи детальную информацию
```javascript
await use_mcp_tool("memory_graph", "open_nodes", {
  names: ["Type_Transaction", "TransactionService", "AccountBalance"]
});
```

3. **Анализ результатов:**
```json
{
  "Type_Transaction": {
    "observations": [
      "Defined in: src/types/Transaction.ts",
      "Used in: TransactionService, AccountBalance"
    ]
  }
}
```

---

## 🔍 Пример 5: RAG Expert

### Сценарий: Поиск в документации AIRules

**Запрос:**
> Найди информацию о Skills в документации AIRules

**Активация:**
Cline загрузит `rag-expert` для поиска релевантного контекста.

**Выполнение:**
1. **Сканирование документов** в директории `docs/`
2. **Создание векторных представлений** для найденных файлов
3. **Vector search** для поиска релевантных фрагментов
4. **Reranking** по релевантности (score, recency)
5. **Синтез ответа** — на основе top-3 фрагментов

**Результат:**
```markdown
# Skills в AIRules

Skills — это специализированные роли, которые загружаются по требованию.

Доступные Skills:
1. **documentation-helper** — работа с документацией
2. **ontology-expert** — онтологические инструменты
3. **context7-researcher** — поиск документации
4. **memory-graph-expert** — работа с Memory Graph
5. **rag-expert** — RAG подходы
6. **multi-agent-workflow** — автоматизация процессов
7. **mcp-tools-expert** — MCP интеграция

Интеграция:
- Skills работают ВМЕСТЕ с базовыми правилами из .clinerules/
- Автоматически активируются при соответствующем запросе
```

---

## 🔄 Пример 6: Multi-Agent Workflow

### Сценарий: Автоматизация релиза

**Запрос:**
> Автоматизируй процесс релиза новой версии проекта

**Активация:**
Cline загрузит `multi-agent-workflow` для координации нескольких фаз.

**Выполнение:**
```
ФАЗА 1 (Architect):
├─ Создать план релиза (docs/RELEASE_PLAN.md)
├─ Определить чеклист проверок
└─ Указать порядок действий

ФАЗА 2 (Executor):
├─ Обновить версию в package.json
├─ Создать CHANGELOG.md
├─ Обновить README.md
└─ Создать git tag

ФАЗА 3 (Validator):
├─ Проверить все изменения
├─ Проверить тесты
├─ Валидировать документацию
└─ Подтвердить готовность к релизу

ФАЗА 4 (Multi-Agent Workflow):
├─ Координация всех фаз
├─ Проверка синергии
└─ Автоматизация git commit
```

**Результат:**
```bash
git tag v1.2.0
git push origin v1.2.0
npm publish
```

---

## 🔧 Пример 7: MCP Tools Expert

### Сценарий: Настройка нового MCP сервера

**Запрос:**
> Настрой MCP сервер для работы с файловой системой

**Активация:**
Cline загрузит `mcp-tools-expert` для настройки MCP интеграции.

**Выполнение:**
1. **Проверка доступности MCP сервера**
```javascript
// Проверка filesystem MCP
await use_mcp_tool("filesystem", "read_file", {
  path: "path/to/file"
});
```

2. **Валидация совместимости**
- Проверь версию MCP протокола
- Проверь совместимость с другими серверами
- Проверь настройки в `mcp/setup/`

3. **Создание конфигурации**
```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem"]
    }
  }
}
```

**Результат:**
```json
{
  "status": "configured",
  "servers": ["filesystem", "git", "fetch"],
  "compatible": true
}
```

---

## 📚 Пример 8: Librarian

### Сценарий: Подготовка к релизу документации

**Запрос:**
> Подготовь документацию проекта к релизу - проверь качество, упорядочи структуру, проведи аудит ссылок, исправь стиль

**Активация:**
Cline автоматически определит запрос как связанный с документацией и загрузит `librarian`.

**Выполнение:**
1. **Валидация документации** (validate-librarian.sh)
```bash
.cline/skills/librarian/scripts/validate-librarian.sh
```
Проверяет:
- Соответствие стандартам Diátaxis
- Наличие необходимых разделов
- Структура заголовков
- Перекрёстные ссылки

2. **Упорядочивание структуры** (order-docs.sh)
```bash
.cline/skills/librarian/scripts/order-docs.sh
```
Создаёт структуру по жанрам Diátaxis:
```
docs/
├── tutorials/       # Обучающие материалы
├── how-to-guides/  # Практические руководства
├── reference/       # Справочные материалы
└── explanations/  # Объяснения концепций
```

3. **Аудит ссылок** (audit-links.sh)
```bash
.cline/skills/librarian/scripts/audit-links.sh --quick
```
Проверяет:
- Битые внутренние ссылки
- Недоступные внешние ссылки
- Орфаные документы без входящих ссылок

4. **Исправление стиля** (fix-style.sh)
```bash
.cline/skills/librarian/scripts/fix-style.sh
```
Исправляет:
- Пассивный залог → активный залог
- Длинные предложения → короткие
- Жаргон → простые термины

5. **Создание отчёта**
```bash
.cline/skills/librarian/scripts/validate-librarian.sh --report release-audit.md
```

**Результат:**
```markdown
## 📊 Отчёт о качестве документации

### Валидация
✅ Документация соответствует стандартам Diátaxis

### Упорядочивание
✅ Структура создана по жанрам

### Аудит ссылок
✅ Все внутренние ссылки корректны
⚠️ 2 внешние ссылки требуют проверки

### Стиль
✅ Активный залог применён
✅ Длинные предложения сокращены

### Итог
✅ Документация готова к релизу

**Скрипты использованы:**
- validate-librarian.sh
- order-docs.sh
- audit-links.sh
- fix-style.sh
```

**Дальнейшие действия:**
1. Проверьте отчёт (release-audit.md)
2. Исправьте отмеченные проблемы
3. Запустите финальную валидацию
```

---

## 🎯 Комбинирование Skills

### Пример: Интеграция нескольких Skills

**Запрос:**
> Создай документацию для новой библиотеки, проверь совместимость и упорядочи структуру

**Активация:**
Cline автоматически загрузит три Skills: `documentation-helper`, `librarian`, и `context7-researcher`.

**Выполнение:**

**documentation-helper:**
1. Создать структуру документации
2. Форматировать в markdown

**librarian:**
1. Проверить соответствие стандартам Diátaxis
2. Упорядочить по жанрам

**context7-researcher:**
1. Найти актуальную документацию библиотеки
2. Проверить совместимость версий

**Синтез:**
```markdown
# Library Documentation

## Overview
Описание библиотеки (создано documentation-helper + librarian)

## Installation
``npm install library-name
```

## Usage
```typescript
// Актуальный пример из context7-researcher + librarian
import { Something } from 'library-name';

const instance = new Something();
instance.doSomething();
```

## Compatibility
- ✅ Node.js 18+ (проверено через context7-researcher)
- ✅ TypeScript 5.0+
- ✅ Документация соответствует Diátaxis (проверено через librarian)

## Style Notes
- Используйте активный залог
- Короткие предложения
- Избегайте жаргона
```

---

## 📊 Метрики использования Skills

### Когда каждый Skill наиболее полезен

| Skill | Чаще всего используется для | Типичные задачи |
|--------|---------------------------|----------------|
| documentation-helper | 40% | README, обновление структуры |
| context7-researcher | 25% | Новые библиотеки, API |
| ontology-expert | 15% | Анализ зависимостей |
| rag-expert | 10% | Поиск контекста |
| memory-graph-expert | 5% | Хранение знаний |
| multi-agent-workflow | 3% | Релизы, деплои |
| mcp-tools-expert | 2% | Настройка MCP |
| **librarian** | 20% | Валидация документации, упорядочивание, аудит ссылок |

---

## ✅ Чеклист использования Skills

Перед использованием Skill:

- [ ] Определить, какой Skill нужен для задачи
- [ ] Проверить, что запрос соответствует описанию Skill
- [ ] Дать достаточно контекста для работы Skill
- [ ] Проверить, что базовые правила не противоречат Skill

Во время работы с Skill:

- [ ] Следовать инструкциям из активного SKILL.md
- [ ] Помнить о базовых правилах из auto-protocol.md
- [ ] Приоритизировать инструкции Skill над базовыми правилами
- [ ] Использовать MCP инструменты по необходимости

После работы с Skill:

- [ ] Проверить качество результата
- [ ] Обновить документацию, если нужно
- [ ] Создать отчет, если требуется
- [ ] Обновить Memory Graph, если использовался

---

## 🔗 Дополнительные ресурсы

- [📄 Skills Feature](SKILLS_FEATURE.md) — Описание функционала Skills
- [🔄 Migration Guide](SKILLS_MIGRATION_GUIDE.md) — Переход с .clinerules на Skills
- [📊 Migration Summary](SKILLS_MIGRATION_SUMMARY.md) — Резюме изменений
- [🤖 Auto-protocol](../.clinerules/auto-protocol.md) — Основной протокол автоматизации
- [📚 Librarian Usage](LIBRARIAN_USAGE.md) — Руководство по использованию Librarian