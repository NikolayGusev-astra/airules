# 🤖 Model Context Protocol (MCP) Integration Guide

**AIRules** полностью интегрирован с Model Context Protocol (MCP) серверами для расширения возможностей AI-разработки.

---

## 📋 Содержание

- [Что такое MCP](#что-такое-mcp)
- [Быстрый старт](#быстрый-старт)
- [Доступные серверы](#доступные-серверы)
- [Настройка прокси](#настройка-прокси)
- [Тестирование](#тестирование)
- [Устранение неисправностей](#устранение-неисправностей)
- [Конфигурация](#конфигурация)

---

## 🎯 Что такое MCP?

**MCP (Model Context Protocol)** - это универсальный стандарт для интеграции AI моделей с внешними инструментами и сервисами.

### Проблема
AI модели работают в изолированном контексте и не могут:
- ❌ Открывать браузер и тестировать веб-приложения
- ❌ Подключаться к базам данных
- ❌ Использовать внешние API
- ❌ Работать с файлами и инструментами

### Решение
MCP предоставляет **стандартизированный интерфейс** для подключения AI к внешним инструментам через **MCP серверы**.

```
┌─────────────┐    ┌──────────────┐    ┌────────────────┐
│   AI Agent  │◄──►│ MCP Protocol │◄──►│ MCP Server     │
│             │    │              │    │ (Chrome, Git,  │
│ (Claude,    │    │ (JSON-RPC)   │    │  Figma, etc.)  │
│  Cursor)    │    │              │    └────────────────┘
└─────────────┘    └──────────────┘           │
                                             ▼
                                   ┌─────────────────────┐
                                   │ External Tools &    │
                                   │ Services            │
                                   └─────────────────────┘
```

---

## 🚀 Быстрый старт

### 1. Автоматическая установка
```bash
cd /home/astralinux.ru/ngusev/Документы/airules

# Установка всех MCP серверов
./setup-mcp.sh

# Или через npm
npm run setup
```

### 2. Настройка переменных окружения
```bash
# Копируем шаблон
cp .env.example .env

# Редактируем ключи API
nano .env

# Основные переменные:
GITHUB_TOKEN=your_github_token_here
MAGIC_API_KEY=your_21st_magic_key_here
DATABASE_URL=postgresql://user:pass@localhost:5432/db
```

### 3. Тестирование
```bash
# Полное тестирование
npm run test

# Перезапуск Cursor IDE
```

---

## 📦 Доступные серверы

### ✅ Полностью рабочие серверы

| Сервер | Назначение | Статус | Особенности |
|--------|------------|--------|-------------|
| **Context7** | Документация и проверка галлюцинаций | ✅ Работает | API документации, code examples |
| **Chrome DevTools** | Браузерная автоматизация | ✅ Работает | Скриншоты, тестирование UI |
| **21st Magic** | Генерация UI компонентов | ✅ Работает | React/Vue компоненты, логотипы |
| **Filesystem** | Работа с файлами | ✅ Работает | CRUD операции с файлами |
| **Memory** | Граф знаний | ✅ Работает | Персистентная память AI |
| **Sequential Thinking** | Структурированное мышление | ✅ Работает | Планирование задач |

### ⚠️ Серверы с дополнительной настройкой

| Сервер | Проблема | Решение |
|--------|----------|---------|
| **Git** | npm токен истек | `npm login` |
| **Time** | npm токен истек | `npm login` |
| **Fetch** | npm токен истек | `npm login` |
| **GitHub** | Нет токена | `GITHUB_TOKEN` в .env |
| **PostgreSQL** | Нет подключения | `DATABASE_URL` в .env |

---

## 🌐 Настройка SOCKS5 прокси

Для решения проблем с таймаутами поддерживается SOCKS5 прокси на `127.0.0.1:2080`.

### Автоматическая настройка
Прокси настройки автоматически добавляются скриптом `setup-mcp.sh` для серверов:
- Context7
- GitHub
- 21st Magic
- PostgreSQL
- Perplexity

### Ручная настройка
Если прокси на другом порту, добавьте в `.env`:
```bash
# SOCKS5 прокси настройки
HTTP_PROXY=socks5://127.0.0.1:2080
HTTPS_PROXY=socks5://127.0.0.1:2080
ALL_PROXY=socks5://127.0.0.1:2080
```

### Тестирование прокси
```bash
# Проверка доступности
curl --socks5 127.0.0.1:2080 https://httpbin.org/ip

# Тестирование с прокси
npm run test
```

---

## 🧪 Тестирование

### Полное тестирование
```bash
npm run test
```

**Текущие результаты:**
```
✅ Успешно: 6/9 серверов
- Context7, Chrome DevTools, 21st Magic
- Filesystem, Memory, Sequential Thinking
⚠️  Требуют настройки: Git, Time, Fetch
```

### Ручное тестирование в Cursor

#### Context7 (документация)
```
"Find real examples of JWT authentication in Express.js"
"Verify if React.useEffect cleanup works in React 18"
"What are current best practices for error handling in Node.js"
```

#### Chrome DevTools (браузер)
```
"Take a screenshot of the current page"
"Click on the login button"
"Fill the email field with test@example.com"
```

#### 21st Magic (UI генерация)
```
"Generate a React button component with hover effects"
"Create a login form with validation"
```

#### Filesystem (файлы)
```
"List all files in the current directory"
"Read the content of package.json"
```

#### Memory (знания)
```
"Create a new entity called 'User' with observations about authentication"
"Find all entities related to error handling"
```

---

## 🔧 Устранение неисправностей

### Проблема: "Сервер не отвечает" / Timeout

**Решения:**
1. **Проверьте интернет-соединение:** `ping google.com`
2. **Настройте SOCKS5 прокси:** `curl --socks5 127.0.0.1:2080 https://httpbin.org/ip`
3. **Обновите npm кэш:** `npm cache clean --force`
4. **Переустановите сервер:** `npx -y @modelcontextprotocol/server-[name]@latest`

### Проблема: "npm токен истек"

**Решение:**
```bash
npm login
# Введите учетные данные npm
```

### Проблема: "API ключ не найден"

**Решение:**
```bash
# Проверьте .env файл
cat .env | grep -E "(GITHUB_TOKEN|MAGIC_API_KEY)"

# Добавьте недостающие ключи
echo "GITHUB_TOKEN=your_token_here" >> .env
```

### Проблема: "Путь не найден" (Filesystem/Git)

**Решение:**
Проверьте MCP конфигурацию в `~/.cursor/mcp.json`:
```json
{
  "filesystem": {
    "env": {
      "ALLOWED_PATHS": "/home/astralinux.ru/ngusev/Документы/airules"
    }
  }
}
```

### Диагностика

#### 1. Проверка системных требований
```bash
# Node.js версия
node --version  # Должно быть >= 18.0.0

# npm версия
npm --version   # Должно быть >= 8.0.0

# npm токен
npm whoami      # Должен показать username
```

#### 2. Проверка конфигурации
```bash
# MCP конфигурация
cat ~/.cursor/mcp.json | jq . 2>/dev/null || cat ~/.cursor/mcp.json

# Переменные окружения
cat .env | head -5  # Не показывайте полные ключи!
```

#### 3. Логи Cursor
- Откройте: `Cursor → Help → Toggle Developer Tools → Console`
- Ищите ошибки MCP в логах

---

## ⚙️ Конфигурация

### Файлы конфигурации

#### `~/.cursor/mcp.json` - MCP серверы для Cursor
```json
{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "env": {
        "HTTP_PROXY": "socks5://127.0.0.1:2080",
        "HTTPS_PROXY": "socks5://127.0.0.1:2080"
      }
    }
  }
}
```

#### `.env` - Переменные окружения
```bash
# GitHub
GITHUB_TOKEN=your_github_token

# 21st Magic
MAGIC_API_KEY=your_magic_key

# Database
DATABASE_URL=postgresql://user:pass@localhost:5432/db

# SOCKS5 Proxy
HTTP_PROXY=socks5://127.0.0.1:2080
HTTPS_PROXY=socks5://127.0.0.1:2080
```

### Scripts

#### `setup-mcp.sh` - Установка MCP серверов
```bash
#!/bin/bash
# Полная установка всех MCP серверов для AIRules

# Проверяет Node.js, создает .env, устанавливает серверы
# Автоматически добавляет прокси настройки
```

#### `test-mcp.js` - Тестирование серверов
```javascript
// Тестирует все MCP серверы
// Показывает статус каждого сервера
// Работает с stdio серверами (Memory, Sequential Thinking)
```

---

## 📚 Дополнительные ресурсы

- [MCP Specification](https://modelcontextprotocol.io/specification/) - Официальная спецификация
- [MCP Servers Registry](https://registry.modelcontextprotocol.io/) - Реестр серверов
- [Context7 Documentation](https://context7.ai/) - Документация Context7
- [21st Magic](https://magic.21st.dev/) - Генерация UI компонентов

---

## 🎯 Эффективность MCP

| Тип задач | Без MCP | С MCP | Улучшение |
|-----------|---------|-------|-----------|
| **Поиск документации** | Ручной поиск | Context7 API | 10x быстрее |
| **Генерация UI** | Шаблоны | 21st Magic | Кастомные компоненты |
| **Тестирование** | Ручное | Chrome DevTools | Автоматизированное |
| **Анализ кода** | Статический | Filesystem + Git | Динамический |
| **Планирование** | Линейное | Sequential Thinking | Структурированное |

---

## 🚀 Следующие шаги

1. **Запустите установку:** `./setup-mcp.sh`
2. **Настройте API ключи:** Отредактируйте `.env`
3. **Протестируйте:** `npm run test`
4. **Используйте в разработке:** Начните с Context7 для поиска документации

---

**MCP превращает AI из чат-бота в полноценного разработчика с доступом к инструментам и сервисам! 🎉**