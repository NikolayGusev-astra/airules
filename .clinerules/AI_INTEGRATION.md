# 🤖 Интеграция ролевой модели в Cline

## 📖 Описание

Документация о том, как работает многоагентная система ролей из `.clinerules` и как её использовать с Cline AI.

---

## 🔄 Как работает ролевая модель

### Архитектура системы

```
.clinerules/
├── auto-protocol.md           # Главный протокол (оркестратор)
├── roles/                        # Определения ролей
│   ├── index.yaml              # Реестр ролей
│   ├── architect/
│   │   ├── instructions.md     # Инструкции архитектора
│   │   └── role.yaml
│   ├── backend-executor/
│   │   ├── instructions.md     # Инструкции разработчика
│   │   └── role.yaml
│   └── validator/
│       ├── instructions.md     # Инструкции валидатора
│       └── role.yaml
└── workflows/
    └── multi-agent-supervisor.md  # Рабочие процессы
```

### Процесс выполнения

Когда вы даёте задачу Cline, система работает в 3 фазы:

```yaml
ФАЗА 1: ARCHITECT (Планирование)
├── Читает: auto-protocol.md
├── Читает: roles/architect/instructions.md
├── Анализирует задачу
├── Создаёт план реализации
└── ПЕРЕХОДИТ к фазе 2

ФАЗА 2: EXECUTOR (Выполнение)
├── Читает: план из фазы 1
├── Читает: roles/backend-executor/instructions.md
├── Реализует код по плану
├── При ошибке фиксирует в PROJECT_STATE.md
└── ПЕРЕХОДИТ к фазе 3

ФАЗА 3: VALIDATOR (Проверка)
├── Проверяет код против плана
├── Ищет нарушения правил
├── При ошибке → жёсткий отказ
└── При успехе → фиксация (git commit)
```

---

## 🎯 Как это работает в Cline

### Промпт для Cline

Когда Cline запускается, он читает файлы из `.clinerules` и формирует контекстный промпт:

```markdown
# Ты находишься в Автономном Режиме Оркестрации.
# Ты — система из 3-х агентов.

## Система ролей
- Architect (Планирование)
- Backend Developer (Выполнение)
- Validator (Проверка)

## Механизм работы
1. Прочитай .clinerules/auto-protocol.md
2. Реализуй фазовый подход
3. Переходи между фазами автоматически
4. При ошибке фиксируй проблему
5. При успехе делай commit

## Правила поведения
- НЕ пиши "я переключился на роль"
- ПРЯМО ДЕЛАЙ проверку и выдавай результат
- НЕ спрашивай "хотите ли вы перейти"
- ВСЕГДА начинай с Фазы 1
```

### Пример использования

**Запрос пользователя:**
> Создай API endpoint для авторизации пользователей.

**Автоматический процесс Cline:**

**Шаг 1: ARCHITECT** (Автоматически)
```
Cline читает:
- .clinerules/auto-protocol.md (протокол)
- roles/architect/instructions.md (инструкции)

Cline создаёт план:
```
# Архитектура: Auth API

## Endpoints
- POST /api/auth/login
- POST /api/auth/register
- POST /api/auth/logout

## Stack
- Node.js, Express, JWT
- TypeScript strict mode
```

Cline завершает фазу 1 и переходит к фазе 2.
```

**Шаг 2: EXECUTOR** (Автоматически)
```
Cline читает:
- План из фазы 1
- roles/backend-executor/instructions.md

Cline пишет код:
```typescript
import { Router, Request, Response } from 'express';
import { z } from 'zod';

const loginSchema = z.object({
  email: z.string().email(),
  password: z.string().min(8),
});

export const createAuthEndpoints = (router: Router) => {
  router.post('/login', async (req: Request, res: Response) => {
    const data = loginSchema.parse(req.body);
    // ... реализация
  });
  
  return router;
};
```

Cline завершает фазу 2 и переходит к фазе 3.
```

**Шаг 3: VALIDATOR** (Автоматически)
```
Cline проверяет:
- Соответствие плану из фазы 1
- TypeScript strict mode
- Отсутствие `any` типов
- Валидация через Zod

Результат проверки:
```
✅ VALIDATION PASSED

Проверено:
- ✅ TypeScript: strict mode соблюдён
- ✅ API endpoints соответствуют плану
- ✅ Валидация через Zod
- ✅ Нет запрещённых библиотек

Задача выполнена корректно.
```

---

## 🔌 Комбинация с новыми шаблонами

### Вариант 1: Полная интеграция

Используйте шаблоны из `templates/` для расширения функциональности:

```
.clinerules/
├── auto-protocol.md           # Существующий протокол
└── roles/
    └── backend-executor/
        ├── instructions.md         # Существующие инструкции
        └── role.yaml

templates/
├── .cursorrules               # Для Cursor
└── docs/
    ├── INSTRUCTIONS.md         # Для Claude/Cline
    ├── TECH_STACK.md          # Метаданные стека
    └── PLAN.md                # Артефакты
```

**Интеграция с Cline:**

```markdown
# .clinerules/auto-protocol.md

Добавь в начало:
```markdown
## Механизм артефактов

Перед началом работы, проверь наличие артефактов:
1. Если есть `docs/PLAN.md` → прочитай и следуй ему
2. Если есть `docs/TECH_STACK.md` → проверь технологический стек
3. Если есть `docs/DEBUG_REPORT.md` → учти существующие проблемы

При работе EXECUTOR:
- Строго следуй docs/PLAN.md
- Используй только технологии из docs/TECH_STACK.md
```
```

### Вариант 2: Использование для конкретных задач

Для задач, требующих специальных знаний, создавайте временные файлы:

```bash
# Для задачи с Python
cp templates/docs/INSTRUCTIONS.md docs/PYTHON_TASK.md

# Редактируйте для специфики
# Добавьте Python-специфические правила
```

---

## 📋 Использование с Cline

### Сценарий 1: Стандартная разработка

```bash
# Cline автоматически читает .clinerules
# Никаких дополнительных действий не требуется

Запрос:
"Создай компонент Button с вариантами"

Результат:
# Cline выполняет все 3 фазы автоматически
# ARCHITECT → EXECUTOR → VALIDATOR
```

### Сценарий 2: Разработка с артефактами

```bash
# 1. Создаём план (вручную или через ARCHITECT)
touch docs/PLAN.md
# Добавляем план

# 2. EXECUTOR будет читать PLAN.md
# Cline автоматически использует артефакты

Запрос:
"Реализуй план из docs/PLAN.md"

Результат:
# EXECUTOR читает docs/PLAN.md и следует ему
```

### Сценарий 3: Использование готовых правил из AIRules

```bash
# 1. Создаём файл правил в проекте
cp airules/examples/react-expert.md docs/REACT_EXPERT.md

# 2. Добавляем контекст в Cline
# @docs/REACT_EXPERT.md

Запрос:
"@docs/REACT_EXPERT.md Создай форму регистрации пользователя"

Результат:
# Cline использует роль из react-expert.md
# Архитектор, разработчик, валидатор в одном файле
```

---

## 🚀 Быстрый старт с Cline

### Шаг 1: Скопируйте базовый протокол

```bash
# Если у вас ещё нет .clinerules
# Используйте ваш существующий или создайте новый
```

### Шаг 2: Адаптируйте под ваш стек

Редактируйте `.clinerules/auto-protocol.md`:

```markdown
## Стек проекта
- Frontend: [React/Vue/Svelte]
- Backend: [Node.js/Python/Go]
- Database: [PostgreSQL/MySQL/MongoDB]
```

### Шаг 3: Добавьте свои правила

В `.clinerules/roles/` добавьте свои специфические инструкции:

```markdown
# Для frontend роли
- CSS Framework (Tailwind/Bootstrap/etc)
- Component Library (MUI/Chakra/etc)

# Для backend роли
- API Style (REST/GraphQL/gRPC)
- ORM (Prisma/TypeORM/Sequelize)
```

---

## 🎨 Сравнение подходов

### .clinerules (Многоагентная модель)

**Преимущества:**
- ✅ Автоматический переход между фазами
- ✅ Жёсткая валидация после реализации
- ✅ Защита от зацикливания (Rabbit Hole)
- ✅ Git commit только после проверки

**Недостатки:**
- ❌ Зафиксированные роли
- ❌ Требует больше времени на полную реализацию

### AIRules (Библиотека шаблонов)

**Преимущества:**
- ✅ Гибкая настройка через templates
- ✅ Легкое создание новых правил
- ✅ Поддержка разных стеков
- ✅ Возможность использовать одну роль целиком

**Недостатки:**
- ❌ Нет автоматического переключения фаз
- ❌ Требует ручного управления ролями

### Гибридный подход

Для максимальной эффективности комбинируйте оба подхода:

```yaml
# Используйте .clinerules как базу
- Автоматическое переключение фаз
- Жёсткая валидация

# Расширяйте через AIRules
- Создавайте готовые роли для разных задач
- Используйте как контекст @role-file.md

Пример:
.clinerules/auto-protocol.md:
  - При старте: проверь docs/PROJECT_ROLES.md
  - Если задача про Python → @docs/PYTHON_EXPERT.md
  - Если задача про React → @docs/REACT_EXPERT.md
```

---

## 📚 Дополнительные материалы

### Связанные файлы
- [../README.md](../README.md) — Главная документация AIRules
- [auto-protocol.md](./auto-protocol.md) — Исходный протокол
- [roles/](./roles/) — Определения ролей

### Полезные паттерны

1. **Progressive Disclosure** — постепенно раскрывайте детали
2. **Few-Shot Learning** — показывайте примеры
3. **Chain of Thought** — явно демонстрируйте ход мысли

---

## 🔧 Оптимизация для Cline

### Контекстное окно

Cline имеет ограниченное контекстное окно (~100K токенов). Оптимизируйте правила:

```markdown
# Вместо длинного описания
❌ Ты должен сначала проанализировать задачу, затем понять требования, 
   затем определить архитектурные решения, после этого создать 
   детальный план реализации, который включает в себя 
   спецификацию всех компонентов с их зависимостями...

# Используй чёткие команды
✅ ФАЗА 1: Архитектор
   - Проектируй архитектуру
   - Создай TypeScript interfaces
   - Определи технологии

ФАЗА 2: Разработчик
   - Реализуй по плану
   - Используй TypeScript strict mode
   - Пиши тесты

ФАЗА 3: Валидатор
   - Проверь соответствие плану
   - Проверь типовую безопасность
   - Проверь безопасность
```

### Кэширование результатов

Используйте механизмы кэширования:

```markdown
# Сохраняй результаты фаз
После ФАЗЫ 1 → сохрани план в docs/TEMP_PLAN.md
После ФАЗЫ 2 → сохрани имплементацию в docs/TEMP_IMPL.md
После ФАЗЫ 3 → удали временные файлы

Это позволяет:
- Ссылаться на предыдущие фазы
- Не повторять контекст
- Оптимизировать токены
```

---

## 💡 Практические примеры

### Как auto-protocol.md работает в Cline

Когда Cline запускается, он автоматически формирует промпт из файлов `.clinerules/`. Давайте рассмотрим как это работает на практике.

#### Пример 1: Создание API endpoint

**Запрос пользователя:**
> Создай endpoint для регистрации пользователей

**Что делает Cline автоматически:**

Cline читает файлы и формирует такой промпт:

```markdown
# Ты находишься в Автономном Режиме Оркестрации. Ты — не просто чат-бот, а система из 3-х агентов.

ФАЗА 1: ARCHITECT (Планирование)
Действуй как Старший Архитектор.

Синхронизация:
Выполни: git -C homeaccounting status (для проверки состояния)
Прочитай: PROJECT_STATE.md
Прочитай: SYSTEM_INSTRUCTION.md (раздел Стек и Правила).
Прочитай: docs/IMPLEMENTATION_PLAN.md.
Анализ:
Сравни задачу с текущей фазой из PROJECT_STATE.md.
Проверь наличие конфликтов (например, "использовать Python").
Решение:
Сформируй план действий.
Укажи Технологический Стек (Node.js, Numeric).
ПЕРЕХОДИ К ФАЗЕ 2.
```

**ФАЗА 1: ARCHITECT** — Результат работы Cline:

Cline (как Архитектор) создаёт план:

```markdown
# Архитектура: User Registration API

## Endpoints
- POST /api/auth/register
- POST /api/auth/login
- POST /api/auth/logout

## Технологический стек
- Node.js, Express, TypeScript
- Prisma ORM
- Zod для валидации

## Структура файлов
```
src/api/auth/
  ├── register.ts
  ├── login.ts
  └── types.ts
```

## План реализации
1. Создать Zod schema для регистрации
2. Реализовать endpoint POST /register
3. Добавить валидацию email/password
4. Обработать ошибки (дубликат email, weak password)
```

**Автоматический переход к ФАЗЕ 2:**

Cline продолжает работу автоматически, теперь как Executor:

```markdown
ФАЗА 2: EXECUTOR (Выполнение)
Действуй как Middle Backend Developer.

Контекст:
Используй план из Фазы 1.
Помни про docs/ACCOUNTING_CONSTITUTION.md.
Кодинг:
Пиши код на TypeScript/Node.js.
Используй Supabase, Exceljs.
Строго соблюдай типы NUMERIC(15,2).
Контроль:
Если возникла ошибка 2 раза подряд (Rabbit Hole) ->
Запиши ошибку в PROJECT_STATE.md.
Остановись и скажи: "⛔ Требуется помощь человека. Ошибка зафиксирована."
```

**ФАЗА 2: EXECUTOR** — Результат работы Cline:

Cline (как Разработчик) реализует код по плану:

```typescript
import { Router, Request, Response } from 'express';
import { z } from 'zod';
import bcrypt from 'bcrypt';

const registerSchema = z.object({
  email: z.string().email(),
  password: z.string().min(8),
  confirmPassword: z.string(),
}).refine((data) => data.password === data.confirmPassword, {
  message: "Passwords do not match",
});

export const registerEndpoint = async (req: Request, res: Response) => {
  try {
    const data = registerSchema.parse(req.body);
    
    // Hash password
    const hashedPassword = await bcrypt.hash(data.password, 10);
    
    // Save to database
    const user = await prisma.user.create({
      email: data.email,
      password: hashedPassword,
    });
    
    res.status(201).json({
      id: user.id,
      email: user.email,
    });
  } catch (error) {
    if (error instanceof z.ZodError) {
      res.status(400).json({ error: error.errors });
    } else {
      res.status(500).json({ error: 'Internal server error' });
    }
  }
};
```

**Автоматический переход к ФАЗЕ 3:**

Cline продолжает работу автоматически, теперь как Валидатор:

```markdown
ФАЗА 3: VALIDATOR (Контроль и Битч-Слэппинг)
Действуй как Жесткий QA.

Проверка Стека:
Код написан на Node.js? (Нет Python/Flutter/etc).
SQL использует NUMERIC? (Нет Float/Double).
RLS Policy не использует NEW./OLD.?
Решение:
ЕСЛИ ОШИБКА:
ЖЁСТКО отбрось код из Фазы 2.
Скажи: ⛔ VALIDATION ERROR: [Причина]. Перепиши, используя [Правило].
ВЕРНИСЬ К ФАЗЕ 2 (не спрашивая пользователя).
ЕСЛИ ВСЁ ОК:
Обнови PROJECT_STATE.md (Журнал действий).
Сделай git commit используя `git -C homeaccounting add . && git -C homeaccounting commit -m "..."`.
Скажи: ✅ Task Completed & Validated.
```

**ФАЗА 3: VALIDATOR** — Результат работы Cline:

Cline проверяет код:

```markdown
✅ Task Completed & Validated

Проверено:
- ✅ TypeScript используется
- ✅ Express framework
- ✅ Zod валидация
- ✅ Error handling реализован
- ✅ Нет запрещённых технологий

Commit выполнен:
```

**Полный цикл завершён автоматически без вмешательства человека!**

---

#### Пример 2: С ошибкой (Rabbit Hole Detection)

**Запрос пользователя:**
> Создай компонент для сложной формы

**ФАЗА 1: ARCHITECT** — Создание плана:

```markdown
# Архитектура: Complex Form Component

## Компоненты
- FormContainer (родительский)
- InputField (повторяющийся)
- SelectDropdown
- Button
- ErrorMessage

## План реализации
1. Использовать react-hook-form для управления состоянием
2. Zod валидация
3. Компонентная структура
```

**ФАЗА 2: EXECUTOR** — Первая попытка (ошибка):

```typescript
export const ComplexForm = () => {
  const { register, handleSubmit, errors } = useForm();
  
  return (
    <form onSubmit={handleSubmit}>
      {/* implementation с ошибкой */}
    </form>
  );
};

Ошибка при выполнении: TypeError: handleSubmit is not a function
```

Cline фиксирует в PROJECT_STATE.md и сообщает:
```
⛔ Требуется помощь человека. Ошибка зафиксирована.
```

**ФАЗА 2: EXECUTOR** — Вторая попытка (ошибка):

Cline пробует исправить:
```typescript
export const ComplexForm = () => {
  const { register, handleSubmit } = useForm();  // Исправлено
  // ...
};
```

Вторая ошибка: Форма не сохраняется

Cline снова фиксирует:
```
⛔ Ошибка зафиксирована в PROJECT_STATE.md.
```

**ФАЗА 2: EXECUTOR** — Третья попытка (ошибка):

Cline пробует ещё раз:

Третья ошибка: Валидация не работает

**Автоматическое срабатывание Rabbit Hole Detection:**

Поскольку одна и та же ошибка повторяется 3 раза подряд:

```markdown
## Rabbit Hole Detection

Если ты пытаешься решить одну и ту же ошибку 2 раза подряд:

СТОП. Не повторяй попытку.
Зафиксируй проблему:
```markdown
## ⚠️ Known Error - 2026-01-03

**Context:** Создание компонента ComplexForm
**Error:** TypeError: handleSubmit is not a function
**Attempt 1:** Исправил вызов useForm()
**Attempt 2:** Проверил импорт react-hook-form
**Attempt 3:** Проверил версию библиотеки

**Status:** Requires human intervention
```

Остановись и скажи: "⛔ Ошибка зафиксирована. Требуется помощь человека."
```

Cline сообщает:
```
⛔ ОШИБКА: Зафиксировал проблему в PROJECT_STATE.md.
Проблема требует вмешательства человека.
```

**Человек должен вмешаться!**

В этом случае Cline НЕ БУДЕТ пытаться снова. Он остановился и ждёт помощи.

---

### Ключевые моменты auto-protocol.md

#### 1. Строгое разделение фаз

Каждая фаза имеет:
- **Ясную роль** (Architect, Executor, Validator)
- **Чёткие границы** (что делать и что НЕ делать)
- **Критерий перехода** (когда переходить к следующей фазе)

```markdown
# Из auto-protocol.md

ФАЗА 1: ARCHITECT (Планирование)
❌ НЕ пиши код в этой фазе.
Если написал — ты сломал протокол.

ФАЗА 2: EXECUTOR (Выполнение)
Если возникла ошибка 2 раза подряд (Rabbit Hole) ->
Запиши ошибку в PROJECT_STATE.md.
```

#### 2. Автоматические проверки

Cline автоматически проверяет:
- Типы данных (NUMERIC vs Float)
- Технологический стек (Node.js vs Python)
- SQL RLS политики
- Соответствие плану из Фазы 1

#### 3. Git commit только после успешной валидации

```markdown
ЕСЛИ ВСЁ ОК:
Обнови PROJECT_STATE.md (Журнал действий).
Сделай git commit используя `git -C homeaccounting add . && git -C homeaccounting commit -m "..."`.
```

---

### Как это использовать на практике

#### Сценарий 1: Полный цикл без ошибок

```bash
# Пользователь просто даёт задачу
user: "Создай страницу профиля пользователя"

# Cline автоматически выполняет:
# ФАЗА 1 (Architect) → создаёт план
# ФАЗА 2 (Executor) → пишет код
# ФАЗА 3 (Validator) → проверяет
# ✅ Git commit

Результат: Задача завершена автоматически
```

#### Сценарий 2: С ошибкой, но исправленной

```bash
# Пользователь даёт задачу
user: "Создай кнопку с анимацией"

# Cline:
# ФАЗА 1 → план создан
# ФАЗА 2 → пишет код, но с ошибкой
# ФАЗА 3 → валидатор находит ошибку
# → ПЕРЕХОД обратно к ФАЗЕ 2
# ФАЗА 2 → исправляет
# ФАЗА 3 → проверяет
# ✅ Git commit

Результат: Задача завершена после 1 итерации
```

#### Сценарий 3: Rabbit Hole — требует вмешательство

```bash
# Пользователь даёт задачу
user: "Интегрируй платёжную систему"

# Cline:
# ФАЗА 1 → план создан
# ФАЗА 2 → попытка 1 → ошибка
# ФАЗА 2 → попытка 2 → ошибка  
# ФАЗА 2 → попытка 3 → ошибка
# → ⛔ Rabbit Hole Detection сработывает
# → ⚠️ Сообщение: требует помощи человека

Результат: Cline остановился, человек должен решить проблему
```

---

## 🎯 Пример полного цикла

### Задача: Создать API для управления пользователями

```yaml
ФАЗА 1 (Architect):
───────────────────────────
Читает:
- .clinerules/auto-protocol.md
- roles/backend-executor/instructions.md
- docs/TECH_STACK.md (если существует)

Создаёт:
docs/PLAN.md:
```markdown
# План: User Management API

## Endpoints
- GET /api/users
- POST /api/users
- PUT /api/users/:id
- DELETE /api/users/:id

## Database Schema
- Table: users
- Columns: id, email, name, role, created_at
```

ФАЗА 2 (Executor):
────────────────────────────
Читает:
- docs/PLAN.md
- docs/TECH_STACK.md

Реализует:
src/api/users.ts:
```typescript
import { Router } from 'express';
import { prisma } from '@/lib/prisma';
import { z } from 'zod';

export const createUserRouter = () => {
  const router = Router();
  
  router.post('/', async (req, res) => {
    const schema = z.object({
      email: z.string().email(),
      name: z.string().min(2),
    });
    
    const data = schema.parse(req.body);
    const user = await prisma.user.create({ data });
    
    res.status(201).json(user);
  });
  
  return router;
};
```

ФАЗА 3 (Validator):
─────────────────────────────
Читает:
- docs/PLAN.md
- src/api/users.ts

Проверяет:
- ✅ TypeScript: strict mode
- ✅ Prisma используется корректно
- ✅ Zod валидация присутствует
- ✅ Error handling реализован

Результат:
```
✅ VALIDATION PASSED

Проверено:
- ✅ Tech stack соблюдён
- ✅ Architecture соответствует плану
- ✅ Code quality стандарты выполнены

git add . && git commit -m "feat: Add user management API"
```

---

## 📝 Резюме

### Когда использовать .clinerules

- ✅ Проекты с чётко определённым стеком
- ✅ Требования жёсткой валидации
- ✅ Нужна автоматизация переходов между фазами
- ✅ Хотите использовать существующую проверенную систему

### Когда использовать AIRules шаблоны

- ✅ Нужна гибкость в ролях
- ✅ Разные стеки в одном проекте
- ✅ Быстрое создание новых правил
- ✅ Хотите использовать готовые роли целиком

### Рекомендация

**Комбинируйте оба подхода для максимальной эффективности:**

```yaml
База:
  .clinerules/ для автоматизации фаз и валидации

Расширения:
  AIRules/examples/ для готовых ролей на разные задачи

Интеграция:
  В auto-protocol.md добавьте проверку docs/PROJECT_ROLES.md
  При старте фазы загружайте роль из PROJECT_ROLES.md
```

---

**Создано для эффективной работы с LLM** 🚀
