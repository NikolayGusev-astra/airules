# Memory Graph Integration - Примеры использования

## 🎯 Обзор

Memory Graph MCP — это персистентный слой знаний о проекте для LLM. Он хранит:
- Сущности (типы, интерфейсы, компоненты)
- Связи (imports, uses)
- Метаданные (наблюдения)

Это позволяет LLM:
- Понимать, как типы используются в проекте
- Предсказывать последствия изменений
- Избегать циклических зависимостей
- Найти неиспользуемый код

---

## 🚀 Быстрый старт

### Шаг 1: Настройка нативных Git hooks

```bash
# Создать директорию hooks (если нет)
mkdir -p .git/hooks

# Создать pre-commit hook
cat > .git/hooks/pre-commit << 'EOF'
#!/bin/sh
# Git pre-commit hook
# Автоматическая синхронизация онтологического графа

echo "🔍 Pre-commit: Анализ зависимостей..."
node scripts/ontology/analyze-dependencies.js
git add docs/DEPENDENCIES.json
EOF

# Сделать исполняемым
chmod +x .git/hooks/pre-commit

# Создать pre-push hook
cat > .git/hooks/pre-push << 'EOF'
#!/bin/sh
# Git pre-push hook
# Валидация онтологического графа

echo "🔍 Pre-push: Валидация..."
node scripts/ontology/validate-graph.js
EOF

# Сделать исполняемым
chmod +x .git/hooks/pre-push
```

### Шаг 2: Тестирование hooks

```bash
# Протестировать pre-commit
git add --all && git commit -m "test commit"

# Проверить, что DEPENDENCIES.json создан
ls -la docs/DEPENDENCIES.json

# Протестировать pre-push
git push origin main --dry-run

# Или протестировать валидацию
node scripts/ontology/validate-graph.js
```

### Шаг 3: Проверка Memory Graph

```bash
# Проверить, что Memory Graph доступен
export MCP_MEMORY_AVAILABLE=true

# Запустить анализ с включённым Memory Graph
node scripts/ontology/analyze-dependencies.js

# Должно показать: "🧠 Memory Graph MCP: Доступен"
```

---

## 📖 Сценарии использования

### Сценарий 1: Редактирование типа

**Запрос пользователя:**
> "Переименуй поле email в emailAddress в типе User"

**Процесс LLM:**

1. **Поиск типа в Memory Graph:**
```
LLM:
→ search_nodes(query: "User interface")
→ Получить: User.ts, AuthService.ts, UserProfile.tsx

→ open_nodes(names: ["Type_User"])
→ Получить:
   - Name: Type_User
   - Observations: [
       "Defined in: src/types/User.ts",
       "Category: type",
       "Exports: 5 items",
       "Used in: AuthService.ts, UserProfile.tsx, TransactionList.tsx"
     ]
```

2. **Анализ последствий:**
```
Изменение: email → emailAddress

Последствия:
✗ authService.ts (строка 15): валидация email сломается
✗ userProfile.tsx (строка 23): форма авторизации сломается  
✗ transactionList.tsx (строка 47): отображение email исчезает
✗ notificationService.ts (строка 12): email шаблоны устаревают

Всего затронуто: 4 файла
```

3. **Предупреждение:**
```
⚠️ Переименование затронет 4 файла:

Изменения:
- src/types/User.ts
- src/services/authService.ts
- src/components/userProfile.tsx
- src/components/transactionList.tsx
- src/services/notificationService.ts

Рекомендуется:
1. Открыть каждый файл
2. Заменить email → emailAddress
3. Проверить типы
4. Запустить тесты
```

4. **Редактирование:**
```
Открывает: src/types/User.ts
Изменяет: email → emailAddress
Сохраняет: файл
Git commit: автоматически синхронизируется
```

### Сценарий 2: Добавление нового типа

**Запрос пользователя:**
> "Создай тип Subscription с полями id, userId, plan, startDate"

**Процесс LLM:**

1. **Проверка существования:**
```
LLM:
→ search_nodes(query: "Subscription")
→ Результат: Тип не найден

→ search_nodes(query: "userId")
→ Результат: userId существует в Type_User

Вывод: Тип Subscription уникален, можно создавать
```

2. **Создание типа:**
```
Создаёт: src/types/Subscription.ts

export interface Subscription {
  id: string;
  userId: UserId;
  plan: 'basic' | 'pro' | 'enterprise';
  startDate: Date;
  endDate?: Date;
}
```

3. **Git commit:**
```
pre-commit hook:
→ Запустится analyze-dependencies.js
→ DEPENDENCIES.json обновлён
→ Memory Graph синхронизируется (если доступен)
→ Git commit успешен
```

### Сценарий 3: Удаление неиспользуемого типа

**Запрос пользователя:**
> "Удали тип OldTransaction, он больше не используется"

**Процесс LLM:**

1. **Проверка использования:**
```
LLM:
→ open_nodes(names: ["Type_OldTransaction"])
→ Получить:
   - Type_OldTransaction
   - Observations: [
       "Defined in: src/types/OldTransaction.ts",
       "Exports: 3 items",
       "Used in: [ни один файл]"
     ]

Вывод: Тип не используется, можно удалять
```

2. **Удаление:**
```
Удаляет: src/types/OldTransaction.ts

Git commit: pre-commit обновляет граф
pre-push: валидация успешна
```

### Сценарий 4: Рефакторинг с циклическими зависимостями

**Запрос пользователя:**
> "Извлеки общую логику из AuthService и UserService в отдельный модуль"

**Процесс LLM:**

1. **Анализ зависимостей:**
```
LLM:
→ search_nodes(query: "AuthService")
→ Получить:
   - File_AuthService.ts
   - Imports: User, Token, Password

→ search_nodes(query: "UserService")
→ Получить:
   - File_UserService.ts
   - Imports: User, Database

Вывод: Оба сервиса используют User
```

2. **Поиск циклов:**
```
Запускается: validate-graph.js

Результат: ЦИКЛ ОБНАРУЖЕН!

Цикл:
AuthService.ts
  → UserService.ts
    → AuthService.ts (цикл!)

Рекомендация:
1. Создать src/types/AuthLogic.ts
2. Перенести общую логику туда
3. Изменить AuthService и UserService на использование AuthLogic
```

3. **Рефакторинг:**
```
Создаёт: src/types/AuthLogic.ts

Рефакторит: AuthService.ts, UserService.ts
Git commit: автоматическая синхронизация
```

---

## 🧠 Работа с Memory Graph MCP

### Использование в AIRules

**Автоматическое включение:**
```yaml
# auto-protocol.md
ФАЗА 2.5: ONTOLOGY_SYNCER
→ Автоматически проверяет Memory Graph
→ Синхронизирует типы и связи
→ Создаёт отчёты
```

**Ручное использование:**
```javascript
// В файле .clinerules/roles/ontologist-syncer/instructions.md

// Поиск типа
search_nodes(query: "User interface")

// Открыть узел
open_nodes(names: ["Type_User", "Type_Transaction"])

// Чтение полного графа
read_graph()
```

### Доступные операции

1. **create_entities** — создание сущностей
2. **create_relations** — создание связей
3. **add_observations** — добавление метаданных
4. **read_graph** — чтение полного графа
5. **search_nodes** — поиск сущностей

### Структура сущности

```javascript
{
  name: "Type_User",
  entityType: "typescript_type",
  observations: [
    "Defined in: src/types/User.ts",
    "Category: type",
    "Exports: 5 items",
    "Used in: AuthService, UserProfile, TransactionList"
  ]
}
```

---

## 📊 Валидация и отчёты

### DEPENDENCIES.json

Структура отчёта:
```json
{
  "timestamp": "2026-01-08T12:00:00Z",
  "summary": {
    "totalFiles": 45,
    "totalNodes": 45,
    "totalEdges": 120,
    "cyclesCount": 0,
    "unusedExportsCount": 3
  },
  "typeNodes": [...],
  "graph": {
    "nodes": [...],
    "edges": [...]
  },
  "cycles": [],
  "unusedExports": [...]
}
```

### validate-graph.js

Проверки:
- ✅ Циклические зависимости
- ✅ Неиспользуемые экспорты
- ✅ Актуальность анализа
- ✅ Целостность графа

---

## 🔧 Интеграция с package.json

Добавьте скрипты:
```json
{
  "scripts": {
    "analyze-deps": "node scripts/ontology/analyze-dependencies.js",
    "validate-graph": "node scripts/ontology/validate-graph.js",
    "sync-types": "node scripts/ontology/sync-types.js",
    "check-ontology": "npm run analyze-deps && npm run validate-graph"
  }
}
```

---

## 💡 Лучшие практики

### 1. Всегда проверять перед редактированием

```bash
# Перед изменением типа
search_nodes(query: "TypeName")

# Проверить использование
open_nodes(names: ["Type_TypeName"])
```

### 2. Проверять последствия

Всегда анализировать:
- Какие файлы затронет изменение
- Может ли это сломать существующий код
- Нужно ли обновить тесты

### 3. Использовать Git hooks

- pre-commit: автоматическая синхронизация
- pre-push: валидация перед отправкой
- Не вручную запускать скрипты

### 4. Регулярно обновлять Memory Graph

Memory Graph должен содержать актуальный граф:
- После каждого рефакторинга
- После добавления новых типов
- После удаления неиспользуемого кода

### 5. Логировать изменения

Используйте git commit messages для документации:
```
git commit -m "refactor(auth): Extract common logic to AuthLogic"

git commit -m "feat(types): Add Subscription interface"
```

---

## 🎓 Заключение

Memory Graph интеграция с AIRules обеспечивает:

✅ **Автоматическая синхронизация** — pre-commit hook
✅ **Валидация перед push** — pre-push hook
✅ **LLM контекст** — актуальные связи типов
✅ **Предсказание последствий** — анализ зависимостей
✅ **Качество кода** — отсутствие циклов и чистота
✅ **Онтологическое соответствие** — следование схеме AIRules

**Результат:** LLM всегда понимает проект структуру и может делать безопасные изменения!