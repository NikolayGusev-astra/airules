# 🚀 AI Best Practices - Руководство для работы с LLM-инструментами

**Цель:** Превратить AI из "генератора случайного кода" в "стабильную IDE уровня Senior-разработчика".

---

## 1. 🧹 Управление Контекстом (Context Hygiene)

Самая частая ошибка — забивать контекстное окно всем подряд ("дай мне весь проект"). AI начинает "забывать" начало кода или галлюцинировать связи.

### Правило "Снизу-Вверх" (Bottom-Up Context)

```bash
# ✅ ПРАВИЛЬНО - Используйте @Symbol
@symbol calculateTotalPrice

# ❌ НЕПРАВИЛЬНО - Перечитывайте весь файл
@file /path/to/very/long/file.ts
```

**Почему это работает:**
- @Symbol фокусируется только на одной функции
- Снижает шум и ускоряет ответы
- Позволяет работать с файлами на 500+ строк

### Cline: Используйте команду /add

```bash
# ✅ ПРАВИЛЬНО - Добавляйте только нужные файлы
/add src/controllers/question.controller.ts
/add src/services/question.service.ts
/add src/repositories/question.repository.ts

# ❌ НЕПРАВИЛЬНО - Добавляйте весь проект
/add backend/
/add frontend/
/add docs/
```

### Изоляция задач (Task Isolation)

```bash
# ❌ НЕПРАВИЛЬНО - Смешивание задач
"Создай backend для авторизации"
"А теперь стилизуй кнопку логина"
"И еще вот этот компонент"

# ✅ ПРАВИЛЬНО - Одна задача за раз
"Создай backend для авторизации"
[Завершение задачи]
"Стилизуй кнопку логина"
[Завершение задачи]
```

**Правило:** AI "засоряется" предыдущей логикой. Завершайте сессию (Topic) после решения конкретной задачи.

### Создайте .cursorignore

```
# .cursorignore - Аналог .gitignore
node_modules/
.next/
dist/
build/
*.log
coverage/
.env.local
.DS_Store
```

**Почему это важно:**
- Ускоряет ответы AI
- Убирает лишний шум из индексации
- Предотвращает "съедение" токенов на сгенерированные файлы

---

## 2. 🔪 Разработка через Вертикальные Срезы (Vertical Slicing)

Вместо того чтобы просить AI "Сделай фронтенд для этой таблицы", используйте подход разработки через срезы.

### Практика: Создайте срез, а не абстракцию

```bash
# ❌ НЕПРАВИЛЬНО - "Сделай фронтенд для этой таблицы"
# Результат: 10 файлов абстрактного кода, который потом не собирается

# ✅ ПРАВИЛЬНО - "Создай интерфейс, mocked API и обработчик событий для одной конкретной строки таблицы"
# Результат: Код, который сразу можно запустить и потестировать
```

**Почему это работает:**
- AI пишет код, который сразу можно запустить и потестировать
- Лучше, чем писать 10 файлов абстрактного кода, который потом не собирается
- Позволяет постепенно наращивать функционал

### Cursor: Используйте Composer (Cmd+I)

Composer видит несколько файлов одновременно и может построить связку (срез) быстрее, чем чат.

---

## 3. 🧪 AI-Driven TDD (Test-First Development)

Это лучшее средство от галлюцинаций.

### Практика: Сначала тесты, потом код

```bash
# ❌ НЕПРАВИЛЬНО
"Напиши функцию авторизации"

# ✅ ПРАВИЛЬНО
"Напиши набор тестов (Jest/Vitest) для функции авторизации, которая должна выбрасывать ошибку, если пароль короче 8 символов."
[Тесты созданы]
"Теперь напиши реализацию, чтобы эти тесты проходили."
```

### Cline: Режим авто-тестов

```typescript
// Включите режим авто-тестов. 
// Позвольте агенту самому запустить npm test после написания кода.
// Если тесты упали, агент сам исправит код (Self-healing), не вовлекая вас в цикл правок.
```

**Self-healing пример:**
1. AI пишет код + тесты
2. Запускает `npm test`
3. Тесты падают
4. AI сам исправляет код
5. Запускает `npm test` снова
6. ✅ Тесты проходят

**Эффект:** Вы не вовлечены в цикл правок, AI сам исправляет ошибки.

---

## 4. 🦆 Метод "Резиновой Утки 2.0" (Rubber Ducking)

Вместо того чтобы просить AI исправить код, попросите его объяснить код или вашу идею.

### Scenario: Вы застряли

```bash
# ❌ НЕПРАВИЛЬНО
"Напиши сложную логику кэширования."

# ✅ ПРАВИЛЬНО
"У меня есть задача кэшировать данные. Вот мой план:
1. Использовать Redis
2. Создать middleware для кэширования
3. Добавить TTL для ключей

Найди логические ошибки в моем плане или предложи архитектурный паттерн, который лучше подходит для Node.js."
```

**Эффект:** AI часто находит проблему в мышлении разработчика еще до написания кода. Это экономит часы переписывания.

---

## 5. 👁 Работа с Diff-ами (Diff-Driven Workflow)

Никогда не нажимайте "Apply All" (Принять всё) слепо, если в задаче больше 3-4 файлов.

### Практика: Постепенное применение

```bash
# ❌ НЕПРАВИЛЬНО - Apply All на 10 файлов
1. AI предложил огромный рефакторинг
2. Нажали Apply All
3. Весь проект сломан
4. 4 часа на откат и понимание что случилось

# ✅ ПРАВИЛЬНО - Постепенное применение
1. AI предложил огромный рефакторинг
2. Разбили на части по 2-3 файла
3. Применили первую часть
4. Сохранили, запустили
5. Всё работает? Идем к следующей части
```

### Прием: Если большой рефакторинг

```bash
# Скопируйте 10% изменений, примените, сохраните, запустите
# Почему: AI часто ломает импорты или типы в середине большого файла
# Постепенное применение позволяет быстро откатиться (Cmd+Z) и переформулировать запрос
```

---

## 6. ⚓ Аннотации и "Якоря" (Anchoring)

AI часто теряет нить, если проект большой. Используйте якоря в коде.

### Практика: Комментарии-задачи для AI

```typescript
// TODO: Refactor this function to use async/await pattern as per SPEC-2024-01
function oldWay() {
  // Legacy code
}

// TODO: Implement rate limiting for this endpoint as per SECURITY-2024-02
export const authController = {
  login: async (req, res) => {
    // Code without rate limiting
  }
};

// TODO: Add error handling for database connection failures
export const questionService = {
  getAll: async () => {
    // Code without error handling
  }
};
```

### Prompt для реализации

```bash
"Найди все комментарии, начинающиеся с TODO, и реализуй их в соответствии с указанным спеком."
```

**Эффект:** Это превращает код в базу задач, которая всегда синхронизирована с тем, что видит AI.

---

## 7. 🛡️ Строгая типизация как подсказка (Type-Safety as Prompt)

Это особенно касается TypeScript/Python.

### Практика: Сначала типы, потом код

```bash
# ❌ НЕПРАВИЛЬНО
"Напиши функцию для обработки ответа API"
// AI "додумает" типы, которые могут не соответствовать реальности

# ✅ ПРАВИЛЬНО
"Вот TypeScript interface для ответа API:
interface ApiResponse {
  data: User[];
  meta: {
    total: number;
    page: number;
  };
}

Напиши функцию для обработки этого ответа."
// AI пишет точный код, который соответствует интерфейсу
```

### Cursor: Генерация типов из API

```bash
# Попросите AI "сгенерировать типы на основе JSON ответа от API"
# Это дает вам 100% гарантию, что фронтенд и бэкенд согласованы
```

---

## 📊 Резюме: Профиль "Супер-юзера"

Если объединить всё, идеальный workflow выглядит так:

### 1. Подготовка
- В `.cursorrules` забиты технологии
- В проекте лежит `SPEC.md` (или аналоги)
- Создан `.cursorignore`

### 2. Планирование
- Вы обсуждаете задачу с AI через "Резиновую утку" (объясняете идею)
- Находите архитектурные ошибки до написания кода

### 3. Декомпозиция
- AI разбивает задачу на файлы
- Вы используете Vertical Slicing (срезы)

### 4. TDD
- Вы просите сгенерировать тесты сначала
- Включаете режим авто-тестов (если есть)

### 5. Реализация
- Вы используете Composer (срезы) или Inline (атомарные правки)
- Но не просите сделать "всё сразу"

### 6. Верификация
- Вы читаете Diff
- Запускаете линтер/тесты (или Cline делает это сам)
- Постепенно применяете изменения

---

## 🎯 Ключевые принципы

| Принцип | ✅ Do | ❌ Don't |
|----------|---------|-----------|
| Контекст | Используйте @Symbol / /add | Добавляйте весь проект |
| Задачи | Одна задача за сессию | Смешивайте темы |
| Срезы | Создавайте вертикальные срезы | Пишите абстрактный код |
| TDD | Сначала тесты | Сначала код |
| План | Обсуждайте с AI | Просите код сразу |
| Diff | Постепенное применение | Apply All |
| Якоря | TODO-комментарии | Магия "он поймет" |
| Типы | Задайте сигнатуры | "Додумайте" |

---

## 🚀 Это превращает AI из:

- ❌ Генератора случайного кода
- ❌ Инструмента для "сделай всё сразу"
- ❌ Источника галлюцинаций

В:

- ✅ Интеллектуального компилятора ваших идей
- ✅ Партнёра для обсуждения архитектуры
- ✅ Тестировщика, который сам исправляет ошибки
- ✅ Стабильной IDE уровня Senior-разработчика

---

**Создано для эффективной работы с LLM-инструментами** 🎯
